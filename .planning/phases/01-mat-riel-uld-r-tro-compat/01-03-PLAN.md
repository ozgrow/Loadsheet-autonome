---
phase: 01-mat-riel-uld-r-tro-compat
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - static/js/app.js
  - static/css/style.css
  - tests/tests.html
autonomous: true
gap_closure: true
requirements:
  - RECAP-01
  - MAT-11
  - MAT-12
  - MAT-13
  - MAT-14
  - TEST-02

# CONTEXT OVERRIDE (decision prise lors du gap closure planning) :
# - D-13 (#liveRecap inchangé) : CONSERVÉ (la barre globale reste ULD/Colis/Poids/LTA).
# - D-14 (badge neutre suffisant) : ANNULÉ — le badge est remplacé par un récap condensé inline
#   sous le header de chaque ULD, conformément à la lecture littérale du critère #3 du ROADMAP.
# Source : 01-VERIFICATION.md "Option A — alignement strict" (recommandation post-vérification)
# Le commentaire libre uldComment, désormais réintroduit visuellement dans le DOM, devient un nouveau
# vecteur XSS et DOIT passer par esc() (MAT-11) avec troncature pour préserver le layout mobile.
#
# AJOUT 2026-04-28 (gap closure étendu, avant mise en prod) :
# Trois nouveaux requirements ont été ajoutés au scope Phase 1 sur demande utilisateur après
# verification (cf. REQUIREMENTS.md MAT-12/13/14 ajoutés 2026-04-28). Plan 01-03 réécrit pour
# couvrir ces 3 ajouts en plus du gap RECAP-01 initial :
# - MAT-12 : case "Rien à facturer" dans le modal matériel (raccourci explicite, désactive les
#   autres champs visuellement et stocke un flag noMaterialToBill côté ULD).
# - MAT-13 : la saisie matériel devient OBLIGATOIRE — tentative d'ajouter une 2e ULD sans avoir
#   rempli la 1ère, ou tentative de générer PDF/email avec une ULD vide → bloqué + alert FR.
#   Définition "matériel saisi" : noMaterialToBill=true OU au moins un champ matériel > 0/forfait
#   coché OU uldComment non-vide.
# - MAT-14 : à chaque création d'une nouvelle ULD via le bouton "+ Ajouter une ULD" (et la
#   première ULD au démarrage d'un nouveau manifeste), le modal matériel s'ouvre automatiquement.
#   loadManifest() doit appeler addUld(autoOpen=false) pour ne PAS ouvrir le modal lors d'un
#   rechargement de manifeste sauvegardé (action utilisateur uniquement).
# Conséquence : ce plan dépend désormais de 01-01 (data model + modal initial) ET 01-02 (boutons
# Générer PDF + Envoyer email — surfaces de validation MAT-13).
#
# AJOUT 2026-04-28 (revision 1/3 — checker feedback) :
# - Blocker #1 : ≥ 8 sites pré-existants `addUld(); addUld();` dans tests/tests.html vont casser
#   sous MAT-13 → Task 3 étend la signature à `addUld(autoOpen=true, skipValidation=false)` pour
#   bypass interne en mode test ; nouvelle Task 5 patche les sites identifiés.
# - Blocker #2 : refreshMaterialBadge split entre Task 1 et Task 4 → toute la logique JS migre
#   dans Task 1, Task 4 reste strictement CSS-only.
# - Blocker #3 : `grep -nE ... >= N` non shell-portable → tous les <verify> utilisent `grep -cE`.
# - Blocker #4 : Test 8/9 race condition alert → `await (async function() { ... })()` + filtrage
#   par substring au lieu de `.pop()`.

must_haves:
  truths:
    # ---- Feature A : Récap inline condensé (RECAP-01 + MAT-11 conservés du plan original)
    - "Sous le header de chaque ULD ayant au moins un champ matériel non-vide OU la case 'Rien à facturer' cochée, un récap inline condensé s'affiche listant soit les valeurs saisies (`Sangles: 3 | Planchers EU: forfait | Com: Fragile`) soit le libellé `Rien à facturer`"
    - "Le format de chaque entrée est `<libellé FR>: <valeur>` séparé par ` | ` (pipe entouré d'espaces)"
    - "Les champs matériel à 0 (et forfait=false) sont OMIS du récap (cohérent avec D-16 / specifics PDF)"
    - "Les checkbox forfait cochées s'affichent littéralement avec le mot `forfait` (cohérent avec D-16 PDF/email)"
    - "Le commentaire libre `uldComment` apparaît tronqué (max 30 caractères + `…` si plus long) et échappé via esc() — XSS payload affiché littéralement, jamais exécuté (MAT-11)"
    - "Si TOUS les champs matériel d'une ULD sont vides/0/false ET case 'Rien à facturer' non cochée, le récap n'affiche rien (pas de span vide, pas de badge fantôme)"
    - "Le récap reste lisible sur écran ≤ 768px (wrap naturel ou word-break au pipe, pas d'overflow horizontal du bloc ULD)"
    - "La barre globale #liveRecap (ULD/Colis/Poids/LTA) reste strictement inchangée (D-13 conservé)"
    # ---- Feature B : Case "Rien à facturer" (MAT-12)
    - "Le modal matériel contient une checkbox `#mat-no-billing` avec label `Rien à facturer pour cette ULD` placée AU-DESSUS de la grille des champs matériel (visuellement séparée)"
    - "Cocher la case 'Rien à facturer' désactive (disabled + grisé) tous les autres inputs/checkbox du modal (sangles, planchers, bois calage, bâches, intercalaires, nids d'abeille, commentaire ULD, checkbox forfaits)"
    - "Décocher la case 'Rien à facturer' réactive tous les autres champs (les checkbox forfaits restent grisées si elles désactivent leur input nombre, comportement D-06 préservé)"
    - "applyMaterialToUld écrit `data-no-billing` (true/false) sur le bloc ULD ; quand noMaterialToBill=true, les autres data-attributes matériel sont mis à 0/false/'' (sauvegarde explicite du raccourci) ET applyMaterialToUld neutralise visuellement les checkbox forfait dans le modal si no-billing coché"
    - "loadManifest lit défensivement `noMaterialToBill` (absence = false, rétro-compat MAT-10 préservée)"
    - "Le récap inline affiche littéralement `Rien à facturer` (pas la liste de zéros) quand la case est cochée — précédence : noBilling > VRAC > champs détaillés"
    - "Le PDF page ULD section Matériel affiche la ligne unique `Rien à facturer` (pas de tableau matériel) quand la case est cochée"
    - "L'email HTML section Matériel par ULD affiche la ligne unique `Rien à facturer` (échappée via esc()) quand la case est cochée"
    - "refreshMaterialBadge émet la classe CSS `.mat-recap-no-billing` sur le span `.material-recap` quand `block.dataset.noBilling === 'true'`, pour signal visuel distinct via CSS"
    # ---- Feature C : Validation matériel obligatoire (MAT-13)
    - "Une fonction helper `uldHasMaterial(block)` retourne true si noMaterialToBill=true OU au moins un champ matériel numérique > 0 OU un forfait coché OU uldComment non-vide"
    - "Le bouton `+ Ajouter une ULD` (index.html ligne 105 : addUld()) bloque l'ajout si AU MOINS une ULD existante n'a PAS de matériel saisi — affiche `alert('ULD N°{n} : matériel non saisi. Cochez \"Rien à facturer\" ou remplissez au moins un champ avant d\\'ajouter une nouvelle ULD.')` puis ouvre le modal de l'ULD incomplète"
    - "Le bouton `Générer le PDF` (showGenerateSection -> generatePdf) bloque la génération si une ou plusieurs ULD n'ont pas de matériel saisi — affiche `alert('Matériel non saisi pour : ULD N°1, ULD N°3. Veuillez remplir le matériel ou cocher \"Rien à facturer\" pour ces ULD.')` listant TOUTES les ULD incomplètes"
    - "Le bouton `Envoyer par email` (sendEmail) bloque l'envoi avec le même message d'alert que generatePdf — validation appliquée AVANT le check destinataires/JWT"
    - "Les 3 alerts utilisent le pattern `alert()` existant du projet (cohérent avec validateRequired app.js:484, saveManifest:572, generatePdf:1106, sendEmail:1124)"
    # ---- Feature D : Auto-ouverture modal sur création (MAT-14)
    - "addUld accepte deux paramètres `autoOpen` (default true) et `skipValidation` (default false) ; quand autoOpen=true, le modal matériel s'ouvre automatiquement à la fin de la création de l'ULD ; quand skipValidation=true, la validation MAT-13 est court-circuitée (usage interne / tests uniquement)"
    - "Le bouton `+ Ajouter une ULD` (index.html ligne 105) appelle `addUld()` (sans argument => autoOpen=true, skipValidation=false)"
    - "newManifest() appelle `addUld()` (autoOpen=true par défaut) pour la première ULD du manifeste vierge"
    - "loadManifest() N'utilise PAS addUld — il construit son propre HTML de bloc ULD via la duplication existante app.js:608-660 — donc le modal NE s'ouvre PAS lors d'un rechargement (comportement attendu MAT-14)"
    # ---- TEST-02 réinforcé
    - "Round-trip save/load préserve le champ noMaterialToBill (test dans tests.html)"
    - "loadManifest restaure le récap inline pour chaque ULD chargée — affiche `Rien à facturer` si flag présent dans le manifeste sauvegardé"
    - "tests/tests.html contient au minimum 8 nouveaux tests couvrant les 4 features (récap 'Rien à facturer', case désactive autres champs, validation Add ULD, validation PDF, validation Email, helper uldHasMaterial, auto-open via addUld, loadManifest n'auto-ouvre pas)"
    - "Les ≥ 8 sites pré-existants utilisant `addUld(); addUld();` dans tests/tests.html ont été migrés vers `addUld(false, true);` pour rester compatibles avec MAT-13 (validation MAT-13 court-circuitée + modal pas auto-ouvert) — préserve la sémantique d'origine de ces tests"

  artifacts:
    - path: "static/js/app.js"
      provides: "Modifications : (1) refreshMaterialBadge intègre noMaterialToBill ET émet la classe .mat-recap-no-billing, (2) openMaterialModal contient checkbox #mat-no-billing + handler toggleNoBilling, (3) applyMaterialToUld lit noMaterialToBill et neutralise les autres champs si coché, (4) addUld accepte (autoOpen=true, skipValidation=false) et ouvre le modal en fin de fonction, (5) loadManifest lit défensivement uldData.noMaterialToBill, (6) helper uldHasMaterial(block) ajouté, (7) addUld + showGenerateSection + sendEmail intègrent la validation MAT-13 avec alerts FR"
      contains: "function uldHasMaterial"
    - path: "static/css/style.css"
      provides: "Classes additionnelles UNIQUEMENT (pas de logique JS) : .mat-no-billing-row (zone séparée en haut du modal), .mat-recap-no-billing (style spécifique du récap quand 'Rien à facturer'), règles disabled visuelles renforcées (préservation pattern existant input:disabled), version mobile compacte"
      contains: ".mat-no-billing"
    - path: "tests/tests.html"
      provides: "≥ 8 nouveaux tests : récap 'Rien à facturer' affiché, case coche désactive les autres champs, applyMaterialToUld neutralise quand coché, loadManifest restaure noMaterialToBill, uldHasMaterial logique complète, validation Add ULD bloquée + alert, validation generatePdf bloquée + alert (async), validation sendEmail bloquée + alert (async), addUld(autoOpen=true) ouvre le modal, addUld(false, true) ne l'ouvre pas + skip validation, loadManifest n'auto-ouvre pas. PLUS : adaptation in-place des ≥ 8 sites pré-existants `addUld(); addUld();` migrés vers `addUld(false, true);` (Task 5)"
      contains: "Materiel ULD - rien a facturer"

  key_links:
    - from: "applyMaterialToUld (app.js)"
      to: "refreshMaterialBadge(uldIndex)"
      via: "appel direct"
      pattern: "refreshMaterialBadge\\(uldIndex\\)"
    - from: "loadManifest (app.js:608-660 forEach)"
      to: "refreshMaterialBadge(i) + setAttribute('data-no-billing', ...)"
      via: "appel direct + écriture défensive du nouveau data-attribute"
      pattern: "data-no-billing|noMaterialToBill"
    - from: "refreshMaterialBadge -> formatCondensedMaterial"
      to: ".uld-block dataset (data-no-billing inclus)"
      via: "lecture du flag pour court-circuiter et retourner 'Rien à facturer' si true + émission classe .mat-recap-no-billing"
      pattern: "block\\.dataset\\.noBilling|dataset\\.noBilling|mat-recap-no-billing"
    - from: "addUld(autoOpen=true)"
      to: "openMaterialModal(i)"
      via: "appel direct en fin de fonction (MAT-14)"
      pattern: "if \\(autoOpen\\)"
    - from: "addUld() bouton (index.html ligne 105)"
      to: "uldHasMaterial(block) sur toutes les ULD existantes"
      via: "validation bloquante dans addUld AVANT uldCount++ (sauf si skipValidation=true)"
      pattern: "uldHasMaterial|findIncompleteUlds"
    - from: "generatePdf / sendEmail"
      to: "uldHasMaterial sur toutes les ULD via collectData ou directement les .uld-block"
      via: "validation bloquante AVANT validateRequired/collectData"
      pattern: "findIncompleteUlds"
    - from: "PDF buildUldMaterialRows / email buildUldMaterialHtml"
      to: "lecture u.noMaterialToBill"
      via: "branche court-circuit retournant [['', 'Rien à facturer']] / HTML simplifié"
      pattern: "noMaterialToBill|Rien à facturer"
    - from: "récap inline rendu"
      to: "uldComment"
      via: "esc() + troncature avant innerHTML"
      pattern: "esc\\("
---

<objective>
Compléter Phase 1 avant mise en prod via 4 sous-features livrées dans un plan unique :

**(A) Récap inline condensé (RECAP-01 + MAT-11)** — fermer le gap identifié par 01-VERIFICATION.md : remplacer le badge neutre `'Matériel saisi'` (refreshMaterialBadge app.js) par un récap condensé qui affiche réellement les valeurs saisies pour chaque ULD, sous la forme `Sangles: 3 | Planchers EU: forfait | Bois calage: 2 | Com: Fragile`.

**(B) Case "Rien à facturer" (MAT-12)** — nouvelle checkbox dans le modal matériel servant de raccourci explicite : quand cochée, désactive visuellement tous les autres champs et stocke un flag `noMaterialToBill: true` qui propage dans la sauvegarde, le récap inline (`Rien à facturer` littéral), le PDF et l'email.

**(C) Saisie matériel obligatoire (MAT-13)** — validation bloquante sur 3 boutons : ajout d'une nouvelle ULD si une ULD existante n'a PAS de matériel saisi, génération PDF, envoi email. Définition "matériel saisi" : noMaterialToBill=true OU au moins un champ numérique > 0 OU un forfait coché OU uldComment non-vide. Messages FR via `alert()` (pattern projet).

**(D) Auto-ouverture du modal sur création ULD (MAT-14)** — le modal matériel s'ouvre automatiquement à chaque appel de `addUld()` (premier ULD au démarrage + chaque clic "Ajouter ULD"). loadManifest reconstruit ses ULD via du HTML inline (sans passer par addUld), donc le modal ne s'ouvre PAS au rechargement d'un manifeste sauvegardé — comportement attendu MAT-14.

**Purpose:** Le critère #3 du ROADMAP exige littéralement « Le récapitulatif écran affiche les infos matériel de chaque ULD sous forme condensée » et l'utilisateur a ajouté 3 exigences avant prod (raccourci facturation, validation bloquante, auto-ouverture) pour cadrer l'usage opérationnel des agents ATH. Ce plan aligne le produit sur 6 requirements (RECAP-01, MAT-11, MAT-12, MAT-13, MAT-14, TEST-02) sans toucher aux 5 critères Phase 1 déjà VERIFIED.

**Output:**
- `static/js/app.js` : `formatCondensedMaterial` étendu (branche noMaterialToBill), `refreshMaterialBadge` étendu (émission classe `.mat-recap-no-billing`), `openMaterialModal` étendu (checkbox no-billing + handler toggleNoBilling), `applyMaterialToUld` étendu (lecture flag + neutralisation + désactivation visuelle des forfait), `addUld` accepte `(autoOpen, skipValidation)` + valide MAT-13 (sauf si skipValidation=true), `loadManifest` lit défensivement noMaterialToBill, helper `uldHasMaterial(block)` ajouté, `generatePdf` + `sendEmail` valident MAT-13 en amont, `buildUldMaterialRows` + `buildUldMaterialHtml` court-circuitent à `Rien à facturer` si flag, conservation de tous les helpers Plan 01-01/01-02.
- `static/css/style.css` : nouvelle zone `.mat-no-billing-row` au-dessus du grid + style `.mat-recap-no-billing` pour le récap spécifique + règles disabled visuelles pour les inputs/checkboxes neutralisés par "Rien à facturer". **CSS-ONLY** : aucune modification JS dans cette task.
- `tests/tests.html` : ≥ 8 nouveaux tests couvrant les 4 features + 1 test round-trip TEST-02 spécifique à noMaterialToBill ; **adaptation in-place des ≥ 8 sites pré-existants** `addUld(); addUld();` migrés vers `addUld(false, true);` (Task 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-VERIFICATION.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-01-PLAN.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-02-PLAN.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-01-SUMMARY.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-02-SUMMARY.md
@CLAUDE.md
@static/js/app.js
@static/css/style.css
@index.html
@tests/tests.html

<interfaces>
<!-- État actuel après Plan 01-01 + 01-02 + Plan 01-03 historique (RECAP-01 fermé) -->

**Source de vérité actuelle pour le récap inline (app.js:140-201) :**
```javascript
function formatCondensedMaterial(block) {
    if (!block) return '';
    var parts = [];
    var isVrac = block.dataset.uldType === 'VRAC';
    var straps = parseInt(block.dataset.straps) || 0;
    if (straps > 0) parts.push('Sangles: ' + straps);
    if (!isVrac) {
        var flEuF = block.dataset.flooringEuForfait === 'true';
        var flEu = parseInt(block.dataset.flooringEu) || 0;
        if (flEuF) parts.push('Planchers EU: forfait');
        else if (flEu > 0) parts.push('Planchers EU: ' + flEu);
        var flStdF = block.dataset.flooringStdForfait === 'true';
        var flStd = parseInt(block.dataset.flooringStd) || 0;
        if (flStdF) parts.push('Planchers Std: forfait');
        else if (flStd > 0) parts.push('Planchers Std: ' + flStd);
    }
    var blocks = parseInt(block.dataset.blocks) || 0;
    if (blocks > 0) parts.push('Bois calage: ' + blocks);
    var tarps = parseInt(block.dataset.tarps) || 0;
    if (tarps > 0) parts.push('Bâches: ' + tarps);
    var dividers = parseInt(block.dataset.dividers) || 0;
    if (dividers > 0) parts.push('Intercalaires: ' + dividers);
    var honeycomb = parseInt(block.dataset.honeycomb) || 0;
    if (honeycomb > 0) parts.push("Nids d'abeille: " + honeycomb);
    var rawComment = String(block.dataset.uldComment || '');
    if (rawComment.length > 0) {
        var truncated = rawComment.length > 30 ? (rawComment.slice(0, 30) + '…') : rawComment;
        parts.push('Com: ' + esc(truncated));
    }
    if (parts.length === 0) return '';
    return parts.join(' | ');
}

function refreshMaterialBadge(uldIndex) {
    var block = document.getElementById('uld-' + uldIndex);
    var wrapper = document.getElementById('material-badge-' + uldIndex);
    if (!block || !wrapper) return;
    var content = formatCondensedMaterial(block);
    wrapper.innerHTML = content ? '<span class="material-recap">' + content + '</span>' : '';
}
```

**Modal actuel (app.js:33-88) — extrait clé à étendre avec checkbox no-billing :**
```javascript
function openMaterialModal(uldIndex) {
    var block = document.getElementById('uld-' + uldIndex);
    if (!block) return;
    closeMaterialModal();
    // ... lecture des dataset existants ...
    var modal = document.createElement('div');
    modal.className = 'material-modal';
    modal.innerHTML =
        '<div class="material-modal-header">' +
            '<h3>Matériel ULD</h3>' +
            '<button class="btn btn-danger btn-sm" onclick="closeMaterialModal()">Fermer</button>' +
        '</div>' +
        // <<<< INSERTION ZONE A : avant material-modal-grid >>>>
        '<div class="material-modal-grid">' +
            '<label>Sangles<input type="number" min="0" class="mat-straps" value="' + esc(straps) + '"></label>' +
            // ... 7 autres champs ...
            '<label class="mat-comment-label">Commentaire ULD<textarea class="mat-uld-comment" rows="3" ...></textarea></label>' +
        '</div>' +
        '<div class="material-modal-actions">' +
            '<button class="btn btn-secondary" onclick="closeMaterialModal()">Annuler</button>' +
            '<button class="btn btn-primary" onclick="applyMaterialToUld(' + uldIndex + ')">Valider</button>' +
        '</div>';
    overlay.appendChild(modal);
    document.body.appendChild(overlay);
    modal.querySelector('.mat-uld-comment').value = uldComment;
}
```

**applyMaterialToUld actuel (app.js:109-139) — extrait à étendre avec lecture noMaterialToBill :**
```javascript
function applyMaterialToUld(uldIndex) {
    var modal = document.querySelector('.material-modal');
    var block = document.getElementById('uld-' + uldIndex);
    if (!modal || !block) return;
    var flEuF = modal.querySelector('.mat-flooring-eu-forfait').checked;
    // ... lecture des autres champs ...
    block.setAttribute('data-straps', String(straps));
    // ... écriture des autres data-attributes ...
    block.setAttribute('data-uld-comment', uldComment);
    refreshMaterialBadge(uldIndex);
    closeMaterialModal();
}
```

**addUld actuel (app.js:348-393) — extrait à étendre avec autoOpen + skipValidation + validation MAT-13 :**
```javascript
function addUld() {
    uldCount++;
    var i = uldCount;
    var div = document.createElement('div');
    div.className = 'uld-block';
    div.id = 'uld-' + i;
    div.setAttribute('data-straps', '0');
    // ... 9 autres data-attributes existants ...
    div.setAttribute('data-uld-comment', '');
    div.setAttribute('data-uld-type', ULD_TYPE_DEFAULT);
    div.innerHTML = '<div class="uld-header">' + ... + '</div>' + ... ;
    document.getElementById('pmcContainer').appendChild(div);
    addRow(i);
    // <<<< INSERTION ZONE B (fin) : if (autoOpen) openMaterialModal(i); >>>>
}
```

**loadManifest actuel (app.js:588-666) — extrait à étendre avec lecture défensive noMaterialToBill :**
```javascript
async function loadManifest(id) {
    // ... reset DOM, lecture data ...
    var uldList = data.ulds || data.pmcs || [];
    uldList.forEach(function(uldData) {
        if (!uldData || !Array.isArray(uldData.rows)) return;
        uldCount++;
        var i = uldCount;
        var div = document.createElement('div');
        div.className = 'uld-block';
        div.id = 'uld-' + i;
        // Materiel ULD (MAT-01..08, MAT-10) : lecture defensive
        div.setAttribute('data-straps', String(parseInt(uldData.strapsCount) || 0));
        // ... 9 autres data-attributes existants ...
        div.setAttribute('data-uld-comment', String(uldData.uldComment || ''));
        // <<<< INSERTION ZONE C : div.setAttribute('data-no-billing', String(uldData.noMaterialToBill === true)); >>>>
        var uldType = (uldData.type && ULD_TYPES.indexOf(uldData.type) >= 0) ? uldData.type : ULD_TYPE_DEFAULT;
        div.setAttribute('data-uld-type', uldType);
        div.innerHTML = ...;
        document.getElementById('pmcContainer').appendChild(div);
        // ... addRow + updateTotals ...
        refreshMaterialBadge(i);  // doit afficher 'Rien à facturer' si noMaterialToBill restauré
    });
    updateRecap();
}
```

**collectData actuel (app.js:490-536) — extrait à étendre avec écriture noMaterialToBill :**
```javascript
function collectData() {
    var ulds = [];
    document.querySelectorAll('.uld-block').forEach(function(block) {
        // ... lecture des data-attributes ...
        uldEntry.uldComment = block.dataset.uldComment || '';
        // <<<< INSERTION ZONE D : uldEntry.noMaterialToBill = block.dataset.noBilling === 'true'; >>>>
        var typeSelect = block.querySelector('.uld-type');
        // ...
        ulds.push(uldEntry);
    });
    return { ... };
}
```

**Boutons cibles pour MAT-13 (index.html:105-119) — pas de modification HTML, validation via JS :**
```html
<button class="btn btn-primary" onclick="addUld()">+ Ajouter une ULD</button>
<button class="btn btn-success" onclick="showGenerateSection()">Generer Loadsheet</button>
...
<button class="btn btn-success" onclick="generatePdf()">Generer le PDF</button>
<button class="btn btn-primary" id="sendEmailBtn" onclick="sendEmail()">Envoyer par email</button>
```

**Pattern alert() existant (app.js:484-485, 572, 578, 1106, 1124-1128) :**
```javascript
if (!agent) { alert('Le nom de l\'agent est obligatoire.'); document.getElementById('agentName').focus(); return false; }
if (data.ulds.length === 0) { alert('Veuillez ajouter au moins une ULD.'); return; }
if (!data.recipients) { alert('Veuillez renseigner au moins un destinataire.'); return; }
```
**Décision** : utiliser `alert()` simple FR pour MAT-13 (cohérent avec validateRequired et les autres validations de generatePdf/sendEmail). Pas de div inline rouge, pas de toast — KISS.

**Buffer helper PDF/email (app.js:753-775 + 810-822) à étendre court-circuit "Rien à facturer" :**
```javascript
function buildUldMaterialRows(u) {
    if (!u) return [];
    // <<<< INSERTION ZONE E (en tête) : if (u.noMaterialToBill === true) return [['', 'Rien à facturer']]; >>>>
    var rows = [];
    var isVrac = u.type === 'VRAC';
    // ...
}

function buildUldMaterialHtml(u) {
    var rows = buildUldMaterialRows(u);  // bénéficie automatiquement du court-circuit ci-dessus
    if (rows.length === 0) return '';
    // ... rendu HTML normal ...
}
```

**Pattern de test (tests/tests.html) :**
```javascript
suite('Nom de la suite');
(function() { resetDOM(); addUld(); /* sync */ assert('desc', condition); })();
await (async function() { /* async pour storage OU pour await generatePdf/sendEmail */ })();
// Pour intercepter alert() : _alertLog est déjà setup (tests.html:55-58)
// Vérifier _alertLog.find par substring : assert('alert affiche message X', !!_alertLog.find(function(m) { return m.indexOf('Texte attendu') >= 0; }));
```

**Inventaire des sites pré-existants `addUld(); addUld();` à patcher (Task 5) :**
- tests/tests.html:191-192 (suite `removeUld`)
- tests/tests.html:1000-1001 (suite `Type ULD - collectData inclut type`)
- tests/tests.html:1156-1157 (suite `Type ULD - updateRecap annotation dont Vrac (VRAC-02, D-07 D-08)`)
- tests/tests.html:1176 (suite `Type ULD - updateRecap pas annotation si aucun VRAC`, ligne `addUld(); addUld();`)
- tests/tests.html:1189 (suite `Type ULD - updateRecap totaux globaux incluent VRAC (D-10)`, ligne `addUld(); addUld();`)
- tests/tests.html:1209 (suite `Type ULD - updateRecap plusieurs VRAC agregent`, ligne `addUld(); addUld(); addUld();`)
- tests/tests.html:2023-2024 (suite `Type ULD - sendEmail integration`)
- tests/tests.html:2129-2130 (suite `updateRecap - multi ULD`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feature B — Case "Rien à facturer" dans le modal (MAT-12) + intégration récap, PDF, email + neutralisation visuelle + classe .mat-recap-no-billing dans refreshMaterialBadge</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js (RELIRE intégralement les lignes 1-230 : esc, openMaterialModal, toggleForfait, closeMaterialModal, applyMaterialToUld, formatCondensedMaterial, refreshMaterialBadge — ce sont les fonctions à étendre)
    - static/js/app.js (lignes 488-535 collectData, 588-666 loadManifest — pour ajouter le data-attribute noBilling à 3 endroits)
    - static/js/app.js (lignes 753-775 buildUldMaterialRows, 810-822 buildUldMaterialHtml — pour court-circuit PDF/email avec 'Rien à facturer')
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (MAT-12/13/14 NEW context override en frontmatter ; D-19/D-20 anti-XSS rappel)
    - tests/tests.html (lignes 649-786 suites modal pré-existantes — pattern à suivre, ne pas casser ; lignes 803-949 suites récap — emplacement où ajouter les nouveaux tests no-billing)
    - CLAUDE.md (Sécurité — esc() obligatoire ; Conventions — JS vanilla, pas de framework)
  </read_first>
  <behavior>
    - **Test 1 (modal contient checkbox no-billing)** : openMaterialModal(1) crée un modal contenant `document.querySelector('.material-modal #mat-no-billing')` (input type=checkbox) avec un label texte exact `Rien à facturer pour cette ULD`.
    - **Test 2 (cocher désactive les 8 autres inputs + 2 checkboxes forfait + textarea)** : cocher `#mat-no-billing` puis appeler le handler onchange (`toggleNoBilling(this)`). Vérifier que `.mat-straps`, `.mat-flooring-eu`, `.mat-flooring-eu-forfait`, `.mat-flooring-std`, `.mat-flooring-std-forfait`, `.mat-blocks`, `.mat-tarps`, `.mat-dividers`, `.mat-honeycomb`, `.mat-uld-comment` ont tous l'attribut `disabled`.
    - **Test 3 (décocher réactive les inputs)** : décocher `#mat-no-billing`, vérifier que `.mat-straps`, `.mat-blocks`, `.mat-uld-comment` n'ont plus disabled. (Note : les inputs planchers EU/Std restent disabled SI leur checkbox forfait correspondante reste cochée — comportement D-06 préservé : `.mat-flooring-eu` reste disabled si `.mat-flooring-eu-forfait` est checked. Vérifier ce cas avec un test : forfait EU coché + no-billing coché puis décoché → `.mat-flooring-eu` reste disabled.)
    - **Test 4 (applyMaterialToUld écrit data-no-billing + neutralise forfait)** : ouvrir modal, cocher `#mat-no-billing`, valider. Vérifier `block.dataset.noBilling === 'true'`. Vérifier aussi que les autres data-attributes matériel sont à 0/false/'' (neutralisation à la sauvegarde) — incluant `data-flooring-eu-forfait === 'false'` ET `data-flooring-std-forfait === 'false'` après no-billing coché (warning #6).
    - **Test 5 (récap inline affiche 'Rien à facturer' + classe CSS distinct)** : applyMaterialToUld avec noMaterialToBill cochée. Vérifier `wrap.querySelector('.material-recap').textContent === 'Rien à facturer'` (chaîne exacte, pas la liste). Vérifier également que le span porte la classe `.mat-recap-no-billing` (`wrap.querySelector('.material-recap').classList.contains('mat-recap-no-billing') === true`).
    - **Test 6 (récap normal si no-billing décochée)** : ULD avec straps=3 + no-billing décochée. Récap = `Sangles: 3` (le récap pré-existant fonctionne toujours, pas de régression). Le span ne porte PAS la classe `.mat-recap-no-billing`.
    - **Test 7 (loadManifest restaure noMaterialToBill)** : await writeSavedManifests avec un manifeste contenant `noMaterialToBill: true` sur l'ULD. await loadManifest. Vérifier `block.dataset.noBilling === 'true'` ET `wrap.querySelector('.material-recap').textContent === 'Rien à facturer'`.
    - **Test 8 (rétro-compat loadManifest sans noMaterialToBill)** : await writeSavedManifests avec un manifeste OMETTANT la clé noMaterialToBill (ancien format). loadManifest. Vérifier `block.dataset.noBilling === 'false'` (default rétro-compat MAT-10).
    - **Test 9 (collectData inclut noMaterialToBill)** : créer ULD, cocher no-billing via le modal, valider, appeler collectData(). Vérifier `data.ulds[0].noMaterialToBill === true` (boolean strict).
    - **Test 10 (buildUldMaterialRows court-circuit)** : `buildUldMaterialRows({ noMaterialToBill: true, strapsCount: 5 })` retourne `[['', 'Rien à facturer']]` (1 seul row, ignore les autres champs).
    - **Test 11 (buildUldMaterialHtml affiche 'Rien à facturer')** : `buildUldMaterialHtml({ noMaterialToBill: true })` retourne un HTML contenant `Rien à facturer` ET échappé via esc() (test : injecter `noMaterialToBill: true, uldComment: '<img onerror=window._xss++>'` ne fait pas exécuter le payload — bonus défense en profondeur).
    - **Test 12 (applyMaterialToUld neutralise les checkbox forfait dans le modal)** : ouvrir modal, cocher forfait EU, cocher no-billing, valider. Vérifier que `data-flooring-eu-forfait === 'false'` (les checkbox forfait sont neutralisées même si elles étaient cochées avant) — warning #8 (behavior 12).
  </behavior>
  <action>
    **1) Étendre `openMaterialModal` (app.js:33-88)** :

    Lire le flag actuel après les lignes lisant les autres data-attributes (après ligne 47 `var uldComment = block.dataset.uldComment || '';`) :
    ```javascript
    var noBilling = block.dataset.noBilling === 'true';
    var disabledAttrIfNoBilling = noBilling ? ' disabled' : '';
    ```

    **Insérer la zone "Rien à facturer" dans le innerHTML AVANT `<div class="material-modal-grid">`** (entre `</div>` du header et l'ouverture du grid) :
    ```javascript
    '<div class="mat-no-billing-row">' +
        '<label class="mat-no-billing-label">' +
            '<input type="checkbox" id="mat-no-billing"' + (noBilling ? ' checked' : '') + ' onchange="toggleNoBilling(this)"> ' +
            'Rien à facturer pour cette ULD' +
        '</label>' +
    '</div>' +
    ```

    **Sur CHACUN des 8 inputs du grid + 2 checkboxes forfait + textarea, ajouter `disabledAttrIfNoBilling` à l'attribut disabled existant** :
    - Sangles : `... value="' + esc(straps) + '"' + disabledAttrIfNoBilling + '>`
    - Planchers EU input : déjà a `(flEuF ? ' disabled' : '')` → devient `((flEuF || noBilling) ? ' disabled' : '')`
    - Checkbox forfait EU : déjà a `(flEuF ? ' checked' : '')` → ajouter ` ' + disabledAttrIfNoBilling + '` après `' onchange="..."`
    - Idem Std (input + checkbox)
    - Bois calage / Bâches / Intercalaires / Nids d'abeille : ajouter `disabledAttrIfNoBilling` à chaque
    - Textarea Commentaire : ajouter `disabledAttrIfNoBilling` dans le `<textarea ...>...`

    **2) Ajouter la fonction `toggleNoBilling` (app.js, juste après `toggleForfait` ligne ~99)** :
    ```javascript
    // MAT-12 : Cocher "Rien à facturer" desactive tous les autres champs du modal (raccourci explicite).
    // Decocher reactive sangles/blocks/tarps/dividers/honeycomb/comment, MAIS preserve l'etat
    // forfait des planchers (D-06 : input plancher reste disabled si sa checkbox forfait est cochee).
    function toggleNoBilling(checkbox) {
        var modal = document.querySelector('.material-modal');
        if (!modal) return;
        var disabled = checkbox.checked;
        // Inputs et textareas qui suivent l'etat de no-billing inconditionnellement
        var basicSelectors = [
            '.mat-straps', '.mat-blocks', '.mat-tarps', '.mat-dividers',
            '.mat-honeycomb', '.mat-uld-comment',
            '.mat-flooring-eu-forfait', '.mat-flooring-std-forfait'
        ];
        basicSelectors.forEach(function(sel) {
            var el = modal.querySelector(sel);
            if (el) el.disabled = disabled;
        });
        // Inputs planchers EU/Std : combinaison no-billing OU forfait coche (D-06 preserve)
        var flEu = modal.querySelector('.mat-flooring-eu');
        var flEuF = modal.querySelector('.mat-flooring-eu-forfait');
        if (flEu) flEu.disabled = disabled || (flEuF && flEuF.checked);
        var flStd = modal.querySelector('.mat-flooring-std');
        var flStdF = modal.querySelector('.mat-flooring-std-forfait');
        if (flStd) flStd.disabled = disabled || (flStdF && flStdF.checked);
    }
    ```

    **3) Étendre `applyMaterialToUld` (app.js:109-139)** :

    Au tout début, après `if (!modal || !block) return;`, lire la checkbox no-billing :
    ```javascript
    var noBilling = !!(modal.querySelector('#mat-no-billing') && modal.querySelector('#mat-no-billing').checked);
    ```

    Si `noBilling === true`, **forcer toutes les autres valeurs à 0/false/''** (cohérent avec D-07 qui force forfait→0) — remplacer le bloc lecture des champs par :
    ```javascript
    var flEuF, flStdF, flEu, flStd, straps, blocks, tarps, dividers, honeycomb, uldComment;
    if (noBilling) {
        flEuF = false; flStdF = false; flEu = 0; flStd = 0;
        straps = 0; blocks = 0; tarps = 0; dividers = 0; honeycomb = 0;
        uldComment = '';
    } else {
        flEuF = modal.querySelector('.mat-flooring-eu-forfait').checked;
        flStdF = modal.querySelector('.mat-flooring-std-forfait').checked;
        flEu = flEuF ? 0 : (parseInt(modal.querySelector('.mat-flooring-eu').value) || 0);
        flStd = flStdF ? 0 : (parseInt(modal.querySelector('.mat-flooring-std').value) || 0);
        straps = parseInt(modal.querySelector('.mat-straps').value) || 0;
        blocks = parseInt(modal.querySelector('.mat-blocks').value) || 0;
        tarps = parseInt(modal.querySelector('.mat-tarps').value) || 0;
        dividers = parseInt(modal.querySelector('.mat-dividers').value) || 0;
        honeycomb = parseInt(modal.querySelector('.mat-honeycomb').value) || 0;
        uldComment = String(modal.querySelector('.mat-uld-comment').value || '');
    }
    ```

    Puis, après les setAttribute existants pour les 10 champs, **ajouter** :
    ```javascript
    block.setAttribute('data-no-billing', String(noBilling));
    ```

    **NOTE WARNING #8 (behavior 12)** : Comme on FORCE flEuF=false / flStdF=false en haut de la branche `if (noBilling)`, l'écriture des data-attributes `data-flooring-eu-forfait` et `data-flooring-std-forfait` qui suit s'écrit avec ces valeurs forcées à false. C'est la neutralisation visuelle des checkbox forfait → applyMaterialToUld neutralise data-flooring-eu-forfait ET data-flooring-std-forfait à 'false' quand no-billing est coché.

    **4) Étendre `formatCondensedMaterial` (app.js:150-188)** :

    Au tout début de la fonction, après `if (!block) return '';`, ajouter le court-circuit :
    ```javascript
    // MAT-12 : Si "Rien à facturer" coché, le récap affiche literalement la chaine, pas la liste.
    // PRECEDENCE noBilling > VRAC > champs détaillés : ce court-circuit est AVANT la lecture
    // de isVrac / des champs détaillés. Une ULD VRAC marquée "Rien à facturer" affiche
    // 'Rien à facturer' (et NON la liste filtrée hors planchers, comme le ferait la branche VRAC).
    if (block.dataset.noBilling === 'true') return 'Rien à facturer';
    ```

    **Note critique** : la chaîne `'Rien à facturer'` est statique (pas user input) donc PAS d'esc() nécessaire (et il ne faut PAS échapper, sinon `&#39;` apparaîtrait à la place de `'`). `refreshMaterialBadge` injecte ensuite `<span class="material-recap mat-recap-no-billing">Rien à facturer</span>`.

    **5) Étendre `refreshMaterialBadge` (app.js:194-201) — émission classe CSS `.mat-recap-no-billing`** (BLOCKER #2 : toute logique JS est ici, Task 4 reste CSS-only) :
    ```javascript
    function refreshMaterialBadge(uldIndex) {
        var block = document.getElementById('uld-' + uldIndex);
        var wrapper = document.getElementById('material-badge-' + uldIndex);
        if (!block || !wrapper) return;
        var content = formatCondensedMaterial(block);
        if (!content) { wrapper.innerHTML = ''; return; }
        // MAT-12 : variante CSS quand "Rien à facturer" pour signal visuel distinct
        var extraClass = (block.dataset.noBilling === 'true') ? ' mat-recap-no-billing' : '';
        wrapper.innerHTML = '<span class="material-recap' + extraClass + '">' + content + '</span>';
    }
    ```

    **6) Étendre `addUld` (app.js:348-393)** :

    Après `div.setAttribute('data-uld-comment', '');` (ligne ~364), ajouter :
    ```javascript
    div.setAttribute('data-no-billing', 'false');
    ```
    *(L'extension `(autoOpen, skipValidation)` est faite en Task 3 — ne pas mélanger ici, garder Task 1 focus MAT-12.)*

    **7) Étendre `loadManifest` (app.js:608-660)** :

    Après `div.setAttribute('data-uld-comment', String(uldData.uldComment || ''));` (ligne 625), ajouter la lecture défensive :
    ```javascript
    div.setAttribute('data-no-billing', String(uldData.noMaterialToBill === true));
    ```

    **8) Étendre `collectData` (app.js:490-535)** :

    Après `uldEntry.uldComment = block.dataset.uldComment || '';` (ligne 518), ajouter :
    ```javascript
    uldEntry.noMaterialToBill = block.dataset.noBilling === 'true';
    ```

    **9) Étendre `buildUldMaterialRows` (app.js:753-775)** :

    Au tout début de la fonction, après `if (!u) return [];`, ajouter le court-circuit :
    ```javascript
    // MAT-12 : Si "Rien a facturer" => 1 seul row affiche litteralement (PDF + email)
    if (u.noMaterialToBill === true) return [['', 'Rien à facturer']];
    ```

    **Note** : en passant `['', 'Rien à facturer']` (label vide), `autoTable` rendra une ligne avec colonne 1 vide (style fillColor par defaut) + colonne 2 contenant le texte. C'est volontairement minimaliste pour signaler clairement le statut au destinataire. `buildUldMaterialHtml` réutilise déjà `buildUldMaterialRows` donc le court-circuit s'applique automatiquement à l'email.

    **10) Tests dans tests/tests.html** — Ajouter les 12 tests du `<behavior>` dans une nouvelle section APRÈS la suite `'Materiel ULD - loadManifest restaure recap (RECAP-01 / TEST-02)'` (tests.html:916, fin de bloc à ligne ~949), AVANT le bloc `// PLAN 02-01 Task 1 : Type ULD VRAC` (ligne ~951) :

    ```javascript
    // ============================================
    // PLAN 01-03 EXTENDED : MAT-12 "Rien à facturer" (case + récap + PDF/email + retro-compat)
    // ============================================

    // --- Test 1 : modal contient checkbox no-billing ---
    suite('Materiel ULD - rien a facturer checkbox presente (MAT-12)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        // addUld auto-ouvre le modal (MAT-14 Task 3) — fermer pour test isolé puis rouvrir manuellement
        closeMaterialModal();
        openMaterialModal(i);
        var cb = document.querySelector('.material-modal #mat-no-billing');
        assert('Checkbox #mat-no-billing presente', !!cb);
        assertEqual('Type checkbox', cb.type, 'checkbox');
        var label = cb.closest('label');
        assert('Label parent existe', !!label);
        assert('Label contient "Rien à facturer pour cette ULD"', label.textContent.indexOf('Rien à facturer pour cette ULD') >= 0);
    })();

    // --- Test 2 : cocher desactive tous les autres champs ---
    suite('Materiel ULD - rien a facturer desactive autres champs (MAT-12)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        closeMaterialModal();
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        var cb = modal.querySelector('#mat-no-billing');
        cb.checked = true;
        toggleNoBilling(cb);
        ['.mat-straps','.mat-flooring-eu','.mat-flooring-eu-forfait','.mat-flooring-std','.mat-flooring-std-forfait','.mat-blocks','.mat-tarps','.mat-dividers','.mat-honeycomb','.mat-uld-comment'].forEach(function(sel) {
            var el = modal.querySelector(sel);
            assert(sel + ' est disabled', !!el && el.disabled === true);
        });
    })();

    // --- Test 3 : decocher reactive (avec preservation forfait D-06) ---
    suite('Materiel ULD - rien a facturer decoche reactive (MAT-12 + D-06)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        closeMaterialModal();
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        // 1) Cocher forfait EU (input EU disabled par D-06)
        var fEuF = modal.querySelector('.mat-flooring-eu-forfait');
        fEuF.checked = true;
        toggleForfait(fEuF, 'mat-flooring-eu');
        // 2) Cocher no-billing (tout disabled)
        var cb = modal.querySelector('#mat-no-billing');
        cb.checked = true;
        toggleNoBilling(cb);
        // 3) Decocher no-billing
        cb.checked = false;
        toggleNoBilling(cb);
        // straps/blocks reactives
        assertEqual('mat-straps reactive', modal.querySelector('.mat-straps').disabled, false);
        assertEqual('mat-blocks reactive', modal.querySelector('.mat-blocks').disabled, false);
        assertEqual('mat-uld-comment reactive', modal.querySelector('.mat-uld-comment').disabled, false);
        // forfait EU coche + input EU doit RESTER disabled (D-06 preserve)
        assertEqual('mat-flooring-eu reste disabled (forfait coche)', modal.querySelector('.mat-flooring-eu').disabled, true);
    })();

    // --- Test 4 : applyMaterialToUld ecrit data-no-billing + neutralise autres + forfait ---
    suite('Materiel ULD - rien a facturer applyMaterialToUld (MAT-12 + warning #6)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        closeMaterialModal();
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        // Saisir un nombre AVANT de cocher no-billing pour verifier la neutralisation
        modal.querySelector('.mat-straps').value = '5';
        modal.querySelector('.mat-uld-comment').value = 'Test';
        // Cocher AUSSI les forfait EU et Std avant no-billing pour verifier neutralisation forfait (warning #6)
        modal.querySelector('.mat-flooring-eu-forfait').checked = true;
        modal.querySelector('.mat-flooring-std-forfait').checked = true;
        modal.querySelector('#mat-no-billing').checked = true;
        applyMaterialToUld(i);
        var block = document.getElementById('uld-' + i);
        assertEqual('data-no-billing = "true"', block.dataset.noBilling, 'true');
        // Neutralisation : autres data-attributes a 0/''
        assertEqual('data-straps neutralise a 0', block.dataset.straps, '0');
        assertEqual('data-uld-comment neutralise vide', block.dataset.uldComment, '');
        // WARNING #6 : data-flooring-eu-forfait et data-flooring-std-forfait neutralises a 'false'
        assertEqual('data-flooring-eu-forfait neutralise a "false"', block.dataset.flooringEuForfait, 'false');
        assertEqual('data-flooring-std-forfait neutralise a "false"', block.dataset.flooringStdForfait, 'false');
    })();

    // --- Test 5 : recap inline affiche 'Rien a facturer' + classe CSS distincte ---
    suite('Materiel ULD - rien a facturer recap inline + classe CSS (MAT-12 + RECAP-01)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        closeMaterialModal();
        var block = document.getElementById('uld-' + i);
        block.setAttribute('data-no-billing', 'true');
        refreshMaterialBadge(i);
        var wrap = document.getElementById('material-badge-' + i);
        var recap = wrap.querySelector('.material-recap');
        assert('Recap present', !!recap);
        assertEqual('Recap textContent = "Rien à facturer"', recap.textContent, 'Rien à facturer');
        // BLOCKER #2 : classe CSS .mat-recap-no-billing emise par refreshMaterialBadge
        assert('Classe CSS .mat-recap-no-billing presente', recap.classList.contains('mat-recap-no-billing'));
    })();

    // --- Test 6 : recap normal si no-billing decoche (pas de regression RECAP-01) ---
    suite('Materiel ULD - rien a facturer decoche recap normal (MAT-12)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        closeMaterialModal();
        var block = document.getElementById('uld-' + i);
        block.setAttribute('data-no-billing', 'false');
        block.setAttribute('data-straps', '3');
        refreshMaterialBadge(i);
        var recap = document.getElementById('material-badge-' + i).querySelector('.material-recap');
        assertEqual('Recap = "Sangles: 3" (pas Rien a facturer)', recap.textContent, 'Sangles: 3');
        assertEqual('Pas de classe .mat-recap-no-billing', recap.classList.contains('mat-recap-no-billing'), false);
    })();

    // --- Test 7 : loadManifest restaure noMaterialToBill ---
    suite('Materiel ULD - rien a facturer loadManifest restore (MAT-12 + TEST-02)');
    await (async function() {
        resetDOM();
        await deriveAndStoreKey('test-password-for-unit-tests');
        var manifests = [{
            manifestId: 'TEST-NOBILL-RESTORE',
            date: '2026-04-28', client: '', agent: 'A', destAirport: 'CDG',
            recipients: '', cc: '',
            ulds: [{
                uldNumber: 'U1',
                rows: [{ lta: 'L', dossier: 'D', colis: 1, dgr: 'N', comment: '' }],
                totalColis: 1,
                strapsCount: 0, flooringEuCount: 0, flooringEuForfait: false,
                flooringStdCount: 0, flooringStdForfait: false,
                blocksCount: 0, tarpsCount: 0, dividersCount: 0, honeycombCount: 0,
                uldComment: '',
                noMaterialToBill: true
            }]
        }];
        await writeSavedManifests(manifests);
        await loadManifest('TEST-NOBILL-RESTORE');
        var block = document.querySelector('.uld-block');
        assertEqual('data-no-billing restaure = "true"', block.dataset.noBilling, 'true');
        var recap = document.querySelector('.material-recap');
        assert('Recap rendu apres loadManifest', !!recap);
        assertEqual('Recap = "Rien à facturer"', recap.textContent, 'Rien à facturer');
    })();

    // --- Test 8 : retro-compat ancien format (noMaterialToBill absent => false) ---
    suite('Materiel ULD - rien a facturer retro-compat absente => false (MAT-12 + MAT-10)');
    await (async function() {
        resetDOM();
        await deriveAndStoreKey('test-password-for-unit-tests');
        var manifests = [{
            manifestId: 'TEST-NOBILL-LEGACY',
            date: '2026-04-28', client: '', agent: 'A', destAirport: 'CDG',
            recipients: '', cc: '',
            ulds: [{
                uldNumber: 'U1',
                rows: [{ lta: 'L', dossier: 'D', colis: 1, dgr: 'N', comment: '' }],
                totalColis: 1
                // PAS de cle noMaterialToBill (ancien format avant MAT-12)
            }]
        }];
        await writeSavedManifests(manifests);
        await loadManifest('TEST-NOBILL-LEGACY');
        var block = document.querySelector('.uld-block');
        assertEqual('data-no-billing default "false" (retro-compat)', block.dataset.noBilling, 'false');
    })();

    // --- Test 9 : collectData inclut noMaterialToBill ---
    suite('Materiel ULD - rien a facturer collectData inclut flag (MAT-12)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        closeMaterialModal();
        var block = document.getElementById('uld-' + i);
        block.setAttribute('data-no-billing', 'true');
        var data = collectData();
        assertEqual('noMaterialToBill = true (boolean strict)', data.ulds[0].noMaterialToBill, true);
    })();

    // --- Test 10 : buildUldMaterialRows court-circuit Rien a facturer ---
    suite('Materiel PDF - rien a facturer court-circuit row (MAT-12)');
    (function() {
        var rows = buildUldMaterialRows({ noMaterialToBill: true, strapsCount: 5, blocksCount: 3 });
        assertEqual('1 seul row', rows.length, 1);
        assertEqual('Row[0] label vide', rows[0][0], '');
        assertEqual('Row[0] valeur = "Rien à facturer"', rows[0][1], 'Rien à facturer');
    })();

    // --- Test 11 : buildUldMaterialHtml HTML "Rien a facturer" + esc defense ---
    suite('Materiel email HTML - rien a facturer affiche litteralement (MAT-12 + MAT-11)');
    (function() {
        var html = buildUldMaterialHtml({ noMaterialToBill: true, uldComment: '<img onerror="window._xss++">' });
        assert('HTML contient "Rien à facturer"', html.indexOf('Rien à facturer') >= 0);
        assert('HTML ne contient pas <img litteral (defense MAT-11)', html.indexOf('<img onerror') < 0);
    })();

    // --- Test 12 : applyMaterialToUld neutralise visuellement les checkbox forfait (warning #8) ---
    suite('Materiel ULD - rien a facturer applyMaterialToUld neutralise forfait (MAT-12 + behavior 12)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        closeMaterialModal();
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        // Pre-conditions : forfait EU coche + saisie planchers Std + no-billing coche
        modal.querySelector('.mat-flooring-eu-forfait').checked = true;
        modal.querySelector('.mat-flooring-std').value = '7';
        modal.querySelector('#mat-no-billing').checked = true;
        applyMaterialToUld(i);
        var block = document.getElementById('uld-' + i);
        // Forfait EU et planchers Std saisis sont TOUS deux neutralises
        assertEqual('data-flooring-eu-forfait = "false" (neutralise)', block.dataset.flooringEuForfait, 'false');
        assertEqual('data-flooring-std = "0" (neutralise)', block.dataset.flooringStd, '0');
    })();
    ```
  </action>
  <verify>
    <automated>grep -c "function toggleNoBilling" static/js/app.js returns 1 AND grep -c "mat-no-billing" static/js/app.js returns >= 3 AND grep -c "noMaterialToBill" static/js/app.js returns >= 3 AND grep -c "data-no-billing" static/js/app.js returns >= 4 AND grep -c "Rien à facturer" static/js/app.js returns >= 3 AND grep -c "mat-recap-no-billing" static/js/app.js returns >= 1 AND grep -cE "suite\('Materiel ULD - rien a facturer" tests/tests.html returns >= 6</automated>
  </verify>
  <done>
    - `grep -c "function toggleNoBilling" static/js/app.js` retourne exactement 1
    - `grep -c "id=\"mat-no-billing\"" static/js/app.js` retourne 1 (dans openMaterialModal innerHTML)
    - `grep -cE "data-no-billing" static/js/app.js` retourne >= 4 (addUld setAttribute, loadManifest setAttribute, applyMaterialToUld setAttribute, formatCondensedMaterial dataset.noBilling)
    - `grep -c "noMaterialToBill" static/js/app.js` retourne >= 3 (loadManifest, collectData, buildUldMaterialRows)
    - `grep -c "Rien à facturer" static/js/app.js` retourne >= 3 (formatCondensedMaterial, buildUldMaterialRows, label modal — vérifier l'apostrophe et l'accent)
    - `grep -c "mat-recap-no-billing" static/js/app.js` retourne >= 1 (refreshMaterialBadge émet la classe — BLOCKER #2 fix)
    - En local, ouvrir un modal d'ULD : la checkbox "Rien à facturer pour cette ULD" est visible AU-DESSUS du grid des champs matériel
    - Cocher la checkbox : tous les autres champs deviennent grisés (visuellement disabled)
    - Décocher : les autres champs redeviennent éditables (sauf input plancher EU si forfait reste coché)
    - Valider avec no-billing coché : le récap inline affiche `Rien à facturer` (texte seul, pas de pipes) avec style ambre (classe `.mat-recap-no-billing`)
    - Sauvegarder le manifeste, créer un nouveau, recharger l'ancien depuis la liste : `Rien à facturer` reste affiché
    - Générer le PDF : la page ULD a une section "Materiel" avec une seule ligne `Rien à facturer`
    - Tests `tests/tests.html` : nouvelles suites `Materiel ULD - rien a facturer *` toutes vertes (12 nouveaux tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feature C — Validation matériel obligatoire (MAT-13) sur Ajouter ULD + Générer PDF + Envoyer email + helper uldHasMaterial</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js (RELIRE addUld lignes 348-393, validateRequired 481-487, generatePdf 1103-1116, sendEmail 1121-1237 — ce sont les 4 surfaces de validation MAT-13)
    - static/js/app.js (RELIRE collectData 490-535 — pour comprendre le shape data.ulds[i] utilisé dans certains chemins de validation)
    - index.html (lignes 105-120 : boutons "+ Ajouter une ULD", "Generer Loadsheet", "Generer le PDF", "Envoyer par email" — JE NE MODIFIE PAS HTML, validation côté JS uniquement)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (note context override MAT-13 dans frontmatter ce plan)
    - tests/tests.html (lignes 38-65 : pattern _alertLog stub pour intercepter alert() ; pattern resetDOM pour isoler les tests)
    - CLAUDE.md (Conventions — pattern alert() FR existant)
  </read_first>
  <behavior>
    - **Test 1 (uldHasMaterial - aucun champ)** : créer ULD vide (defaults addUld), `uldHasMaterial(block) === false`.
    - **Test 2 (uldHasMaterial - noMaterialToBill suffit)** : ULD avec data-no-billing="true" et tout le reste à 0 → `uldHasMaterial(block) === true`.
    - **Test 3 (uldHasMaterial - 1 champ numérique > 0)** : ULD avec data-straps="2" et tout le reste à 0 → `true`.
    - **Test 4 (uldHasMaterial - forfait coché)** : ULD avec data-flooring-eu-forfait="true" et tout le reste à 0/false → `true`.
    - **Test 5 (uldHasMaterial - uldComment non-vide)** : ULD avec data-uld-comment="hello" et tout le reste à 0 → `true`.
    - **Test 6 (addUld bloqué si ULD existante vide)** : addUld() crée ULD #1. Vider _alertLog. addUld() à nouveau → `uldCount` reste à 1 (PAS d'incrémentation), `_alertLog.find(m => m.indexOf('matériel non saisi') >= 0)` non vide. Le modal de l'ULD #1 devrait s'être (re)ouvert : vérifier `document.querySelector('.material-modal')` est présent.
    - **Test 7 (addUld passe si ULD existante a noMaterialToBill)** : addUld() ULD #1, set data-no-billing="true", addUld() à nouveau → `uldCount === 2`, pas d'alert ajoutée.
    - **Test 8 (generatePdf bloqué — async)** : create 2 ULD via `addUld(false, true)` (skip validation pour setup propre — BLOCKER #1), ULD #1 avec straps=2, ULD #2 vide. Vider _alertLog. Set agentName + destAirport + ajouter au moins une ligne avec colis dans ULD #2. **`await generatePdf()`** (BLOCKER #4 : la fonction est async). Vérifier `_alertLog.find(m => m.indexOf('Matériel non saisi') >= 0)` non vide (BLOCKER #4 : filtrage par substring au lieu de `.pop()` pour éviter capture de l'alert "Manifeste sauvegarde"). Vérifier que `manifestStatus.textContent` reste `Brouillon`.
    - **Test 9 (sendEmail bloqué — async)** : setup similaire test 8 via `addUld(false, true)`. **`await sendEmail()`** (BLOCKER #4 + warning #7). Vérifier `_alertLog.find(m => m.indexOf('Matériel non saisi') >= 0)` non vide. L'appel `fetch('/api/send-email')` ne doit PAS être déclenché.
    - **Test 10 (generatePdf liste plusieurs ULD incomplètes)** : 3 ULD via `addUld(false, true)` (BLOCKER #1 + warning #9 : skipValidation pour setup propre, pas d'enchainement de no-billing manuel) : #1 vide, #2 straps=1, #3 vide. **`await generatePdf()`**. Filtrer `_alertLog` par substring `'Matériel non saisi'`. Le message contient les 2 numéros : `'ULD N°1, ULD N°3'`.
  </behavior>
  <action>
    **1) Ajouter le helper `uldHasMaterial` dans app.js**, placé juste après `formatCondensedMaterial` (avant ligne 194 `function refreshMaterialBadge`) :

    ```javascript
    // ============================================
    // MAT-13 : Validation materiel obligatoire (saisie OU "Rien a facturer")
    // ============================================
    // Retourne true si l'ULD a au moins une indication de saisie materiel :
    //  - flag noMaterialToBill explicitement coche (raccourci MAT-12), OU
    //  - au moins un compteur numerique > 0 (sangles, planchers EU/Std, bois, baches, intercalaires, nids), OU
    //  - une checkbox forfait cochee (planchers EU ou Std), OU
    //  - un commentaire non-vide
    // L'ordre des checks fait court-circuiter aussi vite que possible (perf + lisibilite).
    function uldHasMaterial(block) {
        if (!block) return false;
        if (block.dataset.noBilling === 'true') return true;
        if ((parseInt(block.dataset.straps) || 0) > 0) return true;
        if ((parseInt(block.dataset.flooringEu) || 0) > 0) return true;
        if (block.dataset.flooringEuForfait === 'true') return true;
        if ((parseInt(block.dataset.flooringStd) || 0) > 0) return true;
        if (block.dataset.flooringStdForfait === 'true') return true;
        if ((parseInt(block.dataset.blocks) || 0) > 0) return true;
        if ((parseInt(block.dataset.tarps) || 0) > 0) return true;
        if ((parseInt(block.dataset.dividers) || 0) > 0) return true;
        if ((parseInt(block.dataset.honeycomb) || 0) > 0) return true;
        if (String(block.dataset.uldComment || '').length > 0) return true;
        return false;
    }

    // MAT-13 : retourne la liste des indices 1-based des ULD qui n'ont PAS de materiel saisi.
    // Utilise par addUld (avant creation), generatePdf, sendEmail (avant action).
    // L'index visible commence a 1 (pas 0) pour rester coherent avec ce que voit l'utilisateur
    // (i.e. block.id = "uld-1" pour la 1ere ULD creee, conforme uldCount).
    function findIncompleteUlds() {
        var blocks = document.querySelectorAll('.uld-block');
        var incomplete = [];
        blocks.forEach(function(block) {
            if (!uldHasMaterial(block)) {
                // L'index dans uld-{N} = la valeur visible
                var idMatch = block.id.match(/^uld-(\d+)$/);
                var n = idMatch ? idMatch[1] : '?';
                incomplete.push(n);
            }
        });
        return incomplete;
    }
    ```

    **2) Étendre `addUld` (app.js:348-393) — validation MAT-13 EN TÊTE de la fonction** :

    Au tout début, AVANT `uldCount++;`, ajouter (la signature finale `(autoOpen, skipValidation)` est définie en Task 3 ; ici on prépare la validation) :
    ```javascript
    function addUld(autoOpen, skipValidation) {
        // MAT-13 : Bloquer l'ajout d'une nouvelle ULD si une ULD existante n'a PAS de materiel saisi.
        // Premiere ULD du manifeste : pas de blocage (rien a verifier).
        // BLOCKER #1 : skipValidation=true bypass MAT-13 (usage interne / tests uniquement)
        if (!skipValidation) {
            var incomplete = findIncompleteUlds();
            if (incomplete.length > 0) {
                var firstIncomplete = incomplete[0];
                alert('ULD N°' + firstIncomplete + ' : matériel non saisi. Cochez "Rien à facturer" ou remplissez au moins un champ avant d\'ajouter une nouvelle ULD.');
                // Reouvrir le modal de l'ULD incomplete pour faciliter la correction
                openMaterialModal(parseInt(firstIncomplete) || 1);
                return;
            }
        }
        // ... reste de la fonction inchange (uldCount++, etc.) ...
    }
    ```

    **Note signature** : `function addUld(autoOpen, skipValidation)` — les params sont utilisés en Task 3 (MAT-14 + bypass MAT-13). Ici en Task 2, on encadre la validation MAT-13 par `if (!skipValidation)`.

    **3) Étendre `generatePdf` (app.js:1103-1116) — validation MAT-13 EN TÊTE** :

    **BLOCKER #4 fix** : La validation MAT-13 doit être placée **EN TOUT DÉBUT de `generatePdf`**, AVANT `validateRequired()` (qui peut elle-même produire un alert) ET AVANT `await saveManifest()` (qui produit un alert "Manifeste sauvegarde"). Cet ordre garantit que l'alert MAT-13 est le PREMIER (et souvent le seul) alert produit, simplifiant les tests.

    Insérer **EN TOUT DÉBUT de la fonction `generatePdf`** (avant `if (!validateRequired()) return;`) :
    ```javascript
    // MAT-13 : Bloquer la generation PDF si une ULD n'a pas de materiel saisi.
    // Place EN TETE (avant validateRequired et saveManifest) pour eviter pollution _alertLog en test.
    var incompletePdf = findIncompleteUlds();
    if (incompletePdf.length > 0) {
        var listPdf = incompletePdf.map(function(n) { return 'ULD N°' + n; }).join(', ');
        alert('Matériel non saisi pour : ' + listPdf + '. Veuillez remplir le matériel ou cocher "Rien à facturer" pour ces ULD.');
        return;
    }
    ```

    **4) Étendre `sendEmail` (app.js:1121-1237) — validation MAT-13 EN TÊTE** :

    Idem placement : **EN TOUT DÉBUT de la fonction `sendEmail`** (avant `if (!validateRequired()) return;` et avant tout `await`) :
    ```javascript
    // MAT-13 : Bloquer l'envoi email si une ULD n'a pas de materiel saisi
    // (verifie EN TETE, AVANT validateRequired/saveManifest/JWT/fetch).
    var incompleteEmail = findIncompleteUlds();
    if (incompleteEmail.length > 0) {
        var listEmail = incompleteEmail.map(function(n) { return 'ULD N°' + n; }).join(', ');
        alert('Matériel non saisi pour : ' + listEmail + '. Veuillez remplir le matériel ou cocher "Rien à facturer" pour ces ULD.');
        return;
    }
    ```

    **5) Tests dans tests/tests.html** — Ajouter les 10 tests du `<behavior>` APRÈS la suite `'Materiel email HTML - rien a facturer affiche litteralement'` (Task 1 fin) :

    ```javascript
    // ============================================
    // PLAN 01-03 EXTENDED : MAT-13 saisie materiel obligatoire (validation bloquante)
    // ============================================

    // --- Test 1 : uldHasMaterial - aucun champ ---
    suite('Materiel ULD - uldHasMaterial vide => false (MAT-13)');
    (function() {
        resetDOM();
        addUld(false, true);
        var block = document.querySelector('.uld-block');
        assertEqual('uldHasMaterial(block) = false (defauts addUld)', uldHasMaterial(block), false);
    })();

    // --- Test 2 : uldHasMaterial - noMaterialToBill suffit ---
    suite('Materiel ULD - uldHasMaterial noMaterialToBill => true (MAT-13 + MAT-12)');
    (function() {
        resetDOM();
        addUld(false, true);
        var block = document.querySelector('.uld-block');
        block.setAttribute('data-no-billing', 'true');
        assertEqual('uldHasMaterial = true', uldHasMaterial(block), true);
    })();

    // --- Test 3 : uldHasMaterial - 1 champ numerique > 0 ---
    suite('Materiel ULD - uldHasMaterial straps>0 => true (MAT-13)');
    (function() {
        resetDOM();
        addUld(false, true);
        var block = document.querySelector('.uld-block');
        block.setAttribute('data-straps', '2');
        assertEqual('uldHasMaterial = true', uldHasMaterial(block), true);
    })();

    // --- Test 4 : uldHasMaterial - forfait coche ---
    suite('Materiel ULD - uldHasMaterial forfait => true (MAT-13)');
    (function() {
        resetDOM();
        addUld(false, true);
        var block = document.querySelector('.uld-block');
        block.setAttribute('data-flooring-eu-forfait', 'true');
        assertEqual('uldHasMaterial = true', uldHasMaterial(block), true);
    })();

    // --- Test 5 : uldHasMaterial - uldComment non-vide ---
    suite('Materiel ULD - uldHasMaterial comment non-vide => true (MAT-13)');
    (function() {
        resetDOM();
        addUld(false, true);
        var block = document.querySelector('.uld-block');
        block.setAttribute('data-uld-comment', 'hello');
        assertEqual('uldHasMaterial = true', uldHasMaterial(block), true);
    })();

    // --- Test 6 : addUld bloque si ULD existante vide ---
    suite('Materiel ULD - addUld bloque si ULD existante vide (MAT-13)');
    (function() {
        resetDOM();
        addUld(false, true); // ULD #1 vide, skip validation pour le 1er addUld (legitime)
        var countBefore = document.querySelectorAll('.uld-block').length;
        _alertLog.length = 0;
        addUld(); // doit etre bloque (autoOpen=undefined => true, skipValidation=undefined => false)
        var countAfter = document.querySelectorAll('.uld-block').length;
        assertEqual('Pas d\'ajout (count inchange)', countAfter, countBefore);
        // BLOCKER #4 : filtrage par substring au lieu de pop()
        var matAlert = _alertLog.find(function(m) { return m.indexOf('matériel non saisi') >= 0; });
        assert('Alert "matériel non saisi" presente', !!matAlert);
        assert('Alert mentionne ULD N°1', matAlert.indexOf('ULD N°1') >= 0);
        // Modal reouvert pour faciliter correction
        var modal = document.querySelector('.material-modal');
        assert('Modal de l\'ULD incomplete reouvert', !!modal);
        closeMaterialModal();
    })();

    // --- Test 7 : addUld passe si ULD existante a noMaterialToBill ---
    suite('Materiel ULD - addUld OK si ULD existante a noMaterialToBill (MAT-13)');
    (function() {
        resetDOM();
        addUld(false, true); // ULD #1 (skip validation pour setup)
        document.querySelector('.uld-block').setAttribute('data-no-billing', 'true');
        var countBefore = document.querySelectorAll('.uld-block').length;
        _alertLog.length = 0;
        addUld(false); // ULD #2 doit etre cree (autoOpen=false, skipValidation=undefined => false, mais ULD #1 a no-billing donc passe)
        var countAfter = document.querySelectorAll('.uld-block').length;
        assertEqual('ULD ajoutee', countAfter, countBefore + 1);
        var hasMatAlert = _alertLog.some(function(m) { return m.indexOf('matériel') >= 0; });
        assertEqual('Pas d\'alert materiel', hasMatAlert, false);
    })();

    // --- Test 8 : generatePdf bloque (async, filtre _alertLog par substring) ---
    suite('Materiel ULD - generatePdf bloque si ULD vide (MAT-13)');
    await (async function() {
        resetDOM();
        // BLOCKER #1 : addUld(false, true) pour bypass MAT-13 et autoOpen
        addUld(false, true); // ULD #1
        document.querySelector('.uld-block').setAttribute('data-straps', '2');
        addUld(false, true); // ULD #2 - laissee vide
        document.getElementById('agentName').value = 'AGENT';
        document.getElementById('destAirport').value = 'CDG';
        _alertLog.length = 0;
        var statusEl = document.getElementById('manifestStatus');
        statusEl.textContent = 'Brouillon';
        // BLOCKER #4 : await generatePdf (la fonction est async)
        await generatePdf();
        // BLOCKER #4 : filtrage par substring (evite capture de "Manifeste sauvegarde")
        var matAlert = _alertLog.find(function(m) { return m.indexOf('Matériel non saisi') >= 0; });
        assert('Alert "Matériel non saisi" presente', !!matAlert);
        assert('Alert mentionne ULD N°2', matAlert.indexOf('ULD N°2') >= 0);
        assertEqual('Status reste Brouillon', statusEl.textContent, 'Brouillon');
    })();

    // --- Test 9 : sendEmail bloque (async, warning #7) ---
    suite('Materiel ULD - sendEmail bloque si ULD vide (MAT-13)');
    await (async function() {
        resetDOM();
        addUld(false, true);
        document.querySelector('.uld-block').setAttribute('data-straps', '2');
        addUld(false, true);
        document.getElementById('agentName').value = 'AGENT';
        document.getElementById('destAirport').value = 'CDG';
        document.getElementById('recipients').value = 'test@example.com';
        // Spy fetch pour verifier qu'il N'est PAS appele
        var fetchCalled = false;
        var origFetch = window.fetch;
        window.fetch = function() { fetchCalled = true; return Promise.resolve({ ok: false, json: function() { return Promise.resolve({}); } }); };
        _alertLog.length = 0;
        // WARNING #7 : await sendEmail (idem fix #4)
        await sendEmail();
        window.fetch = origFetch;
        var matAlert = _alertLog.find(function(m) { return m.indexOf('Matériel non saisi') >= 0; });
        assert('Alert "Matériel non saisi" presente', !!matAlert);
        assert('Alert mentionne ULD N°2', matAlert.indexOf('ULD N°2') >= 0);
        assertEqual('fetch NON appele', fetchCalled, false);
    })();

    // --- Test 10 : generatePdf liste plusieurs ULD incompletes (warning #9 : setup avec skipValidation) ---
    suite('Materiel ULD - generatePdf liste toutes ULD incompletes (MAT-13)');
    await (async function() {
        resetDOM();
        // WARNING #9 : utiliser addUld(false, true) pour creer 3 ULDs sans manipulations no-billing alambiquees
        addUld(false, true); // ULD #1 - vide
        addUld(false, true); // ULD #2 - on lui met du materiel
        document.querySelector('#uld-2').setAttribute('data-straps', '1');
        addUld(false, true); // ULD #3 - vide
        document.getElementById('agentName').value = 'AGENT';
        document.getElementById('destAirport').value = 'CDG';
        _alertLog.length = 0;
        await generatePdf();
        var matAlert = _alertLog.find(function(m) { return m.indexOf('Matériel non saisi') >= 0; });
        assert('Alert "Matériel non saisi" presente', !!matAlert);
        assert('Alert mentionne ULD N°1', matAlert.indexOf('ULD N°1') >= 0);
        assert('Alert mentionne ULD N°3', matAlert.indexOf('ULD N°3') >= 0);
        // Note : ULD N°2 NE doit PAS etre listee (elle a du materiel)
        assert('Alert ne mentionne pas "N°2"', matAlert.indexOf('N°2') < 0);
    })();
    ```

    **Note critique** : tous les tests d'helpers (1-5) utilisent `addUld(false, true)` pour bypass MAT-13 et auto-open du modal — utilisation interne / tests uniquement. Tests 6/7 testent EXPLICITEMENT MAT-13 donc utilisent `addUld()` (sans args) au moment opportun pour declencher la validation.
  </action>
  <verify>
    <automated>grep -c "function uldHasMaterial" static/js/app.js returns 1 AND grep -c "function findIncompleteUlds" static/js/app.js returns 1 AND grep -cE "findIncompleteUlds\(\)" static/js/app.js returns >= 3 AND grep -c "matériel non saisi" static/js/app.js returns >= 1 AND grep -c "Matériel non saisi pour" static/js/app.js returns >= 2 AND grep -cE "suite\('Materiel ULD - uldHasMaterial|suite\('Materiel ULD - addUld bloque|suite\('Materiel ULD - generatePdf bloque|suite\('Materiel ULD - sendEmail bloque" tests/tests.html returns >= 5</automated>
  </verify>
  <done>
    - `grep -c "function uldHasMaterial" static/js/app.js` retourne 1
    - `grep -c "function findIncompleteUlds" static/js/app.js` retourne 1
    - `grep -cE "findIncompleteUlds\(\)" static/js/app.js` retourne >= 3 (addUld, generatePdf, sendEmail)
    - `grep -c "matériel non saisi" static/js/app.js` retourne >= 1 (alert addUld)
    - `grep -c "Matériel non saisi pour" static/js/app.js` retourne >= 2 (alert generatePdf, sendEmail)
    - En local : créer 1 ULD vide, cliquer "+ Ajouter une ULD" → alert FR, modal de l'ULD vide se rouvre, pas de 2e ULD ajoutée
    - Cocher "Rien à facturer" sur l'ULD #1, valider → alert disparaît, "+ Ajouter une ULD" fonctionne
    - Sur 2 ULD (1 OK, 1 vide) → cliquer "Generer le PDF" → alert listant les ULD incomplètes, PDF non généré
    - Idem pour "Envoyer par email" → alert avant l'appel API
    - Tests `tests/tests.html` : suites `Materiel ULD - uldHasMaterial *`, `Materiel ULD - addUld bloque *`, `Materiel ULD - generatePdf bloque *`, `Materiel ULD - sendEmail bloque *` toutes vertes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feature D — Auto-ouverture du modal sur création ULD (MAT-14) via paramètre addUld(autoOpen, skipValidation)</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js (RELIRE addUld après extension Task 2 — la signature `function addUld(autoOpen, skipValidation)` doit déjà exister, ne pas dupliquer)
    - static/js/app.js (RELIRE newManifest 330-345 — appel addUld() pour le 1er ULD)
    - static/js/app.js (RELIRE loadManifest 588-666 — il N'utilise PAS addUld, il construit son propre HTML inline ; donc MAT-14 sera satisfait par construction sans modification de loadManifest)
    - index.html (ligne 105 : `<button onclick="addUld()">+ Ajouter une ULD</button>` → l'appel sans argument ⇒ autoOpen défaut true ; pas de modif HTML)
    - tests/tests.html (RELIRE les suites pré-existantes utilisant addUld() — beaucoup utilisent `closeMaterialModal()` immédiatement après en Task 1/2, à confirmer ; les suites pré-existantes lignes 451-786 doivent rester vertes après Task 3 — note : Task 5 patche les sites multi-ULD pré-existants)
  </read_first>
  <behavior>
    - **Test 1 (addUld autoOpen=true par défaut ouvre le modal)** : resetDOM, addUld() — vérifier `document.querySelector('.material-modal')` est présent juste après l'appel.
    - **Test 2 (addUld(false) ne l'ouvre pas)** : resetDOM, addUld(false) — vérifier `document.querySelector('.material-modal') === null`.
    - **Test 3 (loadManifest n'auto-ouvre pas)** : await writeSavedManifests([{... ULD ...}]). Fermer tout modal (closeMaterialModal). await loadManifest('id'). Vérifier `document.querySelector('.material-modal') === null` (loadManifest construit son propre HTML, ne passe pas par addUld).
    - **Test 4 (newManifest ouvre le modal pour la 1ère ULD)** : appeler newManifest(). Vérifier `document.querySelector('.material-modal')` est présent (puisque newManifest appelle addUld() sans argument).
    - **Test 5 (autoOpen ne déclenche pas validation MAT-13)** : resetDOM. addUld() crée ULD #1 + ouvre modal. Fermer le modal sans rien saisir. Vider _alertLog. addUld() — devrait être bloqué par MAT-13 (Test 6 Task 2 confirme), MAIS l'autoOpen NE doit PAS s'appliquer dans ce cas car aucune ULD nouvelle n'a été créée. Vérifier qu'on a 1 modal ouvert (celui de l'ULD #1 réouvert par MAT-13), pas 2.
    - **Test 6 (addUld(false, true) ne valide pas ET ne l'ouvre pas — usage test interne)** : resetDOM. addUld(false, true) ULD #1 (vide). addUld(false, true) ULD #2 — vérifier `uldCount === 2` ET `document.querySelector('.material-modal') === null`. Aucun alert produit.
  </behavior>
  <action>
    **1) Étendre la signature `addUld(autoOpen, skipValidation)` (app.js:348-393, déjà étendue en Task 2)** :

    La signature est déjà `function addUld(autoOpen, skipValidation)` après Task 2. Ajouter le défaut autoOpen + l'appel openMaterialModal en FIN de fonction (après `addRow(i);`) :

    ```javascript
    function addUld(autoOpen, skipValidation) {
        // (Task 2) Validation MAT-13 deja ajoutee en haut, encadree par if (!skipValidation)
        if (!skipValidation) {
            var incomplete = findIncompleteUlds();
            if (incomplete.length > 0) { /* alert + openMaterialModal + return */ }
        }

        // MAT-14 : default autoOpen=true (clic utilisateur). loadManifest n'utilise PAS addUld
        // donc le modal ne s'ouvre PAS au rechargement (par construction).
        if (autoOpen === undefined) autoOpen = true;

        uldCount++;
        var i = uldCount;
        // ... reste de la fonction (creation div, setAttributes, innerHTML, appendChild) ...
        addRow(i);

        // MAT-14 : ouvrir le modal materiel pour saisie immediate (UX)
        if (autoOpen) {
            openMaterialModal(i);
        }
    }
    ```

    **Sémantique du paramètre `skipValidation` (BLOCKER #1)** : Le paramètre `skipValidation` (default false) court-circuite la validation MAT-13 dans `addUld`. **Usage interne / tests uniquement** — ne JAMAIS l'utiliser depuis le HTML (les boutons utilisateur appellent `addUld()` sans arguments). Pourquoi : les tests pré-existants (≥ 8 sites identifiés en Task 5) enchaînent `addUld(); addUld();` sur des ULDs vides pour tester d'AUTRES fonctions (removeUld, updateRecap, collectData, sendEmail integration). Sans bypass, ces tests casseraient sous MAT-13. La signature étendue préserve les tests existants tout en imposant la validation à l'usage utilisateur.

    **2) Vérifier `newManifest` (app.js:330-345)** :

    L'appel actuel est `addUld();` (sans argument) → autoOpen défaut true → le modal s'ouvre pour la 1ère ULD du manifeste vierge. **NE PAS MODIFIER** newManifest, le comportement est correct.

    Si l'on souhaite désactiver l'auto-open au démarrage (cas où l'agent n'a pas encore rempli client/agent/dest), c'est un choix UX différent — mais le requirement MAT-14 dit explicitement "à chaque création d'une nouvelle ULD". newManifest crée une nouvelle ULD donc l'auto-open s'applique.

    **3) Vérifier `loadManifest` (app.js:588-666)** :

    loadManifest construit son propre HTML inline (ligne 632-644) sans passer par addUld. Le modal NE s'ouvre PAS au rechargement par construction. **NE PAS MODIFIER** loadManifest.

    **4) Adaptation des tests pré-existants** : voir Task 5 (dédiée à l'adaptation in-place des sites `addUld(); addUld();`).

    **5) Ajouter les 6 nouveaux tests pour MAT-14** dans tests/tests.html, APRÈS la suite `Materiel ULD - generatePdf liste toutes ULD incompletes` (Task 2 fin) :

    ```javascript
    // ============================================
    // PLAN 01-03 EXTENDED : MAT-14 auto-ouverture du modal sur creation ULD
    // ============================================

    // --- Test 1 : addUld() autoOpen=true par defaut ---
    suite('Materiel ULD - addUld autoOpen defaut true ouvre modal (MAT-14)');
    (function() {
        resetDOM();
        addUld();
        var modal = document.querySelector('.material-modal');
        assert('Modal ouvert apres addUld()', !!modal);
        // Verifier que c'est bien le modal de l'ULD nouvellement creee
        assertEqual('data-uld-index = "1"', modal.getAttribute('data-uld-index'), '1');
        closeMaterialModal();
    })();

    // --- Test 2 : addUld(false) ne l'ouvre pas ---
    suite('Materiel ULD - addUld(false) n\'ouvre pas modal (MAT-14)');
    (function() {
        resetDOM();
        addUld(false);
        var modal = document.querySelector('.material-modal');
        assertEqual('Modal absent', modal, null);
    })();

    // --- Test 3 : loadManifest n'auto-ouvre pas ---
    suite('Materiel ULD - loadManifest n\'ouvre pas modal (MAT-14 + TEST-02)');
    await (async function() {
        resetDOM();
        await deriveAndStoreKey('test-password-for-unit-tests');
        var manifests = [{
            manifestId: 'TEST-MAT14-LOAD',
            date: '2026-04-28', client: '', agent: 'A', destAirport: 'CDG',
            recipients: '', cc: '',
            ulds: [{
                uldNumber: 'U1',
                rows: [{ lta: 'L', dossier: 'D', colis: 1, dgr: 'N', comment: '' }],
                totalColis: 1,
                strapsCount: 2,
                noMaterialToBill: false
            }]
        }];
        await writeSavedManifests(manifests);
        // Fermer un eventuel modal residuel
        closeMaterialModal();
        await loadManifest('TEST-MAT14-LOAD');
        var modal = document.querySelector('.material-modal');
        assertEqual('Modal absent apres loadManifest (action utilisateur uniquement)', modal, null);
    })();

    // --- Test 4 : newManifest ouvre le modal pour la 1ere ULD ---
    suite('Materiel ULD - newManifest ouvre modal pour 1ere ULD (MAT-14)');
    (function() {
        resetDOM();
        // newManifest reset le DOM puis appelle addUld() (sans argument)
        // Pour ne pas casser les autres tests, on isole : cleanup apres
        newManifest();
        var modal = document.querySelector('.material-modal');
        assert('Modal ouvert apres newManifest()', !!modal);
        closeMaterialModal();
    })();

    // --- Test 5 : addUld bloque par MAT-13 n'ouvre pas un 2e modal ---
    suite('Materiel ULD - addUld bloque par MAT-13 n\'ouvre pas un 2e modal (MAT-14 + MAT-13)');
    (function() {
        resetDOM();
        addUld(); // ULD #1 vide, modal #1 ouvert (auto-open MAT-14)
        // ULD #1 reste vide (modal pas valide)
        // Tenter addUld() — bloque par MAT-13, devrait reouvrir modal #1 (pas creer #2)
        _alertLog.length = 0;
        addUld();
        var modals = document.querySelectorAll('.material-modal');
        assertEqual('1 seul modal au total (#1 reouvert, pas #2 cree)', modals.length, 1);
        // Verifier que c'est le modal #1 (data-uld-index="1")
        assertEqual('Modal correspond a ULD #1', modals[0].getAttribute('data-uld-index'), '1');
        // Verifier qu'il n'y a qu'1 ULD dans le DOM
        assertEqual('1 seule ULD dans le DOM', document.querySelectorAll('.uld-block').length, 1);
        closeMaterialModal();
    })();

    // --- Test 6 : addUld(false, true) ne valide pas ET ne l'ouvre pas (BLOCKER #1) ---
    suite('Materiel ULD - addUld(false, true) skipValidation + autoOpen=false (MAT-13 + MAT-14)');
    (function() {
        resetDOM();
        addUld(false, true); // ULD #1 vide, pas de modal
        addUld(false, true); // ULD #2 — MAT-13 bypass
        assertEqual('uldCount = 2', document.querySelectorAll('.uld-block').length, 2);
        var modal = document.querySelector('.material-modal');
        assertEqual('Aucun modal ouvert', modal, null);
    })();
    ```
  </action>
  <verify>
    <automated>grep -cE "function addUld\(autoOpen, skipValidation\)" static/js/app.js returns 1 AND grep -cE "if \(autoOpen === undefined\) autoOpen = true" static/js/app.js returns 1 AND grep -cE "if \(autoOpen\)" static/js/app.js returns >= 1 AND grep -cE "openMaterialModal\(i\)" static/js/app.js returns >= 1 AND grep -cE "if \(!skipValidation\)" static/js/app.js returns 1 AND grep -cE "suite\('Materiel ULD - addUld autoOpen|suite\('Materiel ULD - addUld\(false\)|suite\('Materiel ULD - loadManifest n.ouvre pas modal|suite\('Materiel ULD - newManifest ouvre modal" tests/tests.html returns >= 4</automated>
  </verify>
  <done>
    - `grep -cE "function addUld\(autoOpen, skipValidation\)" static/js/app.js` retourne 1 (signature finale étendue avec les 2 params)
    - `grep -cE "if \(autoOpen === undefined\) autoOpen = true" static/js/app.js` retourne 1 (default explicite)
    - `grep -cE "if \(!skipValidation\)" static/js/app.js` retourne 1 (BLOCKER #1 : encadrement MAT-13)
    - `grep -cE "if \(autoOpen\)" static/js/app.js` retourne >= 1 (appel openMaterialModal en FIN d'addUld)
    - En local : ouvrir l'app, login → première ULD créée + modal matériel ouvert automatiquement
    - Cliquer "Annuler" sur le modal, cliquer "+ Ajouter une ULD" (en supposant ULD #1 a du matériel) → modal de l'ULD #2 s'ouvre automatiquement
    - Charger un manifeste sauvegardé depuis la liste → les ULD apparaissent SANS modal ouvert (action de chargement, pas d'action utilisateur de saisie)
    - Tests `tests/tests.html` : suites MAT-14 toutes vertes
    - Pas de régression sur les suites pré-existantes (Plan 01-01, 01-02, 01-03 historique) — voir Task 5 pour adaptation
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: CSS — zone .mat-no-billing-row + style récap "Rien à facturer" + responsive mobile (CSS-ONLY, aucune logique JS)</name>
  <files>static/css/style.css</files>
  <read_first>
    - static/css/style.css (RELIRE INTÉGRALEMENT — surtout MATERIAL MODAL lignes 125-176 et MOBILE max-width 768px lignes 197-260 ; .material-recap déjà ajoutée Plan 01-03 historique ligne 178-188)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (D-04 mobile pattern, RECAP-03 mobile lisible)
    - CLAUDE.md section Conventions (CSS desktop par défaut + media query 768px = seul breakpoint)
  </read_first>
  <action>
    **CSS-ONLY — BLOCKER #2 fix** : Cette task ne contient AUCUNE modification de fichier `.js`. La logique d'émission de la classe CSS `.mat-recap-no-billing` dans `refreshMaterialBadge` est déjà gérée en **Task 1 §5** (intégrée dans la même fonction qui est étendue en Task 1). Cette task se contente de déclarer les styles CSS qui sont consommés par cette classe.

    **1) Ajouter dans static/css/style.css, juste APRÈS la règle `.material-modal-header h3` (ligne 142) et AVANT `.material-modal-grid` (ligne 143)** — la zone "Rien à facturer" placée visuellement entre l'en-tête et le grid des champs :

    ```css
    /* MAT-12 : Zone "Rien a facturer" en haut du modal (raccourci facturation) */
    .mat-no-billing-row {
        margin-bottom: 14px;
        padding: 10px 12px;
        background: #fff8e6;
        border-left: 3px solid #d69e2e;
        border-radius: 4px;
    }
    .mat-no-billing-label {
        display: flex;
        align-items: center;
        gap: 8px;
        font-weight: 600;
        font-size: 0.9em;
        color: #744210;
        cursor: pointer;
    }
    .mat-no-billing-label input[type="checkbox"] {
        width: 16px; height: 16px;
        cursor: pointer;
    }
    ```

    **2) Renforcer la lisibilité visuelle des inputs disabled** — la règle existante `.material-modal-grid input:disabled { background: #edf2f7; color: #a0aec0; }` (ligne 154) couvre déjà les number inputs. Ajouter une règle pour le textarea + les checkboxes désactivés :

    Ajouter APRÈS la règle existante `.material-modal-grid input:disabled` (ligne 154) :
    ```css
    /* MAT-12 : extension visuelle disabled aux textarea + checkboxes du modal */
    .material-modal-grid textarea:disabled {
        background: #edf2f7;
        color: #a0aec0;
        cursor: not-allowed;
    }
    .material-modal-grid input[type="checkbox"]:disabled {
        opacity: 0.5;
        cursor: not-allowed;
    }
    .mat-forfait-label input[type="checkbox"]:disabled + * {
        color: #a0aec0;
    }
    ```

    **3) Ajouter le style spécifique du récap "Rien à facturer"** — visuellement distinct du récap normal (couleur ambre cohérente avec la zone modal) :

    Ajouter APRÈS la règle `.material-recap` (style.css:178-188), AVANT `.uld-header .btn-material` (ligne 191) :
    ```css
    /* MAT-12 : variante visuelle du recap quand "Rien a facturer" est coche */
    /* La classe .mat-recap-no-billing est emise par refreshMaterialBadge (Task 1 §5) */
    .material-recap.mat-recap-no-billing {
        background: #fff8e6;
        color: #744210;
        font-weight: 600;
    }
    ```

    **NOTE BLOCKER #2** : Cette classe est CONSOMMÉE ici (CSS) mais ÉMISE en Task 1 §5 (JS dans `refreshMaterialBadge`). Aucune modification JS dans cette task. Ordre d'exécution garanti par dépendance Task 4 ⊕ Task 1 (les deux modifient le même feature MAT-12 et doivent être commitées ensemble ou Task 1 d'abord).

    **4) Ajouter dans le bloc `@media (max-width: 768px)` existant (style.css:197-260)** — APRÈS la règle existante `.material-recap` mobile (ligne 254-259) et AVANT la `}` de fermeture du media block (ligne 260) :

    ```css
        /* MAT-12 mobile : zone "Rien a facturer" plus compacte */
        .mat-no-billing-row {
            padding: 8px 10px;
            margin-bottom: 10px;
        }
        .mat-no-billing-label {
            font-size: 0.85em;
        }
    ```
  </action>
  <verify>
    <automated>grep -c "\.mat-no-billing-row" static/css/style.css returns >= 2 AND grep -c "\.mat-no-billing-label" static/css/style.css returns >= 2 AND grep -c "\.mat-recap-no-billing" static/css/style.css returns >= 1 AND awk '/@media \(max-width: 768px\)/{flag=1} flag && /mat-no-billing-row/{count++} END{print count}' static/css/style.css returns >= 1</automated>
  </verify>
  <done>
    - `grep -c "\.mat-no-billing-row" static/css/style.css` retourne >= 2 (desktop + mobile)
    - `grep -c "\.mat-no-billing-label" static/css/style.css` retourne >= 2 (desktop + mobile)
    - `grep -c "\.mat-recap-no-billing" static/css/style.css` retourne >= 1 (variante visuelle récap)
    - **AUCUN `.js` modifié dans cette task** — Task 4 est strictement CSS-only (BLOCKER #2 fix). Vérification : `git diff --stat static/js/` après Task 4 ne mentionne PAS `app.js`.
    - En DevTools desktop : modal matériel ouvert → zone amber "Rien à facturer pour cette ULD" visible au-dessus du grid ; cocher la case → autres champs grisés ; valider → récap inline en pill ambre `Rien à facturer`
    - En DevTools mobile (≤ 768px) : la zone "Rien à facturer" reste accessible (pas tronquée), label lisible
    - Aucun chevauchement entre la zone no-billing et le grid en-dessous
    - Aucune régression visuelle sur le modal matériel normal (saisie de champs, forfait, commentaire)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Adaptation in-place des tests pré-existants `addUld(); addUld();` migrés vers `addUld(false, true);` (BLOCKER #1)</name>
  <files>tests/tests.html</files>
  <read_first>
    - tests/tests.html (RELIRE les sites identifiés ligne par ligne — voir liste ci-dessous)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-03-PLAN.md (cette task — read_first reflexif pour s'assurer de la cohérence avec Task 3 signature `addUld(autoOpen, skipValidation)`)
  </read_first>
  <action>
    **Contexte (BLOCKER #1)** : MAT-13 (Task 2) bloque `addUld()` si une ULD existante n'a pas de matériel. Or, ≥ 8 sites pré-existants dans `tests/tests.html` enchaînent `addUld(); addUld();` (ou plus) sur des ULDs vides, dans le but de tester d'AUTRES fonctions (`removeUld`, `updateRecap`, `collectData`, `sendEmail` integration). Sans patch, ces tests casseraient (le 2e+ `addUld()` produit un alert + n'incrémente pas `uldCount` → `data.ulds[1]` devient `undefined` → TypeError silencieux ou pass faux).

    **Solution (recommandée par checker)** : MAT-13 (Task 2) introduit `addUld(autoOpen, skipValidation)`. Cette task patche les sites identifiés pour utiliser `addUld(false, true)` :
    - `false` (autoOpen) → ne pas ouvrir le modal (preserve le comportement d'origine — les tests ne s'attendent pas à un modal ouvert)
    - `true` (skipValidation) → bypass MAT-13 (preserve le setup multi-ULD vide)

    **Sites à patcher** (lignes approximatives — relire le contexte avant chaque modification) :

    | Ligne | Suite name | Action |
    |-------|-----------|--------|
    | 191-192 | `removeUld` | Remplacer les 2 `addUld();` consécutifs par `addUld(false, true);` |
    | 1000-1001 | `Type ULD - collectData inclut type` | Remplacer les 2 `addUld();` par `addUld(false, true);` |
    | 1156-1157 | `Type ULD - updateRecap annotation dont Vrac (VRAC-02, D-07 D-08)` | Remplacer les 2 `addUld(); // ULD ...` par `addUld(false, true); // ULD ...` (préserver les commentaires) |
    | 1176 | `Type ULD - updateRecap pas annotation si aucun VRAC` | Ligne `addUld(); addUld();` → `addUld(false, true); addUld(false, true);` |
    | 1189 | `Type ULD - updateRecap totaux globaux incluent VRAC (D-10)` | Ligne `addUld(); addUld();` → `addUld(false, true); addUld(false, true);` |
    | 1209 | `Type ULD - updateRecap plusieurs VRAC agregent` | Ligne `addUld(); addUld(); addUld();` → `addUld(false, true); addUld(false, true); addUld(false, true);` |
    | 2023-2024 | `Type ULD - sendEmail integration` (ou suite contenant ligne 2018+) | Remplacer les 2 `addUld();` par `addUld(false, true);` |
    | 2129-2130 | `updateRecap - multi ULD` | Remplacer les 2 `addUld();` par `addUld(false, true);` |

    **NE PAS PATCHER** :
    - Les tests qui veulent EXPLICITEMENT déclencher la validation MAT-13 (ils utilisent `addUld()` sans args INTENTIONNELLEMENT pour tester le blocage). Référence : Task 2 Tests 6, 7 ; Task 3 Test 5. Ces tests sont nouveaux (introduits par Plan 01-03 EXTENDED), pas pré-existants.
    - Les single `addUld();` (premier ULD du test) — ils ne déclenchent pas MAT-13 car aucune ULD existante. Cas particulier : si le premier `addUld();` ouvre le modal (auto-open MAT-14) et que le test ne s'y attend pas, on peut aussi patcher en `addUld(false);` (autoOpen=false) — mais c'est une question Task 3 (auto-open) pas Task 5 (validation MAT-13). À traiter au cas par cas si une suite échoue à cause du modal ouvert.

    **Pour chaque site, le diff est minimal** :
    ```diff
    -        addUld();
    -        addUld();
    +        addUld(false, true);
    +        addUld(false, true);
    ```

    **Conservation des commentaires** : Quand un commentaire suit `addUld();` (ex: `addUld(); // ULD 1 : PMC par defaut`), conserver le commentaire après le nouveau `addUld(false, true);`.

    **Vérification fonctionnelle** : Après modification, exécuter `npm run verify`. Toutes les suites pré-existantes doivent rester vertes (cible : 0 nouveau FAIL ; les anciens FAIL connus comme `Session sans expiry` deferred-items.md restent acceptables). Documenter le statut dans le SUMMARY.

    **Vérification quantitative** : Compter le nombre de `addUld();` (sans args) dans tests/tests.html APRÈS patch. La cible est strictement décroissante :
    - Avant patch : ~63 occurrences total dont ≥ 8 sites multi-ULD pré-existants critiques
    - Après patch : ≥ 8 sites convertis à `addUld(false, true);` (donc ~13 disparitions de `addUld();` consécutifs vu les 2-3 par site)
    - Restent : les `addUld();` single-ULD non critiques + les 3-5 nouveaux tests Task 1/2/3 qui veulent SPÉCIFIQUEMENT `addUld()` pour tester MAT-13 et MAT-14
  </action>
  <verify>
    <automated>grep -c "addUld(false, true)" tests/tests.html returns >= 8 AND grep -c "addUld(); addUld();" tests/tests.html returns 0 AND grep -c "addUld(); addUld(); addUld();" tests/tests.html returns 0</automated>
  </verify>
  <done>
    - `grep -c "addUld(false, true)" tests/tests.html` retourne >= 8 (au moins 8 occurrences, peut être plus si les patches inline `addUld(); addUld();` produisent des occurrences multiples)
    - `grep -c "addUld(); addUld();" tests/tests.html` retourne 0 (TOUS les enchaînements `addUld(); addUld();` ont été convertis)
    - `grep -c "addUld(); addUld(); addUld();" tests/tests.html` retourne 0 (TOUS les triple-enchaînements convertis)
    - `npm run verify` après patch : toutes les suites pré-existantes vertes (0 nouveau FAIL)
    - Documenter dans le SUMMARY le nombre exact d'occurrences de `addUld()` (sans args) avant/après patch + le nombre de sites convertis
  </done>
</task>

</tasks>

<verification>
Phase 1 plan 03 EXTENDED — vérifications globales (à tester en local via `npx serve .` AVANT push) :

**Feature A — RECAP-01 (récap inline) — comportement préservé :**
1. Créer ULD avec sangles=3, forfait EU, commentaire "Fragile" → sous le header ULD : `Sangles: 3 | Planchers EU: forfait | Com: Fragile`. Pas de régression vs Plan 01-03 historique.

**Feature B — MAT-12 "Rien à facturer" :**
2. Ouvrir le modal d'un ULD → checkbox "Rien à facturer pour cette ULD" visible AU-DESSUS du grid (zone ambre).
3. Cocher la case → tous les autres champs deviennent grisés (disabled). Décocher → réactivation (sauf input plancher EU si forfait reste coché — D-06 préservé).
4. Valider avec case cochée → récap inline = `Rien à facturer` (style pill ambre, classe `.mat-recap-no-billing` émise par refreshMaterialBadge).
5. Sauvegarder → recharger l'ancien manifeste depuis la liste → flag restauré, récap `Rien à facturer` visible.
6. Générer PDF d'un manifeste avec ULD "Rien à facturer" → page ULD section "Materiel" affiche 1 ligne `Rien à facturer`.
7. Envoyer email de test → htmlBody contient bloc Matériel avec `Rien à facturer` (vérifier dans Network tab).

**Feature C — MAT-13 saisie obligatoire :**
8. ULD #1 vierge (rien rempli) → cliquer "+ Ajouter une ULD" → alert FR : "ULD N°1 : matériel non saisi. Cochez 'Rien à facturer' ou remplissez au moins un champ avant d'ajouter une nouvelle ULD." Le modal de l'ULD #1 se rouvre. Pas de 2e ULD créée.
9. Cocher "Rien à facturer" sur ULD #1, valider, cliquer "+ Ajouter une ULD" → ULD #2 créée + son modal s'ouvre (MAT-14).
10. Avec ULD #1 OK + ULD #2 vierge → cliquer "Generer le PDF" → alert FR : "Matériel non saisi pour : ULD N°2. Veuillez remplir le matériel ou cocher 'Rien à facturer' pour ces ULD." PDF non généré.
11. Idem "Envoyer par email" → alert AVANT le check destinataires/JWT, fetch /api/send-email NON déclenché.

**Feature D — MAT-14 auto-ouverture :**
12. Ouvrir l'app, se connecter → newManifest() crée ULD #1 + le modal matériel s'ouvre automatiquement.
13. Cliquer "Annuler" puis "+ Ajouter une ULD" (avec ULD #1 OK) → ULD #2 créée + modal s'ouvre auto.
14. Sauvegarder un manifeste, créer un nouveau, recharger l'ancien depuis la liste → les ULD apparaissent SANS modal ouvert (action de chargement, pas saisie utilisateur).

**TEST-02 round-trip étendu :**
15. Sauvegarder un manifeste avec 2 ULD : #1 noMaterialToBill=true, #2 strapsCount=5 → recharger → les flags sont restaurés correctement, récaps corrects pour chacune.

**Tests** :
16. `npm run verify` (tests/run-harness.cjs Node + JSDOM) → 0 FAIL OU même 1 échec pré-existant `Session sans expiry` (deferred-items.md). Toutes les nouvelles suites MAT-12, MAT-13, MAT-14 vertes. Sites pré-existants `addUld(); addUld();` patchés via Task 5 → suites Plan 01-01/01-02 + Phase 2 toutes vertes.
17. `tests/tests.html` ouvert dans le navigateur en local → mêmes résultats verts.

**Pas de régression :**
18. Suites Plan 01-01 (modal, collectData, loadManifest, retro-compat, save/load, modal ouverture, forfait, applyMaterialToUld, badge → recap inline historique) → toutes vertes.
19. Suites Plan 01-02 (PDF buildMaterialSummary, formatFlooringDisplay, buildUldMaterialRows, buildPdf, email HTML, XSS uldComment) → toutes vertes.
20. Suites Plan 01-03 historique (recap inline condense, forfait litteral, omet zeros, XSS uldComment recap, troncature, vide=pas de span, loadManifest restaure recap) → toutes vertes.
21. Suites Phase 2 (Type ULD VRAC) → toutes vertes (notamment celles qui utilisent addUld() et openMaterialModal — l'autoOpen MAT-14 ne casse rien grâce à l'idempotence de openMaterialModal ; les sites multi-ULD pré-existants sont patchés Task 5).
22. Suite Phase 3 (E2E lifecycle) → toujours verte.

**D-13 préservé** : la barre `#liveRecap` (ULD/Colis/Poids/LTA/DGR) reste strictement identique. AUCUNE info matériel dedans.
</verification>

<success_criteria>
- **Feature A (RECAP-01 + MAT-11 préservé)** : `formatCondensedMaterial` court-circuit "Rien à facturer" ajouté ; `refreshMaterialBadge` émet la classe `.mat-recap-no-billing` quand applicable (logique JS intégrée en Task 1, BLOCKER #2 fix). Aucune régression sur le format `Sangles: N | Planchers EU: forfait | ... | Com: <tronqué>` existant.
- **Feature B (MAT-12)** : checkbox `#mat-no-billing` présente dans le modal ; `toggleNoBilling` désactive/réactive les autres champs ; `applyMaterialToUld` neutralise les autres data-attributes (incluant `data-flooring-eu-forfait` et `data-flooring-std-forfait` à `'false'`) quand cochée ; `loadManifest` lit défensivement `noMaterialToBill` ; `collectData` inclut le flag ; `buildUldMaterialRows` retourne `[['', 'Rien à facturer']]` court-circuit ; `buildUldMaterialHtml` affiche "Rien à facturer" échappé via esc().
- **Feature C (MAT-13)** : helper `uldHasMaterial(block)` + helper `findIncompleteUlds()` ; validation bloquante avec alert FR sur 3 surfaces (`addUld` au début sauf si `skipValidation=true`, `generatePdf` EN TOUT DÉBUT, `sendEmail` EN TOUT DÉBUT). Le pattern `alert()` est cohérent avec le projet.
- **Feature D (MAT-14)** : `addUld(autoOpen=true par défaut, skipValidation=false par défaut)` ouvre le modal en fin de fonction ; `loadManifest` ne passe pas par addUld → modal NE s'ouvre PAS au rechargement (par construction). Paramètre `skipValidation` documenté comme usage interne / tests uniquement.
- **CSS (Task 4 CSS-ONLY)** : zone `.mat-no-billing-row` ambre au-dessus du grid + `.mat-recap-no-billing` variante récap + règles disabled étendues (textarea, checkboxes) + version mobile compacte de `.mat-no-billing-row`. Aucune modification JS (BLOCKER #2 fix).
- **Tests adaptés (Task 5)** : ≥ 8 sites pré-existants `addUld(); addUld();` migrés vers `addUld(false, true);`. `grep -c "addUld(); addUld();" tests/tests.html` retourne 0 après patch.
- **Tests nouveaux** : ≥ 8 nouveaux tests (cible : 12 Task 1 MAT-12 + 10 Task 2 MAT-13 + 6 Task 3 MAT-14 = 28 tests, mais minimum demandé 8 par quality_gate). Suites pré-existantes Plan 01-01/01-02/01-03 historique toutes vertes APRÈS Task 5.
- **Mobile (RECAP-03 étendu)** : zone `.mat-no-billing-row` lisible ≤ 768px ; récap `Rien à facturer` ne déborde pas.
- **Rétro-compat (MAT-10 étendu)** : un manifeste sans la clé `noMaterialToBill` se charge avec default `false` (pas d'erreur, pas de ULD bloquante).
- **Critère #3 ROADMAP toujours satisfait** (RECAP-01 reste fermé) ; les 3 nouveaux requirements MAT-12/13/14 satisfaits avec evidence vérifiable par grep + tests automatisés + UAT manuel.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mat-riel-uld-r-tro-compat/01-03-SUMMARY.md` documenting :
- Décision : Plan 01-03 RÉÉCRIT pour étendre le périmètre — couvre désormais 4 features (A récap inline + B "Rien à facturer" + C validation obligatoire + D auto-ouverture) sur 6 requirements (RECAP-01, MAT-11, MAT-12, MAT-13, MAT-14, TEST-02). Revision 1/3 (checker feedback) appliquée : 4 blockers + 7 warnings adressés.
- Fonctions publiques ajoutées :
  - `toggleNoBilling(checkbox)` (handler onchange checkbox no-billing modal)
  - `uldHasMaterial(block)` (helper validation MAT-13, retourne boolean)
  - `findIncompleteUlds()` (helper retournant la liste des indices ULD sans matériel)
- Fonctions publiques étendues :
  - `openMaterialModal` (ajout zone .mat-no-billing-row + checkbox #mat-no-billing + disabledAttrIfNoBilling sur 10 inputs/checkboxes/textarea)
  - `applyMaterialToUld` (lecture noBilling + neutralisation des autres champs si coché — incluant forfait EU/Std neutralisés à 'false')
  - `formatCondensedMaterial` (court-circuit `return 'Rien à facturer'` si dataset.noBilling==='true' — précédence noBilling > VRAC > champs détaillés)
  - `refreshMaterialBadge` (ajout classe `.mat-recap-no-billing` quand applicable — Task 1 §5, intégré dans la même fonction étendue, BLOCKER #2 fix)
  - `addUld(autoOpen, skipValidation)` (signature étendue : autoOpen default true, skipValidation default false ; validation MAT-13 EN TÊTE encadrée par `if (!skipValidation)` ; openMaterialModal en FIN si autoOpen)
  - `loadManifest` (lecture défensive `data-no-billing` depuis `uldData.noMaterialToBill === true`)
  - `collectData` (ajout `uldEntry.noMaterialToBill = block.dataset.noBilling === 'true'`)
  - `generatePdf` (validation MAT-13 EN TOUT DÉBUT, AVANT validateRequired et saveManifest — BLOCKER #4 fix)
  - `sendEmail` (validation MAT-13 EN TOUT DÉBUT, AVANT validateRequired et tout await)
  - `buildUldMaterialRows` (court-circuit `return [['', 'Rien à facturer']]` si u.noMaterialToBill)
  - `buildUldMaterialHtml` (bénéficie automatiquement du court-circuit)
- Nouveau data-attribute : `data-no-billing` ('true' | 'false') sur `.uld-block`
- Nouvelle clé JSON manifeste : `noMaterialToBill: boolean`
- Classes CSS ajoutées (Task 4 CSS-only) : `.mat-no-billing-row`, `.mat-no-billing-label`, `.mat-recap-no-billing` (desktop + mobile)
- Tests pré-existants adaptés (Task 5) : ≥ 8 sites `addUld(); addUld();` migrés vers `addUld(false, true);` (liste exhaustive : suite removeUld L191-192, Type ULD - collectData L1000-1001, Type ULD - updateRecap dont Vrac L1156-1157, updateRecap pas annotation L1176, updateRecap totaux globaux L1189, updateRecap plusieurs VRAC L1209, sendEmail integration L2023-2024, updateRecap multi ULD L2129-2130). Documenter le compte avant/après.
- Suites de tests nouvelles ajoutées : 12 (Task 1) + 10 (Task 2) + 6 (Task 3) = 28 nouvelles suites couvrant les 4 features (vs minimum 8 demandé)
- Note rétro-compat : `noMaterialToBill` absent ⇒ default false (rétro-compat MAT-10 préservée)
- Pattern alert() FR : libellés exacts utilisés
  - addUld bloqué : `'ULD N°{n} : matériel non saisi. Cochez "Rien à facturer" ou remplissez au moins un champ avant d\'ajouter une nouvelle ULD.'`
  - generatePdf/sendEmail bloqués : `'Matériel non saisi pour : ULD N°{a}, ULD N°{b}. Veuillez remplir le matériel ou cocher "Rien à facturer" pour ces ULD.'`
- Pointer vers 01-VERIFICATION.md pour traçabilité du gap RECAP-01 fermé (toujours valide)
- Si re-vérification effectuée : statut MAT-12, MAT-13, MAT-14 passent de Pending à SATISFIED dans REQUIREMENTS.md
</output>
