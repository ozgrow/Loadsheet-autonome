---
phase: 01-mat-riel-uld-r-tro-compat
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - static/js/app.js
  - static/css/style.css
  - tests/tests.html
autonomous: true
gap_closure: true
requirements:
  - RECAP-01
  - MAT-11
  - TEST-02

# CONTEXT OVERRIDE (decision prise lors du gap closure planning) :
# - D-13 (#liveRecap inchangé) : CONSERVÉ (la barre globale reste ULD/Colis/Poids/LTA).
# - D-14 (badge neutre suffisant) : ANNULÉ — le badge est remplacé par un récap condensé inline
#   sous le header de chaque ULD, conformément à la lecture littérale du critère #3 du ROADMAP.
# Source : 01-VERIFICATION.md "Option A — alignement strict" (recommandation post-vérification)
# Le commentaire libre uldComment, désormais réintroduit visuellement dans le DOM, devient un nouveau
# vecteur XSS et DOIT passer par esc() (MAT-11) avec troncature pour préserver le layout mobile.

must_haves:
  truths:
    - "Sous le header de chaque ULD ayant au moins un champ matériel non-vide, un récap inline condensé s'affiche listant les valeurs saisies (au lieu du badge neutre 'Matériel saisi')"
    - "Le format de chaque entrée est `<libellé FR>: <valeur>` séparé par ` | ` (pipe entouré d'espaces)"
    - "Les champs matériel à 0 (et forfait=false) sont OMIS du récap (cohérent avec D-16 / specifics PDF)"
    - "Les checkbox forfait cochées s'affichent littéralement avec le mot `forfait` (cohérent avec D-16 PDF/email)"
    - "Le commentaire libre `uldComment` apparaît tronqué (max 30 caractères + `…` si plus long) et échappé via esc() — XSS payload affiché littéralement, jamais exécuté (MAT-11)"
    - "Si TOUS les champs matériel d'une ULD sont vides/0/false, le récap n'affiche rien (pas de span vide, pas de badge fantôme)"
    - "Le récap reste lisible sur écran ≤ 768px (wrap naturel ou word-break au pipe, pas d'overflow horizontal du bloc ULD)"
    - "La barre globale #liveRecap (ULD/Colis/Poids/LTA) reste strictement inchangée (D-13 conservé)"
    - "loadManifest restaure le récap inline pour chaque ULD chargée (appel à refreshMaterialBadge déjà en place app.js:533, vérifier qu'il rend le nouveau format)"
    - "tests/tests.html contient au minimum 3 nouveaux tests dans la suite 'Materiel ULD - recap inline condense (RECAP-01)' couvrant : valeurs présentes dans le DOM, forfait littéral, échappement XSS uldComment"
  artifacts:
    - path: "static/js/app.js"
      provides: "Fonction refreshMaterialBadge réécrite (génère récap inline condensé) + helper formatCondensedMaterial(block) retournant le HTML/string du récap par ULD"
      contains: "function formatCondensedMaterial"
    - path: "static/css/style.css"
      provides: "Classe .material-recap (remplace ou complète .material-badge) avec règles desktop + media query mobile pour wrap"
      contains: ".material-recap"
    - path: "tests/tests.html"
      provides: "Suite 'Materiel ULD - recap inline condense (RECAP-01)' avec ≥ 3 tests (valeurs DOM, forfait, XSS uldComment) + adaptation suite existante 'Materiel ULD - badge Materiel saisi'"
      contains: "Materiel ULD - recap inline condense"
  key_links:
    - from: "applyMaterialToUld (app.js:127)"
      to: "refreshMaterialBadge(uldIndex)"
      via: "appel direct"
      pattern: "refreshMaterialBadge\\(uldIndex\\)"
    - from: "loadManifest (app.js:533)"
      to: "refreshMaterialBadge(i)"
      via: "appel direct dans le forEach uldList"
      pattern: "refreshMaterialBadge\\(i\\)"
    - from: "refreshMaterialBadge"
      to: ".uld-block dataset (data-straps, data-flooring-eu, ..., data-uld-comment)"
      via: "lecture des data-attributes pour construire le récap"
      pattern: "block\\.dataset\\.straps|block\\.dataset\\.uldComment"
    - from: "récap inline rendu"
      to: "uldComment"
      via: "esc() + troncature avant innerHTML"
      pattern: "esc\\(.*comment|esc\\(.*uldComment"
---

<objective>
Fermer le gap RECAP-01 identifié par 01-VERIFICATION.md : remplacer le badge neutre `'Matériel saisi'` (refreshMaterialBadge app.js:133) par un récap condensé inline qui affiche réellement les valeurs matériel saisies pour chaque ULD, sous la forme `Sangles: 3 | Planchers EU: forfait | Bois calage: 2 | Com: Fragile`.

Purpose: Le critère #3 du ROADMAP exige littéralement « Le récapitulatif écran affiche les infos matériel de chaque ULD sous forme condensée ». L'implémentation actuelle (décision D-14, badge neutre) ne satisfait pas cette lecture : l'agent doit rouvrir le modal pour voir ses saisies, ce qui contredit l'intention « affichage écran ». Ce plan aligne strictement le produit sur le critère via l'Option A de 01-VERIFICATION.md.

Output:
- `static/js/app.js` : `refreshMaterialBadge` réécrite + helper `formatCondensedMaterial(block)` qui génère le HTML condensé (avec esc() et troncature uldComment).
- `static/css/style.css` : nouvelle classe `.material-recap` (et conservation rétro-compat de `.material-badge` si pertinent) avec règle mobile dédiée.
- `tests/tests.html` : suite `'Materiel ULD - recap inline condense (RECAP-01)'` avec tests valeurs/forfait/XSS, et adaptation/remplacement des suites `'Materiel ULD - badge Materiel saisi'` (727) et `'Materiel ULD - pas de badge si tout vide'` (742) qui assertent l'ancien comportement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-VERIFICATION.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-01-SUMMARY.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-02-SUMMARY.md
@CLAUDE.md
@static/js/app.js
@static/css/style.css
@tests/tests.html

<interfaces>
<!-- État actuel après Plan 01-01 + 01-02 (vérifié dans 01-VERIFICATION.md) -->

Fonction CIBLE à réécrire (static/js/app.js:131-149) :
```javascript
// AVANT (état actuel)
function refreshMaterialBadge(uldIndex) {
    var block = document.getElementById('uld-' + uldIndex);
    var wrapper = document.getElementById('material-badge-' + uldIndex);
    if (!block || !wrapper) return;
    var hasAny =
        (parseInt(block.dataset.straps) || 0) > 0 ||
        (parseInt(block.dataset.flooringEu) || 0) > 0 ||
        block.dataset.flooringEuForfait === 'true' ||
        (parseInt(block.dataset.flooringStd) || 0) > 0 ||
        block.dataset.flooringStdForfait === 'true' ||
        (parseInt(block.dataset.blocks) || 0) > 0 ||
        (parseInt(block.dataset.tarps) || 0) > 0 ||
        (parseInt(block.dataset.dividers) || 0) > 0 ||
        (parseInt(block.dataset.honeycomb) || 0) > 0 ||
        (block.dataset.uldComment || '').length > 0;
    wrapper.innerHTML = hasAny ? '<span class="material-badge">Matériel saisi</span>' : '';
}
```

Fonction HELPER existante à NE PAS dupliquer (déjà OK pour PDF/email, app.js:~660 zone Plan 01-02) :
```javascript
function buildUldMaterialRows(u)  // retourne [['Sangles', '3'], ['Planchers bois EU', 'forfait'], ...] omettant zéros
```
**Décision** : NE PAS appeler `buildUldMaterialRows` directement depuis le DOM (il prend un objet `u` issu de `collectData`, pas un `block`). Créer un helper dédié `formatCondensedMaterial(block)` qui lit les data-attributes — symétrique au pattern de `refreshMaterialBadge` et `openMaterialModal` (les deux lisent `block.dataset`).

Source de vérité : `block.dataset.*` sur `.uld-block` (cf. décision D-08 + 01-01-SUMMARY.md).
- `block.dataset.straps` (string représentant int)
- `block.dataset.flooringEu` (string int)
- `block.dataset.flooringEuForfait` ('true' | 'false')
- `block.dataset.flooringStd` (string int)
- `block.dataset.flooringStdForfait` ('true' | 'false')
- `block.dataset.blocks` (string int)
- `block.dataset.tarps` (string int)
- `block.dataset.dividers` (string int)
- `block.dataset.honeycomb` (string int)
- `block.dataset.uldComment` (string libre — vecteur XSS, esc() obligatoire)

Helper existant à OBLIGATOIREMENT réutiliser (app.js:16-19) :
```javascript
function esc(str)  // anti-XSS, MANDATORY pour tout innerHTML avec donnée user
```

Conteneur DOM cible (créé par addUld + loadManifest, Plan 01-01) :
```html
<div class="material-badge-wrapper" id="material-badge-{i}"></div>
```
Ce conteneur reste la cible de `refreshMaterialBadge` — pas de modification du HTML d'addUld/loadManifest. On change uniquement le contenu injecté.

CSS existant à étendre (style.css:159-168 + media 768px à 219-225) :
```css
.material-badge-wrapper { margin-top: 4px; margin-bottom: 4px; }
.material-badge { display: inline-block; font-size: 0.75em; color: #718096; ... }
```

Pattern de test à mirrorer (tests/tests.html:727-751, 754-773) :
```javascript
suite('Materiel ULD - badge Materiel saisi');
(function() {
    resetDOM();
    addUld();
    var i = uldCount;
    openMaterialModal(i);
    var modal = document.querySelector('.material-modal');
    modal.querySelector('.mat-straps').value = '3';
    applyMaterialToUld(i);
    var wrap = document.getElementById('material-badge-' + i);
    assert('Badge wrapper contient .material-badge', !!wrap.querySelector('.material-badge'));
    assertEqual('Texte badge', wrap.querySelector('.material-badge').textContent, 'Matériel saisi');
})();
```
Cette suite (727-751) ASSERTE l'ancien comportement : elle DOIT être adaptée (sinon 2 tests rouges après le changement).

Flag XSS du harness (tests/tests.html:60-61) :
```javascript
window._xss = 0;  // incrémenté par les payloads d'attaque ; tests vérifient qu'il reste à 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Réécrire refreshMaterialBadge en récap inline condensé + helper formatCondensedMaterial avec esc() + troncature uldComment</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js (relire en entier les lignes 1-150 : esc, openMaterialModal, applyMaterialToUld, refreshMaterialBadge — la fonction cible)
    - static/js/app.js (relire les lignes ~600-700 où buildUldMaterialRows et formatFlooringDisplay sont définis par Plan 01-02 — pour COHÉRENCE de libellés FR exacts)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-VERIFICATION.md (Gap Summary, Options pour fermer le gap, Recommandation — Option A retenue)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (D-13 conservé, D-14 ANNULÉ, D-19/D-20 anti-XSS uldComment)
    - tests/tests.html (lignes 727-786 : suites badge actuelles à adapter ; lignes 60-61 : pattern window._xss ; lignes 754-773 : pattern test XSS)
    - CLAUDE.md (sections Sécurité — esc() obligatoire ; Tests — règle nouvelle feature = nouveaux tests)
  </read_first>
  <behavior>
    - Test 1 (récap contient les valeurs saisies) : Créer une ULD, ouvrir modal, set straps=5, set blocks=2, valider. `document.getElementById('material-badge-' + i).textContent` contient EXACTEMENT la chaîne `Sangles: 5 | Bois calage: 2` (ordre = ordre canonique défini ci-dessous).
    - Test 2 (forfait affiché littéralement "forfait") : Créer une ULD, ouvrir modal, cocher checkbox `mat-flooring-eu-forfait`, valider. Le récap contient la sous-chaîne `Planchers EU: forfait` (PAS `Planchers EU: 0`, PAS `Planchers EU: true`).
    - Test 3 (zéros omis, pas de pipes orphelins) : Créer une ULD avec uniquement straps=4, valider. `textContent` du récap === `Sangles: 4` (pas de ` | ` en trop, pas de mention `Planchers EU: 0`).
    - Test 4 (XSS uldComment échappé) : Reset window._xss=0. Créer une ULD, ouvrir modal, set uldComment = `<img src=x onerror="window._xss++">`, valider. Le récap : (a) NE contient PAS la chaîne littérale `<img src=x onerror=` dans son `innerHTML`, (b) contient l'entité échappée `&lt;img`, (c) `window._xss === 0`, (d) `wrapper.querySelectorAll('img').length === 0`.
    - Test 5 (uldComment tronqué à 30 caractères + ellipsis) : Set uldComment = `Lorem ipsum dolor sit amet consectetur adipiscing elit` (54 chars). Le récap contient `Com: Lorem ipsum dolor sit amet …` (30 chars de comment + `…`). La chaîne complète NE doit PAS apparaître.
    - Test 6 (vide = pas de récap) : Créer une ULD, ouvrir modal, valider sans rien remplir. `document.getElementById('material-badge-' + i).innerHTML === ''` (strictement vide, pas d'élément span vide).
    - Test 7 (loadManifest restaure le récap) : await getSavedManifests, écrire un manifeste avec ULD ayant strapsCount=7 + uldComment="Fragile", await loadManifest(id), vérifier que le récap chargé contient `Sangles: 7 | Com: Fragile`.
  </behavior>
  <action>
    **1) Réécrire `refreshMaterialBadge` dans static/js/app.js (remplacer lignes 131-149 actuelles)** :

    Le nouveau code injecte un récap inline condensé via `formatCondensedMaterial(block)`. Si la chaîne retournée est vide → conteneur vide (pas de span fantôme).

    ```javascript
    // ============================================
    // RECAP INLINE CONDENSE (RECAP-01 — D-14 OVERRIDDEN par 01-VERIFICATION.md Option A)
    // ============================================
    // Construit une chaîne condensée représentant les valeurs matériel non-nulles
    // de l'ULD, lisible directement à l'écran sous le header de l'ULD.
    // Format : "Sangles: 3 | Planchers EU: forfait | Bois calage: 2 | Com: Fragile"
    // Champs à 0 / forfait=false / commentaire vide : OMIS.
    // uldComment : esc() + troncature 30 chars + "…" si plus long (anti-XSS MAT-11 + layout mobile).
    // Retourne un STRING HTML déjà échappé, prêt à être injecté en innerHTML.
    function formatCondensedMaterial(block) {
        if (!block) return '';
        var parts = [];
        var straps = parseInt(block.dataset.straps) || 0;
        if (straps > 0) parts.push('Sangles: ' + straps);
        // Planchers EU : "forfait" si checkbox cochée, sinon le nombre (si > 0)
        var flEuF = block.dataset.flooringEuForfait === 'true';
        var flEu = parseInt(block.dataset.flooringEu) || 0;
        if (flEuF) parts.push('Planchers EU: forfait');
        else if (flEu > 0) parts.push('Planchers EU: ' + flEu);
        // Planchers Std : symétrique
        var flStdF = block.dataset.flooringStdForfait === 'true';
        var flStd = parseInt(block.dataset.flooringStd) || 0;
        if (flStdF) parts.push('Planchers Std: forfait');
        else if (flStd > 0) parts.push('Planchers Std: ' + flStd);
        // Autres compteurs : omettre si 0
        var blocks = parseInt(block.dataset.blocks) || 0;
        if (blocks > 0) parts.push('Bois calage: ' + blocks);
        var tarps = parseInt(block.dataset.tarps) || 0;
        if (tarps > 0) parts.push('Bâches: ' + tarps);
        var dividers = parseInt(block.dataset.dividers) || 0;
        if (dividers > 0) parts.push('Intercalaires: ' + dividers);
        var honeycomb = parseInt(block.dataset.honeycomb) || 0;
        if (honeycomb > 0) parts.push("Nids d'abeille: " + honeycomb);
        // Commentaire : esc() + troncature 30 + "…" (MAT-11 / D-19)
        var rawComment = String(block.dataset.uldComment || '');
        if (rawComment.length > 0) {
            var truncated = rawComment.length > 30 ? (rawComment.slice(0, 30) + '…') : rawComment;
            parts.push('Com: ' + esc(truncated));
        }
        if (parts.length === 0) return '';
        // Séparateur : " | " (espace-pipe-espace) ; les libellés sont littéraux et sûrs (pas user input)
        return parts.join(' | ');
    }

    // Affiche/masque le récap condensé inline sous le header ULD.
    // Remplace le badge neutre 'Matériel saisi' (D-14 OVERRIDDEN par 01-VERIFICATION.md).
    // Note : la fonction garde son nom historique 'refreshMaterialBadge' pour ne pas casser
    // les 2 appelants existants (applyMaterialToUld app.js:127, loadManifest app.js:533).
    function refreshMaterialBadge(uldIndex) {
        var block = document.getElementById('uld-' + uldIndex);
        var wrapper = document.getElementById('material-badge-' + uldIndex);
        if (!block || !wrapper) return;
        var content = formatCondensedMaterial(block);
        // content est DÉJÀ échappé pour uldComment ; les libellés et nombres sont sûrs (statiques / parseInt).
        wrapper.innerHTML = content ? '<span class="material-recap">' + content + '</span>' : '';
    }
    ```

    **Notes critiques pour l'executor** :
    - **Ne PAS double-échapper** : `formatCondensedMaterial` applique `esc()` UNIQUEMENT sur `uldComment` (le seul vecteur XSS, cf. D-19/D-20). Les libellés FR sont des chaînes littérales statiques. Les nombres sont issus de `parseInt(...) || 0` donc sûrs (entiers JS, pas de chaîne user).
    - **Ne PAS appeler `esc()` sur le résultat final** dans `refreshMaterialBadge` (sinon `&lt;` deviendrait `&amp;lt;`).
    - **Conserver le nom `refreshMaterialBadge`** : 2 appels existent (app.js:127 dans `applyMaterialToUld`, app.js:533 dans `loadManifest`) ; les renommer demanderait des modifs à 3 endroits sans bénéfice.
    - **Ne PAS toucher** à `applyMaterialToUld`, `loadManifest`, `addUld`, `openMaterialModal`, `collectData`, `buildUldMaterialRows` : ces fonctions sont déjà correctes et appellent ou produisent les bonnes données.
    - **Ne PAS toucher** à `#liveRecap` ni à `updateRecap()` : la barre globale reste inchangée (D-13 conservé).

    **2) Adapter les suites de tests existantes dans tests/tests.html** qui assertent l'ANCIEN comportement (sinon 2 tests rouges) :

    **A. Suite ligne 727-739 `'Materiel ULD - badge Materiel saisi'`** : remplacer entièrement par la nouvelle suite (voir étape 3 ci-dessous, Test 1). Le nom de suite peut rester ou être renommé `'Materiel ULD - recap inline condense (RECAP-01)'`.

    **B. Suite ligne 742-751 `'Materiel ULD - pas de badge si tout vide'`** : adapter le nom en `'Materiel ULD - pas de recap si tout vide'`. La logique reste identique (`assertEqual('Wrapper vide', wrap.innerHTML, '')`) — ce comportement est préservé par la nouvelle implémentation (Test 6).

    **C. Suite ligne 754-773 `'Materiel ULD - XSS uldComment (MAT-11)'`** : RENFORCER avec une assertion supplémentaire vérifiant le récap inline (le test actuel vérifie uniquement modal + textarea ; ajouter qu'aucun img n'est dans `material-badge-wrapper` non plus, et que `window._xss === 0` après injection du récap dans le DOM).

    **3) Ajouter la nouvelle suite dans tests/tests.html** — `'Materiel ULD - recap inline condense (RECAP-01)'` — placée APRÈS la suite existante `'Materiel ULD - closeMaterialModal'` (tests.html:776-785) et AVANT le bloc PLAN 01-02 (`// ============================================ // PLAN 01-02 : Materiel PDF`, tests.html:787) :

    ```javascript
    // ============================================
    // PLAN 01-03 : Materiel ULD - recap inline condense (RECAP-01 strict)
    // Ferme le gap identifié par 01-VERIFICATION.md (Option A — D-14 OVERRIDDEN)
    // ============================================

    // --- Recap inline : valeurs presentes dans le DOM ---
    suite('Materiel ULD - recap inline condense (RECAP-01)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        modal.querySelector('.mat-straps').value = '5';
        modal.querySelector('.mat-blocks').value = '2';
        applyMaterialToUld(i);
        var wrap = document.getElementById('material-badge-' + i);
        var recap = wrap.querySelector('.material-recap');
        assert('Element .material-recap present', !!recap);
        assertEqual('Texte recap = "Sangles: 5 | Bois calage: 2"', recap.textContent, 'Sangles: 5 | Bois calage: 2');
    })();

    // --- Recap inline : forfait affiche litteralement ---
    suite('Materiel ULD - recap forfait litteral (RECAP-01 / D-16)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        modal.querySelector('.mat-flooring-eu-forfait').checked = true;
        // Simuler le toggle (D-06 : input desactive et value=0)
        modal.querySelector('.mat-flooring-eu').disabled = true;
        modal.querySelector('.mat-flooring-eu').value = '0';
        applyMaterialToUld(i);
        var wrap = document.getElementById('material-badge-' + i);
        var recap = wrap.querySelector('.material-recap');
        assert('Recap contient "Planchers EU: forfait"', recap.textContent.indexOf('Planchers EU: forfait') >= 0);
        assert('Recap ne contient PAS "Planchers EU: 0"', recap.textContent.indexOf('Planchers EU: 0') < 0);
        assert('Recap ne contient PAS "Planchers EU: true"', recap.textContent.indexOf('Planchers EU: true') < 0);
    })();

    // --- Recap inline : zeros omis, pas de pipes orphelins ---
    suite('Materiel ULD - recap omet zeros (RECAP-01)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        modal.querySelector('.mat-straps').value = '4';
        applyMaterialToUld(i);
        var wrap = document.getElementById('material-badge-' + i);
        var recap = wrap.querySelector('.material-recap');
        assertEqual('Recap = "Sangles: 4" (rien d\'autre)', recap.textContent, 'Sangles: 4');
        assert('Pas de "Planchers EU: 0"', recap.textContent.indexOf('Planchers EU: 0') < 0);
        assert('Pas de "Bois calage: 0"', recap.textContent.indexOf('Bois calage: 0') < 0);
        assert('Pas de pipe orphelin "| |"', recap.textContent.indexOf('| |') < 0);
        assert('Pas de pipe en debut " | S"', recap.textContent.indexOf(' | S') !== 0);
    })();

    // --- Recap inline : XSS uldComment echappe (MAT-11) ---
    suite('Materiel ULD - recap XSS uldComment (RECAP-01 / MAT-11)');
    (function() {
        resetDOM();
        window._xss = 0;
        addUld();
        var i = uldCount;
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        modal.querySelector('.mat-uld-comment').value = '<img src=x onerror="window._xss++">';
        applyMaterialToUld(i);
        var wrap = document.getElementById('material-badge-' + i);
        // Le innerHTML doit contenir l'entite echappee, pas le tag litteral
        assert('innerHTML ne contient pas "<img src=x onerror=" litteral', wrap.innerHTML.indexOf('<img src=x onerror=') < 0);
        assert('innerHTML contient l\'entite "&lt;img"', wrap.innerHTML.indexOf('&lt;img') >= 0);
        // Aucune balise img injectee
        assertEqual('Aucun <img> injecte dans le wrapper', wrap.querySelectorAll('img').length, 0);
        // Pas d'execution
        assertEqual('window._xss inchange (pas d\'execution XSS)', window._xss, 0);
    })();

    // --- Recap inline : uldComment tronque a 30 chars + ellipsis ---
    suite('Materiel ULD - recap uldComment tronque (RECAP-01)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        openMaterialModal(i);
        var modal = document.querySelector('.material-modal');
        var longComment = 'Lorem ipsum dolor sit amet consectetur adipiscing elit'; // 54 chars
        modal.querySelector('.mat-uld-comment').value = longComment;
        applyMaterialToUld(i);
        var wrap = document.getElementById('material-badge-' + i);
        var recap = wrap.querySelector('.material-recap');
        // 30 premiers chars + "…" (ellipsis Unicode U+2026)
        assert('Recap contient "Com: Lorem ipsum dolor sit amet…"', recap.textContent.indexOf('Com: Lorem ipsum dolor sit amet…') >= 0);
        assert('Recap NE contient PAS le commentaire complet', recap.textContent.indexOf('adipiscing elit') < 0);
    })();

    // --- Recap inline : vide = pas de span fantome ---
    suite('Materiel ULD - recap vide = pas de span (RECAP-01)');
    (function() {
        resetDOM();
        addUld();
        var i = uldCount;
        openMaterialModal(i);
        applyMaterialToUld(i); // tous les inputs sont vides/0/false par defaut
        var wrap = document.getElementById('material-badge-' + i);
        assertEqual('innerHTML strictement vide', wrap.innerHTML, '');
        assertEqual('Aucun .material-recap', wrap.querySelectorAll('.material-recap').length, 0);
    })();

    // --- Recap inline : loadManifest restaure le recap (rétro-compat avec données chiffrées) ---
    suite('Materiel ULD - loadManifest restaure recap (RECAP-01 / TEST-02)');
    await (async function() {
        resetDOM();
        await deriveAndStoreKey('test-password-for-unit-tests');
        // Ecrire un manifeste avec materiel
        var manifests = [{
            id: 'TEST-RECAP-01',
            date: '2026-04-23',
            client: '',
            agent: 'A',
            destAirport: 'CDG',
            ulds: [{
                uldNumber: 'U1',
                rows: [{ lta: 'L', dossier: 'D', colis: 1, dgr: 'N', comment: '' }],
                totalColis: 1,
                strapsCount: 7,
                flooringEuCount: 0, flooringEuForfait: false,
                flooringStdCount: 0, flooringStdForfait: false,
                blocksCount: 0, tarpsCount: 0, dividersCount: 0, honeycombCount: 0,
                uldComment: 'Fragile'
            }]
        }];
        await writeSavedManifests(manifests);
        await loadManifest('TEST-RECAP-01');
        // Apres loadManifest, le bloc ULD existe avec data-attributes + recap rendu
        var blocks = document.querySelectorAll('.uld-block');
        assertEqual('1 ULD chargee', blocks.length, 1);
        var wrap = document.querySelector('[id^="material-badge-"]');
        assert('Wrapper recap existe', !!wrap);
        var recap = wrap.querySelector('.material-recap');
        assert('Recap rendu apres loadManifest', !!recap);
        assertEqual('Recap = "Sangles: 7 | Com: Fragile"', recap.textContent, 'Sangles: 7 | Com: Fragile');
    })();
    ```

    **Ordre canonique des champs dans le récap** (à respecter dans `formatCondensedMaterial`) :
    1. Sangles
    2. Planchers EU
    3. Planchers Std
    4. Bois calage
    5. Bâches
    6. Intercalaires
    7. Nids d'abeille
    8. Com (commentaire, en dernier — peut être long)

    **Libellés FR exacts** (LITTÉRAUX dans le code, ne pas réinventer) :
    - `Sangles`
    - `Planchers EU` (court, distinct du libellé PDF `Planchers bois EU` — le mobile a moins d'espace)
    - `Planchers Std`
    - `Bois calage`
    - `Bâches`
    - `Intercalaires`
    - `Nids d'abeille`
    - `Com` (commentaire abrégé)

    **Séparateur** : ` | ` (espace + pipe + espace, exactement 3 caractères).

    **Troncature uldComment** : 30 caractères max + ellipsis Unicode `…` (U+2026, un seul caractère, pas trois points `...`).
  </action>
  <verify>
    <automated>grep -c "function formatCondensedMaterial" static/js/app.js returns 1 AND grep -c "Sangles: " static/js/app.js returns >= 1 AND grep -c "Planchers EU: forfait" static/js/app.js returns >= 1 AND grep -c "esc(truncated)" static/js/app.js returns 1 AND grep -c "material-recap" static/js/app.js returns >= 1 AND grep -cE "suite\('Materiel ULD - recap " tests/tests.html returns >= 5 AND grep -c "Materiel ULD - recap XSS uldComment" tests/tests.html returns 1</automated>
  </verify>
  <done>
    - `grep -n "function formatCondensedMaterial" static/js/app.js` retourne exactement 1 ligne
    - `grep -n "function refreshMaterialBadge" static/js/app.js` retourne exactement 1 ligne (ancienne version remplacée, pas dupliquée)
    - `grep -n "Matériel saisi" static/js/app.js` retourne 0 ligne (le badge neutre est bien remplacé, pas conservé en parallèle)
    - `grep -nE "esc\(truncated\)|esc\(.*comment" static/js/app.js` montre l'appel esc() sur le commentaire tronqué
    - `grep -nE "suite\('Materiel ULD - recap" tests/tests.html` retourne >= 5 nouvelles suites
    - Toutes les suites pre-existantes `Materiel ULD - badge Materiel saisi` et `Materiel ULD - pas de badge si tout vide` ont été ADAPTÉES (renommées ou réécrites) — `grep -n "Materiel ULD - badge Materiel saisi" tests/tests.html` retourne 0
    - En local (`npx serve .`), saisir une ULD avec straps=3, forfait EU coché, commentaire "Fragile" → sous le header ULD apparaît `Sangles: 3 | Planchers EU: forfait | Com: Fragile`
    - Saisir un commentaire `<img src=x onerror=alert(1)>` → le récap affiche `&lt;img src=x onerror=alert(1)&gt;` (rendu littéral), aucune alert, console propre
    - `tests/tests.html` en local : `Passed: 360+, Failed: <= 1` (failure pré-existante `Session sans expiry => invalide` documentée comme acceptable)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CSS .material-recap responsive (desktop inline + mobile wrap au pipe)</name>
  <files>static/css/style.css</files>
  <read_first>
    - static/css/style.css (relire en entier — surtout les blocs MATERIAL MODAL lignes 116-168 et MOBILE max-width 768px lignes 173-226)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (D-04 mobile pattern, RECAP-03 mobile lisible)
    - CLAUDE.md section Conventions (CSS desktop par défaut + media query 768px = seul breakpoint)
  </read_first>
  <action>
    **1) Ajouter la classe `.material-recap` dans static/css/style.css** APRÈS la règle `.material-badge` existante (ligne 161-164) et AVANT le bloc bouton (ligne 166-168) :

    ```css
    /* Material recap inline condense (RECAP-01 — D-14 OVERRIDDEN par 01-VERIFICATION.md) */
    .material-recap {
        display: inline-block;
        font-size: 0.78em;
        color: #4a5568;
        background: #edf2f7;
        padding: 3px 10px;
        border-radius: 4px;
        line-height: 1.5;
        word-break: break-word;
        max-width: 100%;
    }
    ```

    **Note** : `display: inline-block` + `word-break: break-word` + `max-width: 100%` permet le wrap naturel sur ligne suivante quand le contenu déborde (notamment au commentaire). Le séparateur ` | ` est un pipe ASCII donc CSS le traite comme un caractère normal — le break naturel se produira aux espaces autour du pipe.

    **2) Conserver `.material-badge`** dans le fichier (ligne 161-164) pour rétro-compatibilité visuelle — aucune référence ne doit être supprimée. La nouvelle classe `.material-recap` la remplace fonctionnellement, mais le CSS de l'ancienne ne gêne rien (aucun élément `.material-badge` ne sera plus créé après Task 1).

    **3) Ajouter une règle mobile dédiée dans le bloc `@media (max-width: 768px)` existant** (style.css:173-226), inserée APRÈS la règle existante `.material-modal-grid { grid-template-columns: 1fr; gap: 10px; }` (ligne 225) :

    ```css
        /* Material recap inline mobile (RECAP-03 — wrap naturel pour eviter overflow horizontal) */
        .material-recap {
            font-size: 0.72em;
            padding: 4px 8px;
            display: block;
            width: 100%;
        }
    ```

    Sur mobile, on passe en `display: block` + `width: 100%` pour garantir que le récap occupe toute la largeur disponible du `.uld-block` et wrap naturellement. La police légèrement réduite (0.72em vs 0.78em desktop) compense la verbosité potentielle du récap (plusieurs entrées séparées par ` | `).
  </action>
  <verify>
    <automated>grep -c "\.material-recap" static/css/style.css returns >= 2 AND awk '/@media \(max-width: 768px\)/{flag=1} flag && /\.material-recap/{count++} END{print count}' static/css/style.css returns >= 1 AND grep -c "word-break: break-word" static/css/style.css returns >= 1</automated>
  </verify>
  <done>
    - `grep -n "\.material-recap" static/css/style.css` retourne >= 2 lignes (déclaration desktop + déclaration mobile)
    - La déclaration desktop (`.material-recap {`) apparaît AVANT la ligne `@media (max-width: 768px)` (style.css:173)
    - La déclaration mobile (`.material-recap {`) apparaît APRÈS cette ligne et AVANT la fermeture `}` finale du bloc media
    - `grep -n "\.material-badge" static/css/style.css` retourne toujours >= 1 ligne (ancienne règle conservée, pas supprimée)
    - En DevTools desktop : le récap apparaît en pill grise inline sous le header ULD, lisible
    - En DevTools mobile (≤ 768px) : le récap occupe toute la largeur du bloc ULD, wrap naturel quand long, pas de scroll horizontal
  </done>
</task>

</tasks>

<verification>
Phase 1 plan 03 vérifications globales — gap closure RECAP-01 (à tester en local via `npx serve .` avant push) :

1. **RECAP-01 strict (critère #3 ROADMAP)** : créer une ULD avec sangles=3, forfait EU, commentaire "Fragile" → sous le header ULD, le texte `Sangles: 3 | Planchers EU: forfait | Com: Fragile` est VISIBLE sans avoir à rouvrir le modal.
2. **Forfait littéral** : cocher la checkbox forfait Std d'une ULD existante → le récap affiche `Planchers Std: forfait` (pas `0`, pas `true`).
3. **Zéros omis** : ULD avec uniquement bois calage = 1 → récap = `Bois calage: 1` (rien d'autre, pas de pipes parasites).
4. **MAT-11 XSS uldComment** : saisir `<img src=x onerror="alert(1)">` dans le commentaire → le récap affiche le texte littéralement échappé, AUCUNE alert ne se déclenche, AUCUNE balise img n'apparaît dans le DOM (vérifier via DevTools Inspector).
5. **Troncature commentaire** : saisir un commentaire de 60 caractères → le récap n'affiche que les 30 premiers + `…`, le bloc ULD n'a pas d'overflow horizontal.
6. **Mobile RECAP-03** : ouvrir DevTools en mode mobile (≤ 768px), créer une ULD avec plusieurs champs matériel → le récap wrap naturellement sur plusieurs lignes, pas de scroll horizontal du `.uld-block`.
7. **Rétro-compat loadManifest** : sauvegarder un manifeste, créer un nouveau, recharger l'ancien depuis la liste sauvegardée → le récap inline est restauré pour chaque ULD.
8. **D-13 préservé** : la barre `#liveRecap` (ULD/Colis/Poids/LTA/DGR) en haut de l'app reste strictement identique — ne contient AUCUNE info matériel.
9. **TEST-02** : ouvrir `tests/tests.html` en local → toutes les nouvelles suites `Materiel ULD - recap *` en vert, suite `Materiel ULD - XSS uldComment (MAT-11)` toujours verte (renforcée), suite `Materiel ULD - loadManifest restaure recap` verte. Compteur global `Passed: 360+, Failed: <= 1`.
10. **Pas de régression PDF/email** : le PDF généré ET l'email envoyé contiennent toujours leurs sections matériel (le rendu PDF/email ne dépend pas de `refreshMaterialBadge`, juste de `buildUldMaterialRows` / `buildMaterialSummary`). Suites `Materiel PDF - *` et `Materiel email HTML - *` toutes vertes.
</verification>

<success_criteria>
- `formatCondensedMaterial(block)` existe et retourne un string déjà échappé pour `uldComment`
- `refreshMaterialBadge` injecte un `<span class="material-recap">…</span>` ou un wrapper vide
- Format respecté : `<libellé FR>: <valeur>` séparé par ` | ` exactement, libellés FR exactement comme spécifiés
- Champs à 0 / forfait=false / commentaire vide sont OMIS (vérifié par tests omet zeros)
- `uldComment` tronqué à 30 caractères + `…` Unicode et passé par `esc()` (vérifié par tests troncature + XSS)
- Aucune chaîne `Matériel saisi` ne reste dans `static/js/app.js` (badge neutre éliminé)
- CSS `.material-recap` existe avec règle desktop + règle dans `@media (max-width: 768px)`
- Tests : ≥ 5 nouvelles suites `Materiel ULD - recap *` toutes vertes
- Tests pré-existants `Materiel ULD - badge Materiel saisi` (727) et `pas de badge si tout vide` (742) ADAPTÉS (renommés / réécrits — pas en rouge)
- Suites `Materiel ULD - XSS uldComment (MAT-11)`, `Materiel PDF - *`, `Materiel email HTML - *` restent toutes vertes (pas de régression)
- Critère #3 du ROADMAP désormais satisfait littéralement (vérifiable visuellement à l'écran)
</success_criteria>

<output>
After completion, create `.planning/phases/01-mat-riel-uld-r-tro-compat/01-03-SUMMARY.md` documenting :
- Décision : Option A retenue (D-14 ANNULÉ, badge neutre remplacé par récap inline condensé)
- Fonction publique ajoutée : `formatCondensedMaterial(block)` (string)
- Fonction publique réécrite : `refreshMaterialBadge(uldIndex)` (signature inchangée, comportement nouveau)
- Format final du récap : `Sangles: N | Planchers EU: forfait | ... | Com: <texte tronqué>`
- Libellés FR exacts retenus (Sangles, Planchers EU, Planchers Std, Bois calage, Bâches, Intercalaires, Nids d'abeille, Com)
- Troncature commentaire : 30 chars + `…` (U+2026)
- Classes CSS ajoutées : `.material-recap` (desktop + mobile)
- Suites de tests ajoutées (5 minimum) et suites adaptées (badge → recap inline)
- Note rétro-compat : `.material-badge` CSS conservé (pas de breaking change visuel pour future référence)
- Pointer vers 01-VERIFICATION.md pour traçabilité du gap fermé
- Si re-vérification effectuée : statut RECAP-01 passe de PARTIAL à SATISFIED
</output>
