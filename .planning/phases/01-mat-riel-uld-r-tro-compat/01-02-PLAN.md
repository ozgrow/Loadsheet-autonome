---
phase: 01-mat-riel-uld-r-tro-compat
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - static/js/app.js
  - tests/tests.html
autonomous: true
requirements:
  - RECAP-02
  - MAT-11

must_haves:
  truths:
    - "Le PDF généré contient, sur la page détail de chaque ULD, une section 'Matériel' listant les champs non-nuls"
    - "Les planchers bois affichent littéralement 'forfait' (pas un nombre) quand la checkbox forfait est cochée (D-16)"
    - "La page 1 récap du PDF contient une ligne ou bloc 'Totaux matériel' agrégeant les totaux par type (sangles, planchers EU + forfaits, planchers Std + forfaits, bois, bâches, intercalaires, nids d'abeille)"
    - "L'email HTML généré contient le miroir des sections matériel (par ULD + totaux) avec les mêmes règles d'affichage"
    - "Le commentaire uldComment dans l'email HTML est échappé via esc() (D-18, MAT-11)"
    - "Un manifeste ancien format sans matériel continue de générer PDF et email sans section matériel et sans erreur"
    - "Les sections matériel omettent les valeurs à 0 (préférence D-16) pour alléger l'affichage"
  artifacts:
    - path: "static/js/app.js"
      provides: "Fonction helper buildMaterialSummary(ulds), extension de buildPdf pour ajouter section Matériel par page ULD + totaux page 1, extension de sendEmail pour miroir HTML"
      contains: "function buildMaterialSummary"
  key_links:
    - from: "buildPdf (page ULD)"
      to: "data.ulds[i].strapsCount, flooringEuCount, flooringEuForfait, ..."
      via: "lecture des champs ULD + autoTable ou doc.text pour rendre une section Matériel"
      pattern: "flooringEuForfait|strapsCount"
    - from: "buildPdf (page 1)"
      to: "totaux agrégés matériel"
      via: "buildMaterialSummary(data.ulds) puis doc.text ou autoTable supplémentaire"
      pattern: "buildMaterialSummary"
    - from: "sendEmail (HTML)"
      to: "data.ulds[i] champs matériel"
      via: "injection dans html += ... avec esc() sur uldComment"
      pattern: "esc\\(.*uldComment"
---

<objective>
Livrer le rendu visuel des infos matériel dans le PDF (RECAP-02) et dans l'email HTML (miroir PDF, D-17/D-18). Le Plan 01 a établi la donnée (10 champs par ULD dans collectData output) ; ce plan l'affiche dans les deux canaux de sortie du manifeste.

Purpose: L'agent doit voir dans le PDF et l'email les infos matériel qu'il a saisies, avec affichage littéral "forfait" pour les planchers bois, afin que le destinataire du manifeste ait l'info complète.

Output: app.js étendu (buildMaterialSummary helper, buildPdf section matériel par ULD + totaux page 1, sendEmail miroir HTML), tests supplémentaires couvrant le rendu (présence des totaux, affichage "forfait", échappement XSS dans HTML email).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-01-SUMMARY.md
@CLAUDE.md
@static/js/app.js
@tests/tests.html

<interfaces>
<!-- Après le Plan 01, chaque objet ULD dans data.ulds[i] a la forme suivante (D-08) : -->
```javascript
{
  uldNumber: string,
  rows: [...],
  totalColis: number,
  weight?: number,
  strapsCount: number,          // MAT-01
  flooringEuCount: number,      // MAT-02 nombre
  flooringEuForfait: boolean,   // MAT-02 forfait
  flooringStdCount: number,     // MAT-03 nombre
  flooringStdForfait: boolean,  // MAT-03 forfait
  blocksCount: number,          // MAT-04
  tarpsCount: number,           // MAT-05
  dividersCount: number,        // MAT-06
  honeycombCount: number,       // MAT-07
  uldComment: string            // MAT-08 (peut contenir XSS payload — doit passer par esc() en HTML)
}
```
<!-- Pour un manifeste ancien format (rétro-compat, D-10), ces champs peuvent être absents : tout code d'affichage doit utiliser defaults via `|| 0` / `|| ''`. -->

Signatures existantes à étendre :
```javascript
function buildPdf(data)                  // app.js:426-570 — génère le jsPDF, retourne doc
async function sendEmail()                // app.js:590-682 — construit html, appelle /api/send-email
function esc(str)                         // app.js:16-19 — MANDATORY pour uldComment en HTML email
```

Points d'insertion dans buildPdf :
- **Page 1 récap (totaux matériel)** : actuellement le bloc `doc.autoTable({...})` se termine ligne 518, suivi de `drawFooter(1)` ligne 519. Insérer le rendu totaux matériel entre les deux, en utilisant `doc.lastAutoTable.finalY` pour positionner sous la table existante.
- **Page ULD détail (section matériel)** : actuellement le `doc.autoTable({...})` de détail (ligne 552-565) est suivi de `drawFooter(idx + 2)`. Insérer la section matériel entre les deux, en utilisant `doc.lastAutoTable.finalY`.

Points d'insertion dans sendEmail :
- **Table récap totaux** : la table ligne 616-637 se termine par `html += '</table>';` ligne 637. Ajouter un bloc "Totaux matériel" après.
- **Détail par ULD** : le forEach ligne 640-652 termine chaque ULD par `html += '</table>';` ligne 651. Ajouter un bloc "Matériel" après cette fermeture pour chaque ULD.

Library: jspdf-autotable déjà dispo (`doc.autoTable`, `doc.lastAutoTable.finalY`), cohérent avec le reste du code.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper buildMaterialSummary + PDF section matériel par ULD + totaux page 1</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js (relire en entier, surtout buildPdf app.js:426-570 pour comprendre le flow jsPDF et le pattern autoTable — les colonnes, styles, positionnement y+=... doivent être cohérents)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (D-15 deux niveaux d'affichage PDF, D-16 "forfait" littéral + totaux ignorent forfaits dans l'agrégation numérique, specifics "omettre les zéros")
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-01-SUMMARY.md (noms exacts des clés, fonctions ajoutées par Plan 01)
    - tests/tests.html (suites matériel ajoutées par Plan 01, pour greffer les nouveaux tests après)
  </read_first>
  <behavior>
    - Test 1 (buildMaterialSummary helper): Appeler buildMaterialSummary sur un tableau de 2 ULD avec matériel (ULD1: straps=3, flooringEuForfait=true ; ULD2: straps=2, flooringEuCount=5) doit retourner `{ straps:5, flooringEu:{ count:5, forfaits:1 }, flooringStd:{ count:0, forfaits:0 }, blocks:0, tarps:0, dividers:0, honeycomb:0 }` (ou structure équivalente documentée).
    - Test 2 (buildMaterialSummary rétro-compat): Appeler sur un tableau d'ULD ancien format (sans champs matériel) doit retourner un objet avec tous les totaux à 0 et pas d'erreur.
    - Test 3 (buildPdf inclut totaux matériel page 1): Générer buildPdf pour un manifeste avec matériel, appeler `doc.getNumberOfPages()` >= 2, inspecter le PDF en extrayant le texte via `doc.output('datauristring')` ou, plus simple, vérifier que buildPdf ne crash pas et que le total des pages reste correct (1 récap + N ULD). Note: jsPDF ne permet pas facilement de grep dans le PDF, donc le test principal vérifie l'absence de crash + présence des appels. Pour le texte, tester via un spy sur `doc.text` — voir approche ci-dessous.
    - Test 4 (buildPdf section matériel par ULD omet zeros): Avec un ULD n'ayant que straps=4, le code de rendu ne doit pas afficher de ligne pour flooringEu, blocks, etc. (contrôlé via un spy sur doc.text ou via la structure de données passée à autoTable).
    - Test 5 (buildPdf avec ULD ancien format): buildPdf sur un manifeste sans champs matériel doit générer un PDF valide (pas de section matériel visible), aucune erreur.
    - Test 6 (forfait affiché littéralement): Avec flooringEuForfait=true, le rendu matériel doit contenir le mot "forfait" (pas un nombre) — testable via spy doc.text ou via la structure autoTable body.
  </behavior>
  <action>
    **1) Ajouter le helper buildMaterialSummary dans static/js/app.js**, placé juste avant `function buildPdf(data)` (app.js:426) :

    ```javascript
    // ============================================
    // MATERIAL SUMMARY (RECAP-02)
    // ============================================
    // Agrège les totaux matériel sur toutes les ULD.
    // - count = somme des nombres saisis (hors forfaits)
    // - forfaits = nombre d'ULD avec la checkbox forfait cochée
    // Lecture défensive : un ULD sans champs matériel (ancien format) est traité avec defaults.
    function buildMaterialSummary(ulds) {
        var s = { straps: 0, flooringEu: { count: 0, forfaits: 0 }, flooringStd: { count: 0, forfaits: 0 },
                  blocks: 0, tarps: 0, dividers: 0, honeycomb: 0 };
        (ulds || []).forEach(function(u) {
            s.straps += parseInt(u.strapsCount) || 0;
            if (u.flooringEuForfait === true) s.flooringEu.forfaits += 1;
            else s.flooringEu.count += parseInt(u.flooringEuCount) || 0;
            if (u.flooringStdForfait === true) s.flooringStd.forfaits += 1;
            else s.flooringStd.count += parseInt(u.flooringStdCount) || 0;
            s.blocks += parseInt(u.blocksCount) || 0;
            s.tarps += parseInt(u.tarpsCount) || 0;
            s.dividers += parseInt(u.dividersCount) || 0;
            s.honeycomb += parseInt(u.honeycombCount) || 0;
        });
        return s;
    }

    // Formate une entrée "Planchers EU" en texte lisible pour PDF/HTML.
    // "—" (tiret) si count===0 ET forfaits===0. Sinon affiche "12" ou "forfait" ou "12 + 2 forfaits".
    function formatFlooringDisplay(entry) {
        if (!entry || (entry.count === 0 && entry.forfaits === 0)) return '—';
        var parts = [];
        if (entry.count > 0) parts.push(String(entry.count));
        if (entry.forfaits > 0) parts.push(entry.forfaits + ' forfait' + (entry.forfaits > 1 ? 's' : ''));
        return parts.join(' + ');
    }

    // Formate la section matériel d'une ULD unique en paires [label, value] pour autoTable, en omettant les zéros (specifics).
    function buildUldMaterialRows(u) {
        var rows = [];
        var straps = parseInt(u.strapsCount) || 0;
        if (straps > 0) rows.push(['Sangles', String(straps)]);
        // Planchers EU
        if (u.flooringEuForfait === true) rows.push(['Planchers bois EU', 'forfait']);
        else { var fe = parseInt(u.flooringEuCount) || 0; if (fe > 0) rows.push(['Planchers bois EU', String(fe)]); }
        // Planchers Std
        if (u.flooringStdForfait === true) rows.push(['Planchers bois Standard', 'forfait']);
        else { var fs = parseInt(u.flooringStdCount) || 0; if (fs > 0) rows.push(['Planchers bois Standard', String(fs)]); }
        var blocks = parseInt(u.blocksCount) || 0; if (blocks > 0) rows.push(['Bois de calage', String(blocks)]);
        var tarps = parseInt(u.tarpsCount) || 0; if (tarps > 0) rows.push(['Bâches', String(tarps)]);
        var dividers = parseInt(u.dividersCount) || 0; if (dividers > 0) rows.push(['Intercalaires', String(dividers)]);
        var honeycomb = parseInt(u.honeycombCount) || 0; if (honeycomb > 0) rows.push(['Nids d\'abeille', String(honeycomb)]);
        var comment = String(u.uldComment || '');
        if (comment.length > 0) rows.push(['Commentaire ULD', comment]);
        return rows;
    }
    ```

    **2) Étendre buildPdf (app.js:426-570) à deux endroits** :

    **A. Page 1 récap — après le doc.autoTable principal (ligne 518) et AVANT `drawFooter(1)` (ligne 519)** :
    ```javascript
    // Totaux matériel (RECAP-02) — insérés sous la table récap si au moins un total non nul
    var matSummary = buildMaterialSummary(data.ulds);
    var hasAnyMat = matSummary.straps > 0 || matSummary.flooringEu.count > 0 || matSummary.flooringEu.forfaits > 0 ||
                    matSummary.flooringStd.count > 0 || matSummary.flooringStd.forfaits > 0 ||
                    matSummary.blocks > 0 || matSummary.tarps > 0 || matSummary.dividers > 0 || matSummary.honeycomb > 0;
    if (hasAnyMat) {
        var matY = doc.lastAutoTable.finalY + 8;
        doc.setFont('helvetica', 'bold'); doc.setFontSize(10); doc.setTextColor(26, 58, 92);
        doc.text('Totaux matériel', margin, matY);
        var matRows = [];
        if (matSummary.straps > 0) matRows.push(['Sangles', String(matSummary.straps)]);
        var feStr = formatFlooringDisplay(matSummary.flooringEu); if (feStr !== '—') matRows.push(['Planchers bois EU', feStr]);
        var fsStr = formatFlooringDisplay(matSummary.flooringStd); if (fsStr !== '—') matRows.push(['Planchers bois Standard', fsStr]);
        if (matSummary.blocks > 0) matRows.push(['Bois de calage', String(matSummary.blocks)]);
        if (matSummary.tarps > 0) matRows.push(['Bâches', String(matSummary.tarps)]);
        if (matSummary.dividers > 0) matRows.push(['Intercalaires', String(matSummary.dividers)]);
        if (matSummary.honeycomb > 0) matRows.push(['Nids d\'abeille', String(matSummary.honeycomb)]);
        doc.autoTable({
            startY: matY + 2,
            body: matRows,
            margin: { left: margin, right: margin },
            styles: { fontSize: 9, cellPadding: 3, lineColor: [200, 210, 220], lineWidth: 0.3 },
            columnStyles: { 0: { fontStyle: 'bold', fillColor: [240, 244, 248], cellWidth: 60 } }
        });
    }
    ```

    **B. Page ULD détail — après le doc.autoTable du détail (ligne 565) et AVANT `drawFooter(idx + 2)` (ligne 566)**, dans le forEach `data.ulds.forEach(function(u, idx) {...})` :
    ```javascript
    // Section Matériel pour cette ULD (RECAP-02)
    var uldMatRows = buildUldMaterialRows(u);
    if (uldMatRows.length > 0) {
        var uldMatY = doc.lastAutoTable.finalY + 6;
        doc.setFont('helvetica', 'bold'); doc.setFontSize(10); doc.setTextColor(26, 58, 92);
        doc.text('Matériel', margin, uldMatY);
        doc.autoTable({
            startY: uldMatY + 2,
            body: uldMatRows,
            margin: { left: margin, right: margin },
            styles: { fontSize: 9, cellPadding: 3, lineColor: [200, 210, 220], lineWidth: 0.3 },
            columnStyles: { 0: { fontStyle: 'bold', fillColor: [240, 244, 248], cellWidth: 50 } }
        });
    }
    ```

    Important : `doc.text` injecte du texte brut dans le PDF (pas vulnérable XSS mais doit rester propre). `uldComment` passe directement dans `buildUldMaterialRows` → `autoTable` → rendu texte. Pas besoin de `esc()` pour le PDF (jsPDF n'est pas HTML).

    **3) Tests dans tests/tests.html** — ajouter une suite `'Materiel PDF'` avec les tests du `<behavior>`. Pour les tests jsPDF, la stratégie est de spy sur `doc.text` et `doc.autoTable` avant d'appeler buildPdf :
    ```javascript
    suite('Materiel PDF - buildMaterialSummary');
    (function() {
        var s = buildMaterialSummary([
            { strapsCount: 3, flooringEuForfait: true },
            { strapsCount: 2, flooringEuCount: 5 }
        ]);
        assertEqual('Total sangles', s.straps, 5);
        assertEqual('FlooringEu count', s.flooringEu.count, 5);
        assertEqual('FlooringEu forfaits', s.flooringEu.forfaits, 1);
    })();

    suite('Materiel PDF - retro-compat sans champs');
    (function() {
        var s = buildMaterialSummary([{ uldNumber: 'U', rows: [], totalColis: 0 }]);
        assertEqual('Sangles defaut 0', s.straps, 0);
        assertEqual('FlooringEu count defaut 0', s.flooringEu.count, 0);
        assertEqual('FlooringEu forfaits defaut 0', s.flooringEu.forfaits, 0);
    })();

    suite('Materiel PDF - buildUldMaterialRows omet zeros');
    (function() {
        var rows = buildUldMaterialRows({ strapsCount: 4, flooringEuCount: 0, flooringStdForfait: false });
        var labels = rows.map(function(r) { return r[0]; });
        assert('Contient Sangles', labels.indexOf('Sangles') >= 0);
        assert('Omet Planchers bois EU (count=0 et pas forfait)', labels.indexOf('Planchers bois EU') < 0);
        assert('Omet Bois de calage (absent)', labels.indexOf('Bois de calage') < 0);
    })();

    suite('Materiel PDF - forfait affiche litteralement');
    (function() {
        var rows = buildUldMaterialRows({ flooringEuForfait: true, flooringStdForfait: false });
        var feRow = rows.find(function(r) { return r[0] === 'Planchers bois EU'; });
        assert('Planchers EU present avec forfait', !!feRow);
        assertEqual('Valeur = "forfait"', feRow[1], 'forfait');
    })();

    suite('Materiel PDF - buildPdf ne crash pas avec/sans matériel');
    (function() {
        var dataWith = { manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [{ uldNumber: 'U1', rows: [{ lta: 'L', dossier: 'D', colis: 1, dgr: 'N', comment: '' }],
                     totalColis: 1, strapsCount: 3, flooringEuForfait: true, flooringStdForfait: false,
                     flooringEuCount: 0, flooringStdCount: 0, blocksCount: 0, tarpsCount: 0, dividersCount: 0, honeycombCount: 0, uldComment: '' }] };
        var doc1 = null; var err1 = null;
        try { doc1 = buildPdf(dataWith); } catch(e) { err1 = e; }
        assert('buildPdf avec materiel ne crash pas', err1 === null);
        assert('doc1 genere', !!doc1);

        var dataLegacy = { manifestId: 'M2', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [{ uldNumber: 'U1', rows: [{ lta: 'L', dossier: 'D', colis: 1, dgr: 'N', comment: '' }], totalColis: 1 }] };
        var doc2 = null; var err2 = null;
        try { doc2 = buildPdf(dataLegacy); } catch(e) { err2 = e; }
        assert('buildPdf ancien format (sans materiel) ne crash pas', err2 === null);
        assert('doc2 genere', !!doc2);
    })();
    ```
  </action>
  <verify>
    <automated>grep -c "function buildMaterialSummary" static/js/app.js returns 1 AND grep -c "function buildUldMaterialRows" static/js/app.js returns 1 AND grep -c "Totaux matériel" static/js/app.js returns >= 1 AND grep -cE "suite\\(.Materiel PDF" tests/tests.html returns >= 4</automated>
  </verify>
  <done>
    - `grep -n "buildMaterialSummary" static/js/app.js` retourne au moins 2 lignes (définition + appel dans buildPdf)
    - `grep -n "'forfait'" static/js/app.js` apparaît dans buildUldMaterialRows
    - Ouvrir l'app en local, saisir un manifeste avec matériel + forfait EU, générer PDF → page 1 a une section "Totaux matériel", page ULD a une section "Matériel" avec ligne "Planchers bois EU: forfait"
    - Générer PDF sur un manifeste ancien format (sans matériel) → pas de section "Totaux matériel" ni "Matériel", PDF complet sans erreur
    - `tests/tests.html` en local → toutes suites Materiel PDF en vert
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Email HTML miroir — section matériel par ULD + totaux + XSS uldComment</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js (après Task 1 de ce plan — buildMaterialSummary et helpers doivent exister ; lire sendEmail app.js:590-682 dans l'état actuel)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (D-17 miroir PDF, D-18 esc() obligatoire, D-19 uldComment est le seul vecteur XSS)
    - CLAUDE.md section Sécurité (esc() partout, validation regex email, strip CRLF — NE PAS casser ces garanties)
  </read_first>
  <behavior>
    - Test 1 (buildMaterialSummaryHtml helper): Appeler un nouvel helper qui retourne le HTML des totaux matériel ou '' si rien à afficher. Avec matériel, retourne un string contenant les labels + valeurs.
    - Test 2 (buildUldMaterialHtml helper): Appeler sur un ULD avec matériel non-nul, retourne un HTML string contenant les entrées. Sur un ULD vierge, retourne ''.
    - Test 3 (buildUldMaterialHtml échappe uldComment MAT-11/D-18): uldComment contenant `<script>alert(1)</script>`, l'HTML retourné NE contient PAS la chaîne `<script>` littérale mais contient `&lt;script&gt;` (via esc()).
    - Test 4 (buildUldMaterialHtml omet zeros): ULD avec seulement strapsCount=2, le HTML ne mentionne pas "Planchers" ni "Bâches".
    - Test 5 (rétro-compat): buildUldMaterialHtml sur ULD ancien format (sans champs) retourne '' sans crash.
  </behavior>
  <action>
    Ne PAS modifier le flow réseau de sendEmail (fetch /api/send-email, jwt, validation). Ajouter uniquement le rendu HTML des sections matériel.

    **1) Ajouter 2 helpers dans static/js/app.js**, juste après `buildUldMaterialRows` (fin de Task 1 de ce plan) :

    ```javascript
    // HTML email — Totaux matériel (RECAP-02 mirror D-17)
    function buildMaterialSummaryHtml(ulds) {
        var s = buildMaterialSummary(ulds);
        var hasAny = s.straps > 0 || s.flooringEu.count > 0 || s.flooringEu.forfaits > 0 ||
                     s.flooringStd.count > 0 || s.flooringStd.forfaits > 0 ||
                     s.blocks > 0 || s.tarps > 0 || s.dividers > 0 || s.honeycomb > 0;
        if (!hasAny) return '';
        var rows = [];
        if (s.straps > 0) rows.push(['Sangles', String(s.straps)]);
        var feStr = formatFlooringDisplay(s.flooringEu); if (feStr !== '—') rows.push(['Planchers bois EU', feStr]);
        var fsStr = formatFlooringDisplay(s.flooringStd); if (fsStr !== '—') rows.push(['Planchers bois Standard', fsStr]);
        if (s.blocks > 0) rows.push(['Bois de calage', String(s.blocks)]);
        if (s.tarps > 0) rows.push(['Bâches', String(s.tarps)]);
        if (s.dividers > 0) rows.push(['Intercalaires', String(s.dividers)]);
        if (s.honeycomb > 0) rows.push(['Nids d\'abeille', String(s.honeycomb)]);
        var html = '<h4 style="color:#1a3a5c;margin-top:16px;">Totaux matériel</h4>';
        html += '<table style="border-collapse:collapse;font-size:13px;">';
        rows.forEach(function(r) {
            // Valeurs numériques/strings controlées côté code (pas user input), esc() par sécurité
            html += '<tr><td style="padding:4px 10px;background:#f0f4f8;font-weight:600;">' + esc(r[0]) + '</td>';
            html += '<td style="padding:4px 10px;">' + esc(r[1]) + '</td></tr>';
        });
        html += '</table>';
        return html;
    }

    // HTML email — Section matériel par ULD (RECAP-02 mirror D-17, MAT-11 esc uldComment D-18)
    function buildUldMaterialHtml(u) {
        var rows = buildUldMaterialRows(u); // réutilise le helper PDF
        if (rows.length === 0) return '';
        var html = '<h4 style="color:#1a3a5c;margin-top:10px;">Matériel</h4>';
        html += '<table style="border-collapse:collapse;font-size:13px;">';
        rows.forEach(function(r) {
            // D-18 : esc() sur r[0] (label statique, defensive) ET r[1] (peut contenir uldComment user-supplied)
            html += '<tr><td style="padding:4px 10px;background:#f0f4f8;font-weight:600;">' + esc(r[0]) + '</td>';
            html += '<td style="padding:4px 10px;">' + esc(r[1]) + '</td></tr>';
        });
        html += '</table>';
        return html;
    }
    ```

    **2) Modifier sendEmail (app.js:590-682)** à deux endroits :

    **A. Après la table récap (ligne 637 `html += '</table>';`)**, ajouter :
    ```javascript
    html += buildMaterialSummaryHtml(data.ulds);
    ```

    **B. Dans le forEach `data.ulds.forEach(function(u) {...})` (ligne 640-652), après le `html += '</table>';` final (ligne 651)**, ajouter :
    ```javascript
    html += buildUldMaterialHtml(u);
    ```

    Ne pas toucher au reste (validation, fetch, jwt, subject, pdfBase64).

    **3) Tests dans tests/tests.html** — ajouter une suite `'Materiel email HTML'` :
    ```javascript
    suite('Materiel email HTML - summary');
    (function() {
        var htmlEmpty = buildMaterialSummaryHtml([{ uldNumber: 'U', rows: [], totalColis: 0 }]);
        assertEqual('Pas de HTML si pas de materiel', htmlEmpty, '');

        var htmlWith = buildMaterialSummaryHtml([{ strapsCount: 3, flooringEuForfait: true }]);
        assert('HTML contient Sangles', htmlWith.indexOf('Sangles') >= 0);
        assert('HTML contient Totaux matériel', htmlWith.indexOf('Totaux mat') >= 0);
        assert('HTML contient forfait', htmlWith.indexOf('forfait') >= 0);
    })();

    suite('Materiel email HTML - ULD section');
    (function() {
        var htmlEmpty = buildUldMaterialHtml({ uldNumber: 'U', rows: [], totalColis: 0 });
        assertEqual('Pas de HTML pour ULD sans materiel', htmlEmpty, '');

        var htmlStraps = buildUldMaterialHtml({ strapsCount: 2 });
        assert('HTML contient Sangles', htmlStraps.indexOf('Sangles') >= 0);
        assert('HTML omet Planchers', htmlStraps.indexOf('Planchers') < 0);
        assert('HTML omet Bâches', htmlStraps.indexOf('Bâches') < 0);
    })();

    suite('Materiel email HTML - SECU XSS uldComment (MAT-11 D-18)');
    (function() {
        var xssPayload = '<script>window._xss++<\/script><img src=x onerror="window._xss++">';
        var html = buildUldMaterialHtml({ uldComment: xssPayload });
        assert('HTML ne contient pas <script> littéral', html.indexOf('<script>') < 0);
        assert('HTML ne contient pas <img src=x onerror littéral', html.indexOf('<img src=x onerror=') < 0);
        assert('HTML contient entité échappée &lt;script&gt;', html.indexOf('&lt;script&gt;') >= 0);

        // Injection réelle dans le DOM pour prouver qu'il n'y a pas d'exécution
        var before = window._xss;
        var testDiv = document.createElement('div');
        testDiv.innerHTML = html;
        document.body.appendChild(testDiv);
        assertEqual('window._xss inchangé après injection', window._xss, before);
        testDiv.remove();
    })();
    ```
  </action>
  <verify>
    <automated>grep -c "function buildMaterialSummaryHtml" static/js/app.js returns 1 AND grep -c "function buildUldMaterialHtml" static/js/app.js returns 1 AND grep -nE "buildMaterialSummaryHtml\\(data\\.ulds\\)|buildUldMaterialHtml\\(u\\)" static/js/app.js returns 2 matches (appels dans sendEmail) AND grep -cE "esc\\(r\\[1\\]\\)" static/js/app.js returns >= 2 AND grep -cE "suite\\(.Materiel email HTML" tests/tests.html returns >= 3</automated>
  </verify>
  <done>
    - Les 2 helpers existent et sont appelés dans sendEmail aux bons endroits
    - Les valeurs `r[1]` (pouvant contenir uldComment) passent bien par esc() dans le HTML email
    - En local (`npx serve .`), saisir un manifeste avec uldComment `<script>alert(1)</script>`, cliquer "Envoyer par email" — l'appel API part avec le payload, aucune alerte XSS ne se déclenche, et le serveur (en local sans SMTP config : renverra une erreur 500 attendue) reçoit bien le HTML échappé
    - `tests/tests.html` passe, incluant les 3 nouvelles suites "Materiel email HTML"
    - Aucune régression sur les tests SECU existants (XSS loadManifest, refreshSavedList, etc.)
  </done>
</task>

</tasks>

<verification>
Phase 1 plan 02 vérifications globales (à tester en local via `npx serve .` avant push) :

1. **PDF page 1 RECAP-02** : créer un manifeste avec 2 ULD ayant matériel mixte (ULD1 straps=3 + forfait EU, ULD2 straps=2 + flooringEu=5), générer PDF → page 1 a une section "Totaux matériel" avec "Sangles: 5", "Planchers bois EU: 5 + 1 forfait".
2. **PDF page détail ULD RECAP-02** : chaque page ULD montre une section "Matériel" listant uniquement les champs non-nuls pour cette ULD. Les forfaits s'affichent littéralement "forfait".
3. **PDF manifeste vide matériel** : un manifeste sans matériel génère un PDF identique à avant (pas de section matériel en plus).
4. **PDF ancien format** : ouvrir un manifeste ancien depuis la liste sauvegardée, générer PDF → fonctionne, pas de section matériel.
5. **Email HTML D-17/D-18** : cliquer "Envoyer par email" → le HTML envoyé contient les mêmes sections matériel que le PDF (section par ULD + totaux récap). Vérifier dans l'onglet Network du navigateur que le `htmlBody` contient les nouvelles sections.
6. **Email XSS MAT-11** : uldComment = `<img src=x onerror="window._xss++">` → dans le htmlBody envoyé, la chaîne est échappée (`&lt;img...`) pas littérale.
7. **Tests** : `tests/tests.html` passe avec toutes les suites (Plan 01 + Plan 02), summary all-pass.
</verification>

<success_criteria>
- buildMaterialSummary et buildUldMaterialRows existent et couvrent toutes les clés matériel
- buildPdf ajoute une section "Totaux matériel" sur page 1 (conditionnelle, omet zéros)
- buildPdf ajoute une section "Matériel" sur chaque page ULD détail (conditionnelle, omet zéros)
- "forfait" est affiché littéralement dans le PDF quand flooringXxxForfait=true
- sendEmail construit un HTML miroir du PDF avec les 2 niveaux (totaux + par ULD)
- uldComment est échappé via esc() dans le HTML email (testé avec payload XSS)
- Anciens manifestes génèrent PDF et email sans erreur et sans section matériel parasite
- Tous les tests passent (suites existantes + Plan 01 + Plan 02)
- Aucune modification des garanties anti-XSS et anti-CRLF existantes (code réseau sendEmail inchangé)
</success_criteria>

<output>
After completion, create `.planning/phases/01-mat-riel-uld-r-tro-compat/01-02-SUMMARY.md` documenting :
- Signature et structure retournée par buildMaterialSummary, buildUldMaterialRows, formatFlooringDisplay
- Points d'insertion exacts dans buildPdf (page 1 + page détail ULD)
- Points d'insertion exacts dans sendEmail (après table récap + dans forEach ULD)
- Format visuel choisi (autoTable 2 colonnes bold label + valeur)
- Liste exacte des tests ajoutés (complétant TEST-01 qui sera finalisé en Phase 3)
- Tout écart par rapport à CONTEXT.md D-15/D-16/D-17/D-18 (et justification)
</output>
