---
phase: 01-mat-riel-uld-r-tro-compat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/js/app.js
  - static/css/style.css
  - tests/tests.html
autonomous: true
requirements:
  - MAT-01
  - MAT-02
  - MAT-03
  - MAT-04
  - MAT-05
  - MAT-06
  - MAT-07
  - MAT-08
  - MAT-09
  - MAT-10
  - MAT-11
  - RECAP-01
  - RECAP-03
  - TEST-02

must_haves:
  truths:
    - "L'agent clique sur un bouton 'Matériel' dans le header de chaque ULD et un modal s'ouvre"
    - "Le modal contient 8 champs matériel (sangles, planchers EU avec checkbox forfait, planchers Std avec checkbox forfait, bois de calage, bâches, intercalaires, nids d'abeille, commentaire libre)"
    - "Quand la checkbox 'Forfait négocié' est cochée, l'input nombre correspondant devient disabled et grisé"
    - "Après fermeture du modal avec au moins un champ rempli, un badge 'Matériel saisi' apparaît sous le header de l'ULD"
    - "collectData() retourne pour chaque ULD les 10 nouveaux champs matériel en plus des champs existants"
    - "Un manifeste sauvegardé avec les nouveaux champs se recharge (loadManifest) en restaurant les valeurs matériel dans le modal"
    - "Un manifeste ancien format (sans aucun champ matériel) se charge sans erreur et avec valeurs par défaut (0/false/'')"
    - "Le commentaire libre contenant du HTML ou JS est échappé via esc() partout où il est affiché (badge, modal réaffiché, tests le prouvent)"
    - "Le modal est utilisable sur écran ≤ 768px (prend tout l'écran, champs empilés en 1 colonne)"
    - "Les tests tests/tests.html passent tous, incluant au moins 3 nouveaux tests matériel + rétro-compat (TEST-02)"
  artifacts:
    - path: "static/js/app.js"
      provides: "openMaterialModal, closeMaterialModal, applyMaterialToUld, collectUldMaterial, readMaterialFromUld, extended collectData, extended loadManifest"
      contains: "function openMaterialModal"
    - path: "static/css/style.css"
      provides: "Classes .material-modal, .material-modal-overlay, .material-modal-content, .material-modal-grid, .material-badge, + media query mobile"
      contains: ".material-modal"
    - path: "tests/tests.html"
      provides: "Suite 'Materiel ULD' et 'Retro-compatibilite materiel' avec au moins 3 tests par suite"
      contains: "Materiel ULD"
  key_links:
    - from: "bouton .btn-material dans .uld-header"
      to: "openMaterialModal(uldIndex)"
      via: "onclick inline handler"
      pattern: "onclick=\"openMaterialModal\\("
    - from: "openMaterialModal"
      to: ".uld-block dataset (data-straps, data-flooring-eu, etc.)"
      via: "lecture/écriture data-attributes"
      pattern: "dataset\\.straps|setAttribute\\('data-straps'"
    - from: "collectData()"
      to: "objet ULD dans data.ulds[i]"
      via: "lecture des data-attributes ou éléments DOM du bloc ULD"
      pattern: "strapsCount|flooringEuCount"
    - from: "loadManifest()"
      to: ".uld-block dataset"
      via: "écriture défensive avec valeurs par défaut"
      pattern: "uldData\\.strapsCount \\|\\| 0|uldData\\.uldComment \\|\\| ''"
    - from: "badge .material-badge"
      to: "commentaire uldComment affiché"
      via: "innerHTML via esc()"
      pattern: "esc\\(.*uldComment"
---

<objective>
Livrer la saisie des 8 champs matériel ULD (MAT-01..08) sur tous types d'ULD (MAT-09) via un modal dialog construit from scratch, avec indicateur "renseigné" sur chaque bloc ULD (RECAP-01), rétro-compatibilité totale des manifestes localStorage existants (MAT-10), protection XSS du commentaire libre (MAT-11), responsivité mobile ≤ 768px (RECAP-03), et tests anti-régression couvrant le chargement d'anciens manifestes (TEST-02).

Purpose: Sans ce plan, les champs matériel n'existent nulle part dans le code. C'est la fondation data + UI sur laquelle le Plan 02 (rendu PDF/email) s'appuiera.

Output: app.js étendu (modal, collectData, loadManifest, addUld), style.css étendu (modal + media query), tests.html étendu (suite matériel + suite rétro-compat).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md
@CLAUDE.md
@static/js/app.js
@static/css/style.css
@index.html
@tests/tests.html

<interfaces>
<!-- Signatures et patterns à respecter pour interagir avec le code existant. -->

Depuis static/js/app.js (à étendre, pas à refactorer) :
```javascript
function esc(str)                                        // app.js:16-19 — MANDATORY pour tout innerHTML avec donnée user
function addUld()                                        // app.js:137-159 — construit le HTML du bloc ULD (header + table + totals)
function loadManifest(id)                                // app.js:316-372 — async, reconstruit le HTML du bloc ULD (duplication avec addUld)
function collectData()                                   // app.js:233-264 — itère .uld-block, construit data.ulds[]
function updateRecap()                                   // app.js:193-215 — barre #liveRecap, reste inchangée (D-13)

var STORAGE_KEY = "loadsheet_manifests"                  // pour les tests
async function getSavedManifests()                       // app.js:279-291
async function writeSavedManifests(manifests)            // app.js:293-296
```

Structure d'un objet ULD actuel (dans data.ulds[i]) :
```javascript
{
  uldNumber: string,
  rows: [{ lta, dossier, colis, dgr, comment }],
  totalColis: number,
  weight?: number      // optionnel, absent si pas saisi
}
```

Structure cible après ce plan (D-08) — ajouter 10 clés à plat au même niveau :
```javascript
{
  uldNumber, rows, totalColis, weight?,    // existants
  strapsCount: number,                      // MAT-01 (0 si non saisi)
  flooringEuCount: number,                  // MAT-02 nombre (0 si forfait ou non saisi)
  flooringEuForfait: boolean,               // MAT-02 forfait (false par défaut)
  flooringStdCount: number,                 // MAT-03 nombre
  flooringStdForfait: boolean,              // MAT-03 forfait
  blocksCount: number,                      // MAT-04 bois de calage
  tarpsCount: number,                       // MAT-05 bâches
  dividersCount: number,                    // MAT-06 intercalaires
  honeycombCount: number,                   // MAT-07 nids d'abeille
  uldComment: string                        // MAT-08 commentaire libre (peut contenir XSS payload)
}
```

Classes CSS existantes réutilisables (style.css) :
```css
.btn, .btn-sm, .btn-primary, .btn-secondary, .btn-danger
.uld-block, .uld-header, .uld-totals
@media (max-width: 768px)  /* seul breakpoint du projet */
```

Pattern de test (tests/tests.html) :
```javascript
suite('Nom de la suite');
(function() { assert('desc', condition); })();                     // sync
await (async function() { assertEqual('desc', actual, expected); })();  // async
resetDOM();                                                        // pour isoler
await deriveAndStoreKey('test-password-for-unit-tests');           // déjà fait en amont
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend data model (collectData, loadManifest, addUld) with 10 material fields + storage in DOM via data-attributes</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js (lire lignes 137-159 addUld, 233-264 collectData, 316-372 loadManifest — ce sont les 3 sources de vérité à étendre)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (D-08 schéma JSON exact, D-09 pas de version, D-10 lecture défensive, D-11 réécriture au save OK)
    - tests/tests.html (lire le bloc "Retro-compatibilite ancien format (pmcs)" ligne 411-440 — modèle pour les nouveaux tests)
    - CLAUDE.md (sections Conventions + Sécurité + Tests)
  </read_first>
  <behavior>
    - Test 1 (collectData nouveaux champs): Créer un ULD avec data-attributes matériel sur .uld-block, appeler collectData(), vérifier que data.ulds[0] contient les 10 clés avec les bonnes valeurs typées (strapsCount=5 number, flooringEuForfait=true boolean, uldComment="Fragile" string, etc.).
    - Test 2 (collectData valeurs par défaut): Créer un ULD via addUld() sans saisir matériel, collectData() doit retourner strapsCount:0, flooringEuCount:0, flooringEuForfait:false, ..., uldComment:''.
    - Test 3 (loadManifest rétro-compat MAT-10/TEST-02): localStorage contient un manifeste SANS aucun champ matériel (ancien format), loadManifest() ne doit pas crasher et le bloc ULD chargé doit avoir data-attributes matériel avec valeurs par défaut (0/false/'').
    - Test 4 (loadManifest nouveau format): Sauvegarder un manifeste avec matériel, le recharger, vérifier que les data-attributes sont restaurés avec les bonnes valeurs.
    - Test 5 (save→load round-trip ancien manifeste TEST-02): Charger un ancien manifeste (sans matériel), le re-saveManifest(), vérifier que les champs matériel apparaissent avec valeurs par défaut dans le JSON persisté (acceptable selon D-11).
  </behavior>
  <action>
    Étendre trois fonctions dans static/js/app.js **sans rien casser de l'existant** :

    **1) addUld() (app.js:137-159)** — Ajouter les 10 data-attributes vides dans `div.setAttribute` APRÈS `div.id = 'uld-' + i;` (ne pas inliner dans innerHTML, plus propre) :
    ```javascript
    div.setAttribute('data-straps', '0');
    div.setAttribute('data-flooring-eu', '0');
    div.setAttribute('data-flooring-eu-forfait', 'false');
    div.setAttribute('data-flooring-std', '0');
    div.setAttribute('data-flooring-std-forfait', 'false');
    div.setAttribute('data-blocks', '0');
    div.setAttribute('data-tarps', '0');
    div.setAttribute('data-dividers', '0');
    div.setAttribute('data-honeycomb', '0');
    div.setAttribute('data-uld-comment', '');
    ```
    Dans l'innerHTML du header (.uld-header), ajouter AVANT le bouton "Supprimer ULD" :
    ```
    '<button class="btn btn-secondary btn-sm btn-material" onclick="openMaterialModal(' + i + ')">Matériel</button>' +
    ```
    (openMaterialModal sera défini en Task 2 — déclarer `function openMaterialModal(){}` vide en tête de app.js pour l'instant si Task 1 committe avant Task 2.)

    Et AJOUTER après le `</div>` de uld-header et avant le `<table>`, un conteneur d'indicateur vide :
    ```
    '<div class="material-badge-wrapper" id="material-badge-' + i + '"></div>' +
    ```

    **2) loadManifest() (app.js:316-372)** — Dans le forEach `uldList.forEach(function(uldData) {...})`, après création de `div` et AVANT `document.getElementById('pmcContainer').appendChild(div)`, ajouter la **lecture défensive** (D-10) :
    ```javascript
    div.setAttribute('data-straps', String(parseInt(uldData.strapsCount) || 0));
    div.setAttribute('data-flooring-eu', String(parseInt(uldData.flooringEuCount) || 0));
    div.setAttribute('data-flooring-eu-forfait', String(uldData.flooringEuForfait === true));
    div.setAttribute('data-flooring-std', String(parseInt(uldData.flooringStdCount) || 0));
    div.setAttribute('data-flooring-std-forfait', String(uldData.flooringStdForfait === true));
    div.setAttribute('data-blocks', String(parseInt(uldData.blocksCount) || 0));
    div.setAttribute('data-tarps', String(parseInt(uldData.tarpsCount) || 0));
    div.setAttribute('data-dividers', String(parseInt(uldData.dividersCount) || 0));
    div.setAttribute('data-honeycomb', String(parseInt(uldData.honeycombCount) || 0));
    div.setAttribute('data-uld-comment', String(uldData.uldComment || ''));
    ```
    Ajouter dans l'innerHTML du header (miroir exact de addUld) le bouton "Matériel" et le conteneur badge `material-badge-wrapper`. Après `updateTotals(i)`, appeler `refreshMaterialBadge(i)` (fonction définie en Task 2, stub vide OK ici).

    **3) collectData() (app.js:233-264)** — Dans le forEach `.uld-block`, lire les data-attributes et les ajouter à `uldEntry` :
    ```javascript
    uldEntry.strapsCount = parseInt(block.dataset.straps) || 0;
    uldEntry.flooringEuCount = parseInt(block.dataset.flooringEu) || 0;
    uldEntry.flooringEuForfait = block.dataset.flooringEuForfait === 'true';
    uldEntry.flooringStdCount = parseInt(block.dataset.flooringStd) || 0;
    uldEntry.flooringStdForfait = block.dataset.flooringStdForfait === 'true';
    uldEntry.blocksCount = parseInt(block.dataset.blocks) || 0;
    uldEntry.tarpsCount = parseInt(block.dataset.tarps) || 0;
    uldEntry.dividersCount = parseInt(block.dataset.dividers) || 0;
    uldEntry.honeycombCount = parseInt(block.dataset.honeycomb) || 0;
    uldEntry.uldComment = block.dataset.uldComment || '';
    ```

    **Note forfait (D-07)** : la règle "si forfait=true alors count ignoré à la save" est appliquée en Task 2 dans applyMaterialToUld (qui écrit les data-attributes depuis le modal). Ici, collectData lit simplement ce que le DOM contient. Si forfait=true et count=0, c'est cohérent.

    **Ajouter les tests dans tests/tests.html** (après la suite "Retro-compatibilite ancien format (pmcs)" ligne 440) — au moins les 5 tests du bloc `<behavior>`. Utiliser le pattern `suite('Materiel ULD - collectData')`, `suite('Materiel ULD - loadManifest retro-compat')`, etc. Les tests de save→load doivent être `await (async function() { ... })()`.

    Ne PAS toucher à updateRecap (la barre liveRecap reste inchangée, D-13).
  </action>
  <verify>
    <automated>grep -n "data-straps" static/js/app.js | wc -l returns >= 3 (addUld, loadManifest, collectData) AND grep -c "strapsCount" static/js/app.js returns >= 2 AND grep -c "test_materialRetroCompat\\|Materiel ULD" tests/tests.html returns >= 2</automated>
  </verify>
  <done>
    - `grep -n "function addUld" static/js/app.js` montre la fonction contenant `data-straps` et `btn-material`
    - `grep -n "strapsCount" static/js/app.js` apparaît dans collectData et loadManifest au minimum
    - `grep -c "suite(.Materiel" tests/tests.html` retourne >= 2 (suites matériel + rétro-compat)
    - Ouvrir `tests/tests.html` en local (via `npx serve .` + login) affiche tous les tests en vert (`summary all-pass`)
    - Un manifeste ancien format dans localStorage se recharge sans erreur console
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build modal dialog UI + badge indicateur + CSS responsive</name>
  <files>static/js/app.js, static/css/style.css, tests/tests.html</files>
  <read_first>
    - static/js/app.js (état après Task 1 — addUld doit contenir btn-material et data-attributes)
    - static/css/style.css (lire en entier, notamment @media max-width:768px pour respecter le pattern de stacking mobile)
    - index.html (structure SPA, le modal doit être injecté dynamiquement, pas de modification structurelle requise)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md (D-01 modal from scratch, D-02 badge, D-03 commentaire niveau ULD distinct du .comment-input ligne LTA, D-04 full-screen mobile, D-05 grid 2 colonnes desktop, D-06/D-07 forfait UX, D-19/D-20 XSS)
    - CLAUDE.md section Sécurité (esc obligatoire)
  </read_first>
  <behavior>
    - Test 1 (openMaterialModal): Appeler openMaterialModal(1) pour une ULD existante, vérifier que `document.querySelector('.material-modal')` existe et contient 10 inputs/checkbox (strapsInput, flooringEuInput, flooringEuForfaitCheckbox, flooringStdInput, flooringStdForfaitCheckbox, blocksInput, tarpsInput, dividersInput, honeycombInput, uldCommentInput).
    - Test 2 (forfait desactive input D-06): Cocher la checkbox forfait EU dans le modal, vérifier que flooringEuInput a l'attribut disabled.
    - Test 3 (applyMaterialToUld écrit data-attributes): Remplir le modal avec straps=5 et uldComment="Fragile", valider, vérifier que le bloc ULD a data-straps="5" et data-uld-comment="Fragile".
    - Test 4 (forfait effacer le nombre D-07): Saisir flooringEuCount=3, puis cocher forfait, valider, vérifier que data-flooring-eu="0" et data-flooring-eu-forfait="true".
    - Test 5 (badge "Matériel saisi"): Après applyMaterialToUld avec au moins 1 champ non-nul, le conteneur #material-badge-{i} contient un élément `.material-badge` avec texte "Matériel saisi".
    - Test 6 (pas de badge si tout est à 0/false/''): applyMaterialToUld avec tous champs à vides/0/false, le conteneur badge est vide.
    - Test 7 (XSS uldComment MAT-11): uldComment = `<img src=x onerror="window._xss++">`, applyMaterialToUld puis rouvrir le modal via openMaterialModal, vérifier qu'aucune balise `<img>` n'est dans le DOM du modal ET que window._xss reste à 0. Le badge éventuel ne doit pas injecter le commentaire brut.
    - Test 8 (modal se ferme via closeMaterialModal): Appeler closeMaterialModal(), vérifier que .material-modal n'est plus dans le DOM.
  </behavior>
  <action>
    **1) Écrire 4 fonctions dans static/js/app.js**, à placer après `removeUld` (app.js:182) et avant `updateTotals` :

    ```javascript
    // ============================================
    // MATERIAL MODAL (MAT-01..08)
    // ============================================
    function openMaterialModal(uldIndex) {
        var block = document.getElementById('uld-' + uldIndex);
        if (!block) return;
        closeMaterialModal(); // idempotent

        var straps = block.dataset.straps || '0';
        var flEu = block.dataset.flooringEu || '0';
        var flEuF = block.dataset.flooringEuForfait === 'true';
        var flStd = block.dataset.flooringStd || '0';
        var flStdF = block.dataset.flooringStdForfait === 'true';
        var blocks = block.dataset.blocks || '0';
        var tarps = block.dataset.tarps || '0';
        var dividers = block.dataset.dividers || '0';
        var honeycomb = block.dataset.honeycomb || '0';
        var uldComment = block.dataset.uldComment || '';

        var overlay = document.createElement('div');
        overlay.className = 'material-modal-overlay';
        overlay.id = 'materialModalOverlay';
        overlay.onclick = function(e) { if (e.target === overlay) closeMaterialModal(); };

        var modal = document.createElement('div');
        modal.className = 'material-modal';
        modal.setAttribute('data-uld-index', String(uldIndex));
        // IMPORTANT : valeurs numériques injectées via setAttribute OK (types sûrs),
        // uldComment injecté via .value = ... (pas innerHTML) pour anti-XSS D-19
        modal.innerHTML =
            '<div class="material-modal-header">' +
                '<h3>Matériel ULD</h3>' +
                '<button class="btn btn-danger btn-sm" onclick="closeMaterialModal()">Fermer</button>' +
            '</div>' +
            '<div class="material-modal-grid">' +
                '<label>Sangles<input type="number" min="0" class="mat-straps" value="' + esc(straps) + '"></label>' +
                '<label>Planchers bois EU<input type="number" min="0" class="mat-flooring-eu" value="' + esc(flEu) + '"' + (flEuF ? ' disabled' : '') + '>' +
                    '<span class="mat-forfait-label"><input type="checkbox" class="mat-flooring-eu-forfait"' + (flEuF ? ' checked' : '') + ' onchange="toggleForfait(this, \'mat-flooring-eu\')"> Forfait négocié</span></label>' +
                '<label>Planchers bois Standard<input type="number" min="0" class="mat-flooring-std" value="' + esc(flStd) + '"' + (flStdF ? ' disabled' : '') + '>' +
                    '<span class="mat-forfait-label"><input type="checkbox" class="mat-flooring-std-forfait"' + (flStdF ? ' checked' : '') + ' onchange="toggleForfait(this, \'mat-flooring-std\')"> Forfait négocié</span></label>' +
                '<label>Bois de calage<input type="number" min="0" class="mat-blocks" value="' + esc(blocks) + '"></label>' +
                '<label>Bâches<input type="number" min="0" class="mat-tarps" value="' + esc(tarps) + '"></label>' +
                '<label>Intercalaires<input type="number" min="0" class="mat-dividers" value="' + esc(dividers) + '"></label>' +
                '<label>Nids d\'abeille<input type="number" min="0" class="mat-honeycomb" value="' + esc(honeycomb) + '"></label>' +
                '<label class="mat-comment-label">Commentaire ULD<textarea class="mat-uld-comment" rows="3" placeholder="Commentaire libre niveau ULD"></textarea></label>' +
            '</div>' +
            '<div class="material-modal-actions">' +
                '<button class="btn btn-secondary" onclick="closeMaterialModal()">Annuler</button>' +
                '<button class="btn btn-primary" onclick="applyMaterialToUld(' + uldIndex + ')">Valider</button>' +
            '</div>';
        overlay.appendChild(modal);
        document.body.appendChild(overlay);
        // ANTI-XSS D-19 : uldComment injecté via .value (pas innerHTML) — textarea traite comme texte
        modal.querySelector('.mat-uld-comment').value = uldComment;
    }

    function toggleForfait(checkbox, inputClass) {
        var modal = document.querySelector('.material-modal');
        if (!modal) return;
        var input = modal.querySelector('.' + inputClass);
        if (!input) return;
        if (checkbox.checked) { input.disabled = true; input.value = '0'; }
        else { input.disabled = false; }
    }

    function closeMaterialModal() {
        var overlay = document.getElementById('materialModalOverlay');
        if (overlay) overlay.remove();
    }

    function applyMaterialToUld(uldIndex) {
        var modal = document.querySelector('.material-modal');
        var block = document.getElementById('uld-' + uldIndex);
        if (!modal || !block) return;

        var flEuF = modal.querySelector('.mat-flooring-eu-forfait').checked;
        var flStdF = modal.querySelector('.mat-flooring-std-forfait').checked;
        // D-07 : si forfait coché, ignorer le nombre (forcé à 0)
        var flEu = flEuF ? 0 : (parseInt(modal.querySelector('.mat-flooring-eu').value) || 0);
        var flStd = flStdF ? 0 : (parseInt(modal.querySelector('.mat-flooring-std').value) || 0);
        var straps = parseInt(modal.querySelector('.mat-straps').value) || 0;
        var blocks = parseInt(modal.querySelector('.mat-blocks').value) || 0;
        var tarps = parseInt(modal.querySelector('.mat-tarps').value) || 0;
        var dividers = parseInt(modal.querySelector('.mat-dividers').value) || 0;
        var honeycomb = parseInt(modal.querySelector('.mat-honeycomb').value) || 0;
        var uldComment = String(modal.querySelector('.mat-uld-comment').value || '');

        block.setAttribute('data-straps', String(straps));
        block.setAttribute('data-flooring-eu', String(flEu));
        block.setAttribute('data-flooring-eu-forfait', String(flEuF));
        block.setAttribute('data-flooring-std', String(flStd));
        block.setAttribute('data-flooring-std-forfait', String(flStdF));
        block.setAttribute('data-blocks', String(blocks));
        block.setAttribute('data-tarps', String(tarps));
        block.setAttribute('data-dividers', String(dividers));
        block.setAttribute('data-honeycomb', String(honeycomb));
        block.setAttribute('data-uld-comment', uldComment);

        refreshMaterialBadge(uldIndex);
        closeMaterialModal();
    }

    function refreshMaterialBadge(uldIndex) {
        var block = document.getElementById('uld-' + uldIndex);
        var wrapper = document.getElementById('material-badge-' + uldIndex);
        if (!block || !wrapper) return;
        var hasAny =
            (parseInt(block.dataset.straps) || 0) > 0 ||
            (parseInt(block.dataset.flooringEu) || 0) > 0 ||
            block.dataset.flooringEuForfait === 'true' ||
            (parseInt(block.dataset.flooringStd) || 0) > 0 ||
            block.dataset.flooringStdForfait === 'true' ||
            (parseInt(block.dataset.blocks) || 0) > 0 ||
            (parseInt(block.dataset.tarps) || 0) > 0 ||
            (parseInt(block.dataset.dividers) || 0) > 0 ||
            (parseInt(block.dataset.honeycomb) || 0) > 0 ||
            (block.dataset.uldComment || '').length > 0;
        // D-19/D-20 : on n'injecte PAS le commentaire dans le badge (décision deferred dans CONTEXT.md), badge neutre
        wrapper.innerHTML = hasAny ? '<span class="material-badge">Matériel saisi</span>' : '';
    }
    ```

    Après loadManifest dans le forEach, appeler `refreshMaterialBadge(i);` juste après `updateTotals(i);` pour afficher le badge au chargement.

    **2) Ajouter CSS dans static/css/style.css**, à la fin du fichier AVANT le bloc `@media (max-width: 768px)` existant :
    ```css
    /* ---- Material modal (MAT-01..08) ---- */
    .material-modal-overlay {
        position: fixed; inset: 0; background: rgba(0,0,0,0.5);
        display: flex; justify-content: center; align-items: center;
        z-index: 2000;
    }
    .material-modal {
        background: #fff; border-radius: 8px; padding: 20px 24px;
        width: 600px; max-width: 95vw; max-height: 90vh; overflow-y: auto;
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    }
    .material-modal-header {
        display: flex; justify-content: space-between; align-items: center;
        margin-bottom: 16px; border-bottom: 1px solid #e2e8f0; padding-bottom: 10px;
    }
    .material-modal-header h3 { margin: 0; color: #1a3a5c; }
    .material-modal-grid {
        display: grid; grid-template-columns: 1fr 1fr; gap: 12px 20px;
    }
    .material-modal-grid label {
        display: flex; flex-direction: column; gap: 4px; font-weight: 600; font-size: 0.9em; color: #4a5568;
    }
    .material-modal-grid input[type="number"] {
        padding: 6px 8px; border: 1px solid #cbd5e0; border-radius: 4px; font-size: 1em; width: 100%;
    }
    .material-modal-grid input:disabled { background: #edf2f7; color: #a0aec0; }
    .material-modal-grid textarea {
        padding: 6px 8px; border: 1px solid #cbd5e0; border-radius: 4px; font-size: 1em; width: 100%; font-family: inherit; resize: vertical;
    }
    .mat-forfait-label {
        font-size: 0.8em; font-weight: 400; color: #718096; display: flex; align-items: center; gap: 4px; margin-top: 2px;
    }
    .mat-comment-label { grid-column: 1 / -1; }
    .material-modal-actions {
        display: flex; justify-content: flex-end; gap: 10px; margin-top: 16px;
    }

    /* ---- Material badge (RECAP-01) ---- */
    .material-badge-wrapper { margin-top: 4px; margin-bottom: 4px; }
    .material-badge {
        display: inline-block; font-size: 0.75em; color: #718096;
        font-style: italic; background: #edf2f7; padding: 2px 8px; border-radius: 10px;
    }

    /* ---- btn-material inside uld-header ---- */
    .uld-header .btn-material { background: #4a5568; color: #fff; }
    .uld-header .btn-material:hover { background: #2d3748; }
    ```

    Puis, dans le bloc `@media (max-width: 768px)` existant (après la ligne `.saved-item-actions { justify-content: flex-end; margin-top: 4px; }`), AJOUTER :
    ```css
    /* Material modal full-screen mobile (D-04 RECAP-03) */
    .material-modal-overlay { align-items: stretch; }
    .material-modal {
        width: 100%; max-width: 100%; max-height: 100vh; height: 100vh;
        border-radius: 0; padding: 14px 12px;
    }
    .material-modal-grid { grid-template-columns: 1fr; gap: 10px; }
    ```

    **3) Ajouter tests dans tests/tests.html** — les 8 tests du `<behavior>`, dans une suite `'Materiel ULD - modal'`. Test XSS (#7) clé : vérifier `document.querySelectorAll('.material-modal img').length === 0` et `window._xss === 0`.
  </action>
  <verify>
    <automated>grep -c "function openMaterialModal" static/js/app.js returns 1 AND grep -c "function applyMaterialToUld" static/js/app.js returns 1 AND grep -c ".material-modal" static/css/style.css returns >= 3 AND grep -c "@media (max-width: 768px)" static/css/style.css returns >= 1 AND grep -cE "suite\\(.Materiel ULD - modal" tests/tests.html returns >= 1</automated>
  </verify>
  <done>
    - Modal s'ouvre en cliquant "Matériel" sur n'importe quelle ULD (desktop + mobile <=768px en DevTools)
    - Cocher "Forfait négocié" désactive le champ nombre et le grise
    - Valider le modal met à jour les data-attributes du bloc ULD
    - Badge "Matériel saisi" apparaît sous le header ULD quand au moins un champ est rempli
    - Commentaire avec `<img src=x onerror="alert(1)">` ne déclenche pas d'alert et n'injecte pas de balise img
    - Sur écran simulé ≤ 768px, le modal occupe tout l'écran avec champs en 1 colonne
    - `tests/tests.html` (via `npx serve .`) affiche `all-pass` avec la suite "Materiel ULD - modal"
  </done>
</task>

</tasks>

<verification>
Phase 1 plan 01 vérifications globales (à tester en local via `npx serve .` avant push) :

1. **Rétro-compat MAT-10 / TEST-02** : ouvrir en local un manifeste déjà présent dans localStorage (avant la mise à jour) — il doit se charger sans erreur console, les data-attributes matériel initialisés aux valeurs par défaut.
2. **Saisie MAT-01..08 / MAT-09** : créer une nouvelle ULD (palette OU conteneur — le type ne change rien), cliquer "Matériel", remplir les 8 champs, valider. Rouvrir le modal, les valeurs sont persistées en mémoire DOM.
3. **Forfait exclusif D-07** : cocher forfait EU → champ nombre grisé et valeur 0 à la save. Décocher → champ réactivé.
4. **Badge RECAP-01** : le badge "Matériel saisi" apparaît sur les ULD avec matériel, pas sur les ULD vierges.
5. **Save/load round-trip** : saveManifest, newManifest, re-loadManifest depuis la liste sauvegardée → matériel restauré dans les data-attributes + badge visible.
6. **XSS MAT-11** : saisir `<script>alert(1)</script>` dans le commentaire, valider, rouvrir le modal — la textarea affiche le texte brut, aucune alerte, aucune balise script injectée.
7. **Mobile RECAP-03** : réduire la fenêtre à ≤ 768px → modal full-screen, champs en 1 colonne, bouton Fermer accessible en haut.
8. **Tests TEST-02** : ouvrir `tests/tests.html` en local → summary all-pass (nouveaux tests matériel + rétro-compat inclus).
</verification>

<success_criteria>
- collectData() retourne les 10 nouveaux champs pour chaque ULD (testé)
- loadManifest() charge anciens ET nouveaux manifestes sans erreur (testé)
- Modal matériel fonctionnel sur desktop et mobile (testé visuellement + tests DOM)
- Badge "Matériel saisi" affiché conditionnellement (testé)
- Forfait desactive/efface le nombre (testé)
- uldComment échappé partout où il est affiché, window._xss === 0 (testé)
- Tous les tests de tests/tests.html passent (test count >= tests existants + 8)
- Aucune régression sur les tests existants (save/load/collectData/etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-mat-riel-uld-r-tro-compat/01-01-SUMMARY.md` documenting :
- Structure DOM choisie (data-attributes sur .uld-block)
- Noms exacts des 10 clés JSON (strapsCount, flooringEuCount, ..., uldComment)
- Fonctions publiques ajoutées (openMaterialModal, closeMaterialModal, applyMaterialToUld, refreshMaterialBadge, toggleForfait)
- Classes CSS ajoutées (.material-modal-*, .material-badge, .btn-material)
- Liste exacte des tests ajoutés (utilisable par Plan 02 et Phase 3 pour TEST-01)
- Décisions d'implémentation libres (layout modal desktop, libellés champs, ordre d'affichage)
</output>
