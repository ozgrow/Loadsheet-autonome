---
phase: 02-type-uld-vrac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/js/app.js
  - static/css/style.css
  - tests/tests.html
autonomous: true
requirements:
  - VRAC-01
  - VRAC-02

# CONTEXT OVERRIDE (decisions prises lors du planning Phase 2) :
# - D-09 Phase 1 (matériel uniforme pour tous types d'ULD) : OVERRIDE PARTIEL via D-18 Phase 2.
#   Les 2 champs planchers bois (EU + Std, avec leurs checkbox forfait) sont MASQUES dans le modal
#   quand type=VRAC. Les data-attributes correspondants sont PRESERVES (D-19), pas de reset destructif.
# - D-09 Phase 2 (lecture nuancée du critère ROADMAP) : le compteur "ULD" reste conservé,
#   AUCUN relabel en "Palettes". L'annotation "dont Vrac : N (X colis, Y kg)" apparaît quand ≥1 ULD VRAC.
#   Ceci doit être documenté explicitement dans 02-VERIFICATION.md pour éviter un gap type RECAP-01
#   (leçon tirée de 01-VERIFICATION.md : le ROADMAP doit être lu littéralement, les nuances explicitées).

must_haves:
  truths:
    - "Chaque bloc ULD (addUld ET loadManifest) affiche un <select class=\"uld-type\"> contenant exactement 5 options dans cet ordre : PMC, AKE, AKN, PAG, VRAC"
    - "Le <select> apparaît AVANT le champ N° ULD dans le .uld-header (ordre visuel : [Type ▼] [N° ULD] [Poids] [Matériel] [Supprimer])"
    - "Le type par défaut d'une ULD nouvellement créée via addUld() est 'PMC' (D-03)"
    - "loadManifest() applique la lecture défensive : un manifeste ancien sans clé 'type' charge chaque ULD avec type='PMC' (D-15)"
    - "collectData() inclut la clé 'type' (string parmi PMC/AKE/AKN/PAG/VRAC) dans chaque objet ulds[i] (D-05)"
    - "Le DOM a un data-attribute 'data-uld-type' sur .uld-block, mais la source de vérité live pour collectData est la valeur du <select> (D-06)"
    - "Un changement du <select> déclenche : updateRecap() + refreshMaterialBadge(i) + (si modal ouvert pour cette ULD) re-render du modal pour toggle planchers (D-21)"
    - "#liveRecap affiche l'annotation 'dont Vrac : N (X colis, Y kg)' CONDITIONNELLEMENT : seulement si au moins une ULD VRAC est présente (D-07, D-08)"
    - "Format exact avec VRAC : 'ULD : 4 dont Vrac : 1 (12 colis, 240 kg) | Colis : 50 | Poids : 800 kg | LTA : ... | DGR' (D-08)"
    - "Format sans VRAC : inchangé par rapport à l'existant (backward compatible visuellement)"
    - "Les totaux globaux Colis et Poids du #liveRecap INCLUENT les ULD VRAC (D-10 : pas de soustraction)"
    - "Le modal openMaterialModal masque les 2 champs Planchers EU + Planchers Std (et leurs checkbox forfait) quand le type de l'ULD est VRAC (D-18)"
    - "Les data-attributes data-flooring-eu, data-flooring-eu-forfait, data-flooring-std, data-flooring-std-forfait sont PRESERVES quand type bascule vers VRAC (D-19 : pas de reset destructif)"
    - "formatCondensedMaterial (récap inline) EXCLUT les entrées Planchers EU/Std quand type=VRAC (D-21)"
    - "esc() est appliqué au type dans les insertions HTML pour défense en profondeur (même si contraint par <select>)"
    - "tests/tests.html contient au moins 8 nouvelles suites 'Type ULD - xxx' couvrant : default PMC, rétro-compat absence, collectData, updateRecap annotation, modal masque planchers, modal affiche planchers si non-VRAC, formatCondensedMaterial exclut planchers VRAC, persistance flooring data-attributes quand toggle VRAC"
    - "Le <select class=\"uld-type\"> reste utilisable sur mobile ≤ 768px (largeur auto, touchabilité via padding hérité du .uld-header input)"

  artifacts:
    - path: "static/js/app.js"
      provides: "Constante ULD_TYPES, addUld() étendu, loadManifest() étendu, collectData() étendu, updateRecap() étendu, openMaterialModal() étendu (toggle planchers), formatCondensedMaterial() étendu (exclusion planchers VRAC), handler changeUldType(selectEl, uldIndex)"
      contains: "var ULD_TYPES"
    - path: "static/css/style.css"
      provides: "Classe .uld-type (largeur + ordre visuel) + règle mobile ≤ 768px (largeur 100% sur le select en mode colonne)"
      contains: ".uld-type"
    - path: "tests/tests.html"
      provides: "Suite d'au moins 8 nouveaux tests 'Type ULD - ...' couvrant toutes les décisions D-01 à D-10, D-15 à D-21 de Phase 2"
      contains: "Type ULD - addUld"

  key_links:
    - from: "addUld() (app.js)"
      to: ".uld-header contient <select class='uld-type'> avec 5 options PMC/AKE/AKN/PAG/VRAC, PMC sélectionné, event onchange='changeUldType(this, i)'"
      via: "innerHTML injecté AVANT <input class='uld-number'>"
      pattern: "class=\"uld-type\"|class='uld-type'"
    - from: "loadManifest() (app.js)"
      to: "Même <select class='uld-type'> avec option sélectionnée = uldData.type || 'PMC' (rétro-compat D-15)"
      via: "innerHTML dynamique + setAttribute selected"
      pattern: "uldData\\.type \\|\\| 'PMC'|uldData\\.type\\|\\|.PMC"
    - from: "collectData() (app.js)"
      to: "uldEntry.type = block.querySelector('.uld-type').value (source de vérité live, D-06)"
      via: "lecture directe du select dans le forEach .uld-block"
      pattern: "uldEntry\\.type|\\.type = .*\\.uld-type"
    - from: "updateRecap() (app.js)"
      to: "#liveRecap contient 'dont Vrac : N (X colis, Y kg)' quand hasVrac, sinon rien"
      via: "itération blocks + compteur VRAC + concat conditionnelle dans l'innerHTML"
      pattern: "dont Vrac|vracCount|hasVrac"
    - from: "openMaterialModal() (app.js)"
      to: "Les 2 labels planchers bois (EU + Std) sont masqués si block.dataset.uldType === 'VRAC' (D-18)"
      via: "lecture du type + class conditionnelle .mat-flooring-hidden ou display:none inline"
      pattern: "mat-flooring-hidden|data-uld-type.*VRAC|VRAC.*flooring"
    - from: "formatCondensedMaterial() (app.js)"
      to: "Les entrées 'Planchers EU: ...' et 'Planchers Std: ...' sont OMISES quand block.dataset.uldType === 'VRAC' (D-21)"
      via: "check du type en début de fonction et skip des 2 branches planchers si VRAC"
      pattern: "VRAC.*Planchers|isVrac"
    - from: "changeUldType() (app.js, nouvelle fonction)"
      to: "block.dataset.uldType mis à jour + updateRecap() + refreshMaterialBadge(uldIndex) + si modal ouvert → re-render"
      via: "handler onchange du select"
      pattern: "function changeUldType"
---

<objective>
Livrer (a) le sélecteur de type ULD avec liste figée PMC/AKE/AKN/PAG/VRAC (VRAC-01), (b) l'exclusion des ULD VRAC du compteur palettes via annotation "dont Vrac" dans le #liveRecap (VRAC-02), (c) l'extension du modèle de données ULD avec la clé `type` en rétro-compatibilité totale (lecture défensive, D-15), et (d) l'override partiel de D-09 Phase 1 : masquage conditionnel des 2 champs planchers bois dans le modal quand type=VRAC (D-18), avec préservation défensive des data-attributes planchers (D-19) et exclusion des planchers du récap inline condensé pour les ULD VRAC (D-21).

Purpose: Sans ce plan, le type ULD n'existe pas dans le code et aucune ULD ne peut être marquée comme VRAC. C'est la fondation data + UI + récap écran sur laquelle Plan 02-02 (rendu PDF + email) s'appuiera. Le critère #1 et #2 du ROADMAP Phase 2 sont adressés ici ; le critère #3 l'est partiellement (la ligne "Vrac" existe déjà dans #liveRecap à ce stade — le rendu PDF/email vient dans Plan 02-02).

Output: `static/js/app.js` étendu (ULD_TYPES, addUld, loadManifest, collectData, updateRecap, openMaterialModal, formatCondensedMaterial, changeUldType), `static/css/style.css` étendu (.uld-type desktop + mobile), `tests/tests.html` étendu (≥ 8 suites `Type ULD - ...`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-type-uld-vrac/02-CONTEXT.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-CONTEXT.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-01-SUMMARY.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-03-SUMMARY.md
@CLAUDE.md
@static/js/app.js
@static/css/style.css
@tests/tests.html

<interfaces>
<!-- État actuel (après Phase 1 complète) — signatures à étendre, pas à refactorer. -->

Fonctions cibles à MODIFIER dans static/js/app.js :
```javascript
function addUld()                          // app.js:304-339 : construit le HTML .uld-block, data-attributes matériel
function loadManifest(id)                  // app.js:507-577 : async, reconstruit le HTML .uld-block
function collectData()                     // app.js:413-455 : itère .uld-block, construit data.ulds[i]
function updateRecap()                     // app.js:373-395 : rend le #liveRecap
function openMaterialModal(uldIndex)       // app.js:27-78 : construit le modal matériel
function formatCondensedMaterial(block)    // app.js:140-173 : string condensé injecté par refreshMaterialBadge
```

Structure actuelle d'un objet ULD dans data.ulds[i] (après Phase 1) :
```javascript
{
  uldNumber: string,
  rows: [{ lta, dossier, colis, dgr, comment }],
  totalColis: number,
  weight?: number,
  strapsCount, flooringEuCount, flooringEuForfait, flooringStdCount, flooringStdForfait,
  blocksCount, tarpsCount, dividersCount, honeycombCount, uldComment
}
```

Structure CIBLE après ce plan (ajout d'une clé à plat — D-05) :
```javascript
{
  uldNumber, rows, totalColis, weight?,
  strapsCount, flooringEuCount, flooringEuForfait, flooringStdCount, flooringStdForfait,
  blocksCount, tarpsCount, dividersCount, honeycombCount, uldComment,
  type: 'PMC' | 'AKE' | 'AKN' | 'PAG' | 'VRAC'  // NOUVEAU (D-05)
}
```

Pattern data-attributes établi en Phase 1 (à étendre) :
```html
<div class="uld-block" id="uld-N"
     data-straps="0"
     data-flooring-eu="0" data-flooring-eu-forfait="false"
     data-flooring-std="0" data-flooring-std-forfait="false"
     data-blocks="0" data-tarps="0" data-dividers="0" data-honeycomb="0"
     data-uld-comment=""
     data-uld-type="PMC">   <!-- NOUVEAU (D-06) -->
```

HTML actuel du .uld-header dans addUld() (app.js:321-329) :
```html
<div class="uld-header">
  <label>ULD N° :</label>
  <input type="text" class="uld-number" placeholder="Numéro ULD" style="width:180px">
  <label style="margin-left:16px;">Poids (kg) :</label>
  <input type="number" class="uld-weight" placeholder="Optionnel" style="width:100px" ...>
  <button class="btn btn-secondary btn-sm btn-material" onclick="openMaterialModal(N)">Matériel</button>
  <button class="btn btn-danger" onclick="removeUld(N)">Supprimer ULD</button>
</div>
```

HTML CIBLE avec <select> inséré AVANT <input class="uld-number"> (D-04) :
```html
<div class="uld-header">
  <label>Type :</label>
  <select class="uld-type" onchange="changeUldType(this, N)">
    <option value="PMC" selected>PMC</option>
    <option value="AKE">AKE</option>
    <option value="AKN">AKN</option>
    <option value="PAG">PAG</option>
    <option value="VRAC">VRAC</option>
  </select>
  <label>ULD N° :</label>
  <input type="text" class="uld-number" ...>
  ... (reste inchangé)
</div>
```

Fonction HELPER existante à ÉTENDRE (app.js:140-173) :
```javascript
function formatCondensedMaterial(block) { ... }
// Doit lire block.dataset.uldType en début, skipper les 2 branches planchers si VRAC (D-21).
```

Fonction HELPER existante à ÉTENDRE (app.js:27-78 openMaterialModal) :
```javascript
// Lire le type de l'ULD (block.dataset.uldType) en début
// Si type === 'VRAC' : injecter class="mat-flooring-hidden" sur les 2 <label> planchers (EU + Std)
// La classe CSS .mat-flooring-hidden { display: none; } cache visuellement sans détruire le DOM
```

Fonction CIBLE à CRÉER (nouvelle) :
```javascript
// Handler onchange du <select class="uld-type">
// - Met à jour block.dataset.uldType (source de vérité DOM, D-06)
// - Appelle updateRecap() pour recalculer l'annotation "dont Vrac"
// - Appelle refreshMaterialBadge(uldIndex) pour recalculer le récap inline (exclusion planchers si VRAC)
// - Si le modal est ouvert pour cette ULD (rare mais robuste D-21) : le ferme pour éviter état incohérent
//   (alternative : re-render, plus complexe, pas prioritaire — fermer + laisser l'agent rouvrir suffit)
function changeUldType(selectEl, uldIndex) { ... }
```

#liveRecap format actuel (updateRecap app.js:389-394) :
```javascript
var html = '<span class="recap-item">ULD : <span class="recap-value">' + nbUld + '</span></span>';
html += '<span class="recap-item">Colis : <span class="recap-value">' + totalColis + '</span></span>';
if (hasWeight) html += '<span class="recap-item">Poids : <span class="recap-value">' + totalWeight + ' kg</span></span>';
html += '<span class="recap-item">LTA : <span class="recap-value">' + (ltas.length > 0 ? esc(ltas.join(', ')) : '-') + '</span></span>';
if (hasDgr) html += '<span class="recap-item recap-dgr">DGR : OUI</span>';
```

#liveRecap format CIBLE avec VRAC (D-07, D-08) — annotation CONDITIONNELLE dans le span ULD :
```javascript
// Compter vrac + agréger leurs colis + poids AVANT de construire le HTML
var vracCount = 0, vracColis = 0, vracWeight = 0;
blocks.forEach(function(block) {
    var sel = block.querySelector('.uld-type');
    if (sel && sel.value === 'VRAC') {
        vracCount++;
        block.querySelectorAll('.colis-input').forEach(function(inp) { vracColis += parseInt(inp.value) || 0; });
        var w = parseFloat(block.querySelector('.uld-weight').value);
        if (!isNaN(w) && w > 0) vracWeight += w;
    }
});
var uldSpanInner = nbUld;
if (vracCount > 0) {
    // Format EXACT : "4 dont Vrac : 1 (12 colis, 240 kg)"
    uldSpanInner = nbUld + ' dont Vrac : ' + vracCount + ' (' + vracColis + ' colis, ' + vracWeight + ' kg)';
}
// Puis injection dans <span class="recap-value"> (esc non nécessaire — tous ints, mais safe : esc sur le tout)
var html = '<span class="recap-item">ULD : <span class="recap-value">' + uldSpanInner + '</span></span>';
// ... reste identique
```

Helper esc() (app.js:16-19) :
```javascript
function esc(str)  // anti-XSS, MANDATORY en défense en profondeur même si type contraint (<select>)
```

Pattern de tests existant à mirrorer (tests/tests.html:729, 803-913) :
```javascript
suite('Type ULD - addUld cree select avec PMC par defaut');
(function() {
    resetDOM();
    addUld();
    var block = document.querySelector('.uld-block');
    var sel = block.querySelector('.uld-type');
    assert('Select .uld-type present', !!sel);
    assertEqual('Nombre d\'options', sel.querySelectorAll('option').length, 5);
    assertEqual('Valeur par defaut', sel.value, 'PMC');
})();
```

Flag XSS du harness (tests/tests.html:60-61) :
```javascript
window._xss = 0;  // incrémenté par les payloads d'attaque ; tests vérifient qu'il reste à 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sélecteur type ULD (addUld + loadManifest + collectData) + data model + CSS + rétro-compat + tests</name>
  <files>static/js/app.js, static/css/style.css, tests/tests.html</files>
  <read_first>
    - static/js/app.js lignes 1-20 (esc et version) + 304-339 (addUld) + 413-455 (collectData) + 507-577 (loadManifest)
    - static/js/app.js lignes 373-395 (updateRecap — à laisser intact dans cette Task 1 ; sera étendu en Task 2)
    - static/css/style.css lignes 52-62 (.uld-header, .uld-block) + 186-247 (@media max-width: 768px — patterns de stacking mobile à respecter)
    - .planning/phases/02-type-uld-vrac/02-CONTEXT.md (lire les décisions D-01 à D-06, D-15, D-16, D-17 — fondations data model)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-01-SUMMARY.md (pattern data-attributes, key naming convention)
    - tests/tests.html lignes 135-158 (suite 'addUld / addRow' pattern à suivre) + 451-488 (suite 'Materiel ULD - collectData nouveaux champs' pattern à suivre) + 511-558 (suite 'Materiel ULD - loadManifest retro-compat' pattern à suivre) + 415-450 (suite 'Retro-compatibilite ancien format (pmcs)' pour le save/load test)
    - CLAUDE.md sections Conventions (JS vanilla, pas de framework) + Sécurité (esc obligatoire en défense en profondeur) + Tests (nouvelle feature → tests obligatoires)
  </read_first>
  <behavior>
    - Test 1 (Type ULD - addUld cree select avec PMC par defaut) : addUld() crée un <select class="uld-type"> avec 5 options (PMC, AKE, AKN, PAG, VRAC), valeur `.value === 'PMC'` par défaut, et le select est placé AVANT l'input .uld-number dans le .uld-header (test via compareDocumentPosition ou indexOf).
    - Test 2 (Type ULD - addUld data-uld-type est PMC) : Après addUld(), `block.dataset.uldType === 'PMC'` (le data-attribute est défini en écho du select, D-06).
    - Test 3 (Type ULD - collectData inclut type) : Créer 2 ULD, changer le type du 2ème à 'VRAC' via son <select>, appeler collectData(), vérifier `data.ulds[0].type === 'PMC'` et `data.ulds[1].type === 'VRAC'` (D-05, source de vérité = select D-06).
    - Test 4 (Type ULD - loadManifest retro-compat type absent → PMC) : Écrire un manifeste ANCIEN format (sans aucune clé 'type' dans les ulds) dans localStorage chiffré, await loadManifest(id). Chaque bloc ULD chargé doit avoir `.uld-type` avec value='PMC' ET `dataset.uldType === 'PMC'` (D-15).
    - Test 5 (Type ULD - loadManifest nouveau format restaure type) : Écrire un manifeste avec `ulds[0].type = 'VRAC'`, loadManifest, vérifier que le select .uld-type du bloc chargé a value='VRAC' ET data-uld-type="VRAC".
    - Test 6 (Type ULD - changeUldType met a jour dataset + declenche updateRecap + refreshMaterialBadge) : Créer une ULD, changer son select vers 'VRAC' (dispatch event change OU appel direct de changeUldType), vérifier dataset.uldType='VRAC', vérifier qu'updateRecap a été déclenché (lire #liveRecap → contient "dont Vrac").
    - Test 7 (Type ULD - XSS defense en profondeur sur le type) : Même si le select est contraint, appeler esc() sur le type quand il est inséré via innerHTML (test : mocker block.dataset.uldType = '<img src=x>', puis checker que le récap inline ou une autre innerHTML ne contient pas '<img src=x' littéral). Note : le type vient toujours du <select>, donc ce test est défensif.
    - Test 8 (Type ULD - save/load round-trip preserve type) : addUld(), changer type à 'AKE', saveManifest(), newManifest(), loadManifest(id), vérifier que le select a value='AKE'.
  </behavior>
  <action>
    **0) Ajouter la constante ULD_TYPES en tête de `static/js/app.js`** (après `// --- State ---` ligne 10-13, AVANT la section HTML escape) :

    ```javascript
    // --- Types ULD (VRAC-01, D-01, D-02) ---
    // Liste figée des types officiels. VRAC est le seul avec comportement spécial (exclusion compteur palettes, masquage planchers modal).
    var ULD_TYPES = ['PMC', 'AKE', 'AKN', 'PAG', 'VRAC'];
    var ULD_TYPE_DEFAULT = 'PMC';
    ```

    **1) Étendre `addUld()` (app.js:304-339)** — ajouter le data-attribute data-uld-type ET injecter le <select> dans le header AVANT l'input .uld-number :

    Après la ligne `div.setAttribute('data-uld-comment', '');` (app.js:320), ajouter :
    ```javascript
    // Type ULD (VRAC-01, D-05, D-06) — defaut PMC
    div.setAttribute('data-uld-type', ULD_TYPE_DEFAULT);
    ```

    Puis modifier l'innerHTML du .uld-header (app.js:321-329) pour insérer le select AVANT le label "ULD N° :". Le nouveau HTML du header devient :
    ```javascript
    div.innerHTML =
        '<div class="uld-header">' +
            '<label>Type :</label>' +
            '<select class="uld-type" onchange="changeUldType(this, ' + i + ')">' +
                '<option value="PMC" selected>PMC</option>' +
                '<option value="AKE">AKE</option>' +
                '<option value="AKN">AKN</option>' +
                '<option value="PAG">PAG</option>' +
                '<option value="VRAC">VRAC</option>' +
            '</select>' +
            '<label>ULD N° :</label>' +
            '<input type="text" class="uld-number" placeholder="Numéro ULD" style="width:180px">' +
            '<label style="margin-left:16px;">Poids (kg) :</label>' +
            '<input type="number" class="uld-weight" placeholder="Optionnel" style="width:100px" min="0" step="0.1" oninput="updateRecap()">' +
            '<button class="btn btn-secondary btn-sm btn-material" onclick="openMaterialModal(' + i + ')">Matériel</button>' +
            '<button class="btn btn-danger" onclick="removeUld(' + i + ')">Supprimer ULD</button>' +
        '</div>' +
        '<div class="material-badge-wrapper" id="material-badge-' + i + '"></div>' +
        '<table>...(reste inchangé)</table>';
    ```

    Note : ordre visuel final des éléments dans .uld-header (D-04) = `[Type ▼] [N° ULD ____] [Poids __] [Matériel] [Supprimer]`. Le label "Type :" est court et aligné avec le label "ULD N° :" existant. La typographie suit le pattern des autres labels du header (font-weight: 600 hérité de .uld-header label).

    **2) Étendre `loadManifest()` (app.js:507-577)** — ajouter le data-attribute data-uld-type en lecture défensive + injecter le <select> avec l'option correspondante pré-sélectionnée :

    Après `div.setAttribute('data-uld-comment', String(uldData.uldComment || ''));` (app.js:544), ajouter :
    ```javascript
    // Type ULD (VRAC-01, D-05, D-15 retro-compat : type absent → PMC)
    var uldType = (uldData.type && ULD_TYPES.indexOf(uldData.type) >= 0) ? uldData.type : ULD_TYPE_DEFAULT;
    div.setAttribute('data-uld-type', uldType);
    ```

    Note `ULD_TYPES.indexOf(uldData.type) >= 0` : protection contre valeurs corrompues dans le localStorage (défense en profondeur D-15). Si la valeur n'est pas dans la liste figée, fallback sur PMC.

    Modifier l'innerHTML du .uld-header dans loadManifest (app.js:546-551) pour injecter le <select> de type avec l'option correspondante `selected`. Le helper pour construire les options :
    ```javascript
    // Helper inline — construit les <option> avec la bonne option pre-selectionnee
    var typeOptions = ULD_TYPES.map(function(t) {
        return '<option value="' + t + '"' + (t === uldType ? ' selected' : '') + '>' + t + '</option>';
    }).join('');
    ```

    Nouveau header de loadManifest :
    ```javascript
    div.innerHTML =
        '<div class="uld-header">' +
            '<label>Type :</label>' +
            '<select class="uld-type" onchange="changeUldType(this, ' + i + ')">' + typeOptions + '</select>' +
            '<label>ULD N° :</label>' +
            '<input type="text" class="uld-number" placeholder="Numéro ULD" style="width:180px" value="' + esc(uldData.uldNumber || uldData.pmcNumber || '') + '">' +
            '<label style="margin-left:16px;">Poids (kg) :</label>' +
            '<input type="number" class="uld-weight" placeholder="Optionnel" style="width:100px" min="0" step="0.1" oninput="updateRecap()" value="' + esc(uldData.weight || '') + '">' +
            '<button class="btn btn-secondary btn-sm btn-material" onclick="openMaterialModal(' + i + ')">Matériel</button>' +
            '<button class="btn btn-danger" onclick="removeUld(' + i + ')">Supprimer ULD</button>' +
        '</div>' +
        '<div class="material-badge-wrapper" id="material-badge-' + i + '"></div>' +
        '<table>...(reste inchangé)</table>';
    ```

    **3) Étendre `collectData()` (app.js:413-455)** — ajouter la lecture de .type via le <select> (source de vérité live, D-06) :

    Après `uldEntry.uldComment = block.dataset.uldComment || '';` (app.js:441), ajouter :
    ```javascript
    // Type ULD (VRAC-01, D-05, D-06 : source de verite live = valeur du <select>, fallback PMC)
    var typeSelect = block.querySelector('.uld-type');
    var typeValue = typeSelect ? typeSelect.value : ULD_TYPE_DEFAULT;
    uldEntry.type = (ULD_TYPES.indexOf(typeValue) >= 0) ? typeValue : ULD_TYPE_DEFAULT;
    ```

    **4) Créer `changeUldType(selectEl, uldIndex)` dans static/js/app.js** — à placer juste après `formatCondensedMaterial` et `refreshMaterialBadge` (app.js:186-189) AVANT la section ENCRYPTED STORAGE :

    ```javascript
    // ============================================
    // TYPE ULD - Handler onchange du <select class="uld-type"> (VRAC-01, D-21)
    // ============================================
    // Déclenché quand l'agent change le type dans le dropdown d'une ULD.
    // Effets en cascade :
    //  (a) Met à jour block.dataset.uldType (synchronisation avec le select, D-06)
    //  (b) Appelle updateRecap() pour rafraichir l'annotation "dont Vrac" dans le #liveRecap
    //  (c) Appelle refreshMaterialBadge(uldIndex) pour actualiser le récap inline
    //      (les planchers disparaissent de la chaine condensee quand type devient VRAC, D-21)
    //  (d) Si le modal est ouvert pour CETTE ULD (cas rare, robustesse) : on le ferme
    //      pour eviter un etat incoherent (planchers affiches alors que VRAC).
    //      L'agent peut rouvrir le modal si besoin — simple et sur.
    function changeUldType(selectEl, uldIndex) {
        var block = document.getElementById('uld-' + uldIndex);
        if (!block || !selectEl) return;
        var newType = selectEl.value;
        // Defensive: valider que newType est dans la liste figée
        if (ULD_TYPES.indexOf(newType) < 0) newType = ULD_TYPE_DEFAULT;
        block.setAttribute('data-uld-type', newType);
        // Si le modal est ouvert pour cette ULD, fermer (evite incoherence planchers)
        var openModal = document.querySelector('.material-modal');
        if (openModal && openModal.getAttribute('data-uld-index') === String(uldIndex)) {
            closeMaterialModal();
        }
        // Rafraichir recap global (annotation "dont Vrac") + recap inline (exclusion planchers si VRAC)
        updateRecap();
        refreshMaterialBadge(uldIndex);
    }
    ```

    **5) Ajouter CSS `.uld-type` dans static/css/style.css** — APRÈS la règle `.uld-header input` (ligne 60) et AVANT `.uld-totals` (ligne 61) :

    ```css
    /* Type ULD select (VRAC-01) */
    .uld-header .uld-type {
        padding: 6px 10px;
        border: 1px solid #cbd5e0;
        border-radius: 4px;
        font-size: 1em;
        background: #fff;
        min-width: 90px;
    }
    ```

    Dans le bloc `@media (max-width: 768px)` existant (style.css:186-247), AJOUTER après `.uld-header input { width: 100% !important; }` (ligne 205) :
    ```css
        /* Type ULD select mobile (VRAC-01) */
        .uld-header .uld-type { width: 100% !important; }
    ```

    **6) Ajouter les tests dans tests/tests.html** — suite à placer APRÈS la suite `'Materiel ULD - loadManifest restaure recap (RECAP-01 / TEST-02)'` (tests.html:916-953) et AVANT le commentaire `// PLAN 01-02 : Materiel PDF` :

    ```javascript
    // ============================================
    // PLAN 02-01 : Type ULD VRAC - selecteur + data model + retro-compat (VRAC-01, VRAC-02)
    // ============================================

    // --- Test 1 : addUld cree select avec PMC par defaut ---
    suite('Type ULD - addUld cree select avec PMC par defaut');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        var sel = block.querySelector('.uld-type');
        assert('Select .uld-type present dans le header', !!sel);
        var opts = sel.querySelectorAll('option');
        assertEqual('Nombre d\'options = 5', opts.length, 5);
        assertEqual('Option 0 = PMC', opts[0].value, 'PMC');
        assertEqual('Option 1 = AKE', opts[1].value, 'AKE');
        assertEqual('Option 2 = AKN', opts[2].value, 'AKN');
        assertEqual('Option 3 = PAG', opts[3].value, 'PAG');
        assertEqual('Option 4 = VRAC', opts[4].value, 'VRAC');
        assertEqual('Valeur par defaut = PMC', sel.value, 'PMC');
    })();

    // --- Test 2 : addUld positionne le select AVANT .uld-number ---
    suite('Type ULD - select positionne avant uld-number');
    (function() {
        resetDOM();
        addUld();
        var header = document.querySelector('.uld-header');
        var sel = header.querySelector('.uld-type');
        var numInput = header.querySelector('.uld-number');
        assert('Select ET input present', !!sel && !!numInput);
        // compareDocumentPosition : Node.DOCUMENT_POSITION_FOLLOWING (4) signifie que numInput suit sel
        var pos = sel.compareDocumentPosition(numInput);
        assert('Select precede .uld-number (Node.DOCUMENT_POSITION_FOLLOWING)', (pos & 4) !== 0);
    })();

    // --- Test 3 : addUld ecrit data-uld-type = PMC ---
    suite('Type ULD - addUld data-uld-type par defaut');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        assertEqual('data-uld-type = PMC', block.dataset.uldType, 'PMC');
    })();

    // --- Test 4 : collectData inclut la cle type ---
    suite('Type ULD - collectData inclut type');
    (function() {
        resetDOM();
        addUld();
        addUld();
        var blocks = document.querySelectorAll('.uld-block');
        // Changer le type de la 2eme ULD a VRAC via le select
        blocks[1].querySelector('.uld-type').value = 'VRAC';
        // Simuler un change (pour mettre a jour dataset + recap via changeUldType)
        changeUldType(blocks[1].querySelector('.uld-type'), 2);
        var data = collectData();
        assertEqual('ULD 0 type = PMC', data.ulds[0].type, 'PMC');
        assertEqual('ULD 1 type = VRAC', data.ulds[1].type, 'VRAC');
    })();

    // --- Test 5 : collectData fallback PMC si select absent ou value corrompue ---
    suite('Type ULD - collectData fallback PMC');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        // Simuler une valeur corrompue hors liste (defense en profondeur)
        block.querySelector('.uld-type').innerHTML += '<option value="BOGUS">BOGUS</option>';
        block.querySelector('.uld-type').value = 'BOGUS';
        var data = collectData();
        assertEqual('Valeur corrompue fallback a PMC', data.ulds[0].type, 'PMC');
    })();

    // --- Test 6 : loadManifest retro-compat (type absent) ---
    suite('Type ULD - loadManifest retro-compat type absent => PMC (D-15)');
    await (async function() {
        resetDOM();
        await deriveAndStoreKey('test-password-for-unit-tests');
        // Manifeste ANCIEN format : aucune cle "type" dans l'ULD
        var manifests = [{
            manifestId: 'TEST-VRAC-OLD',
            date: '2026-04-23',
            client: '',
            agent: 'A',
            destAirport: 'CDG',
            ulds: [{
                uldNumber: 'U1',
                rows: [{ lta: 'L', dossier: 'D', colis: 1, dgr: 'N', comment: '' }],
                totalColis: 1
                // Pas de type, pas de champ materiel non plus
            }]
        }];
        await writeSavedManifests(manifests);
        await loadManifest('TEST-VRAC-OLD');
        var block = document.querySelector('.uld-block');
        assert('Bloc ULD charge', !!block);
        var sel = block.querySelector('.uld-type');
        assertEqual('Select present avec valeur PMC (default)', sel.value, 'PMC');
        assertEqual('data-uld-type = PMC', block.dataset.uldType, 'PMC');
    })();

    // --- Test 7 : loadManifest restaure type VRAC ---
    suite('Type ULD - loadManifest restaure type VRAC');
    await (async function() {
        resetDOM();
        await deriveAndStoreKey('test-password-for-unit-tests');
        var manifests = [{
            manifestId: 'TEST-VRAC-NEW',
            date: '2026-04-23',
            client: '',
            agent: 'A',
            destAirport: 'CDG',
            ulds: [{
                uldNumber: 'V1',
                rows: [{ lta: 'L', dossier: 'D', colis: 5, dgr: 'N', comment: '' }],
                totalColis: 5,
                type: 'VRAC',
                strapsCount: 0, flooringEuCount: 0, flooringEuForfait: false,
                flooringStdCount: 0, flooringStdForfait: false,
                blocksCount: 0, tarpsCount: 0, dividersCount: 0, honeycombCount: 0,
                uldComment: ''
            }]
        }];
        await writeSavedManifests(manifests);
        await loadManifest('TEST-VRAC-NEW');
        var block = document.querySelector('.uld-block');
        var sel = block.querySelector('.uld-type');
        assertEqual('Select value = VRAC', sel.value, 'VRAC');
        assertEqual('data-uld-type = VRAC', block.dataset.uldType, 'VRAC');
    })();

    // --- Test 8 : loadManifest rejette type corrompu → PMC ---
    suite('Type ULD - loadManifest rejette type corrompu (D-15)');
    await (async function() {
        resetDOM();
        await deriveAndStoreKey('test-password-for-unit-tests');
        var manifests = [{
            manifestId: 'TEST-VRAC-BOGUS',
            date: '2026-04-23',
            client: '',
            agent: 'A',
            destAirport: 'CDG',
            ulds: [{
                uldNumber: 'U1',
                rows: [{ lta: 'L', dossier: 'D', colis: 1, dgr: 'N', comment: '' }],
                totalColis: 1,
                type: '<img src=x onerror=alert(1)>' // payload XSS
            }]
        }];
        await writeSavedManifests(manifests);
        await loadManifest('TEST-VRAC-BOGUS');
        var block = document.querySelector('.uld-block');
        var sel = block.querySelector('.uld-type');
        assertEqual('Select fallback a PMC', sel.value, 'PMC');
        assertEqual('data-uld-type = PMC', block.dataset.uldType, 'PMC');
        // Verifier qu'aucun <img> n'a ete injecte dans le DOM
        assertEqual('Aucun <img> dans le bloc ULD', block.querySelectorAll('img').length, 0);
    })();

    // --- Test 9 : changeUldType met a jour dataset + declenche updateRecap + refreshMaterialBadge ---
    suite('Type ULD - changeUldType met a jour dataset');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        var sel = block.querySelector('.uld-type');
        sel.value = 'VRAC';
        changeUldType(sel, 1);
        assertEqual('data-uld-type synchro a VRAC', block.dataset.uldType, 'VRAC');
        // updateRecap a ete appele => #liveRecap contient "dont Vrac"
        var recap = document.getElementById('liveRecap').innerHTML;
        assert('#liveRecap contient "dont Vrac"', recap.indexOf('dont Vrac') >= 0);
    })();

    // --- Test 10 : save/load round-trip preserve type ---
    suite('Type ULD - save/load round-trip preserve type (TEST-02)');
    await (async function() {
        resetDOM();
        await deriveAndStoreKey('test-password-for-unit-tests');
        addUld();
        var block = document.querySelector('.uld-block');
        block.querySelector('.uld-number').value = 'AKE001';
        block.querySelector('.uld-type').value = 'AKE';
        changeUldType(block.querySelector('.uld-type'), 1);
        document.getElementById('agentName').value = 'A';
        document.getElementById('destAirport').value = 'CDG';
        await saveManifest();
        var savedId = manifestId;
        newManifest();
        await loadManifest(savedId);
        var loadedBlock = document.querySelector('.uld-block');
        var loadedSel = loadedBlock.querySelector('.uld-type');
        assertEqual('Type AKE preserve apres save/load', loadedSel.value, 'AKE');
        assertEqual('data-uld-type AKE preserve', loadedBlock.dataset.uldType, 'AKE');
    })();
    ```

    **Ne PAS toucher dans cette Task 1** : `openMaterialModal` (Task 2 Plan 02-02), `formatCondensedMaterial` (Task 2 de ce plan), `updateRecap` (Task 2 de ce plan), `buildPdf`/`sendEmail`/`buildUldMaterialRows`/etc. (Plan 02-02).
  </action>
  <verify>
    <automated>grep -c "var ULD_TYPES" static/js/app.js returns 1 AND grep -cE "class=\"uld-type\"" static/js/app.js returns 2 AND grep -c "data-uld-type" static/js/app.js returns >= 3 AND grep -c "function changeUldType" static/js/app.js returns 1 AND grep -c "\\.uld-type" static/css/style.css returns >= 2 AND grep -cE "suite\\(.Type ULD - " tests/tests.html returns >= 8</automated>
  </verify>
  <done>
    - `grep -n "var ULD_TYPES" static/js/app.js` retourne 1 ligne contenant `['PMC', 'AKE', 'AKN', 'PAG', 'VRAC']`
    - `grep -n "class=\"uld-type\"" static/js/app.js` retourne exactement 2 lignes (addUld + loadManifest)
    - `grep -n "data-uld-type" static/js/app.js` retourne >= 3 lignes (addUld setAttribute + loadManifest setAttribute + changeUldType setAttribute)
    - `grep -n "function changeUldType" static/js/app.js` retourne exactement 1 ligne
    - `grep -n "ULD_TYPES.indexOf" static/js/app.js` retourne >= 2 lignes (loadManifest validation + collectData validation)
    - `grep -n "\\.uld-type" static/css/style.css` retourne >= 2 lignes (desktop + mobile)
    - `grep -nE "suite\\(.Type ULD - " tests/tests.html` retourne >= 8 nouvelles suites
    - En local (`npx serve .`) : ouvrir l'app, voir un select "Type" à côté de chaque ULD avec PMC sélectionné par défaut
    - Changer le select vers VRAC sur une ULD, voir #liveRecap se mettre à jour avec "dont Vrac : 1 (... colis, ... kg)"
    - Sauvegarder puis recharger le manifeste, le type VRAC est préservé
    - `tests/tests.html` affiche `all-pass` avec les nouvelles suites `Type ULD - *`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: updateRecap annotation "dont Vrac" + openMaterialModal masque planchers VRAC + formatCondensedMaterial exclut planchers VRAC + tests</name>
  <files>static/js/app.js, static/css/style.css, tests/tests.html</files>
  <read_first>
    - static/js/app.js (état après Task 1 : ULD_TYPES défini, addUld et loadManifest ont le <select>, collectData lit .type, changeUldType existe)
    - static/js/app.js lignes 27-78 (openMaterialModal — structure innerHTML du modal, identifier les 2 <label> planchers EU et Std)
    - static/js/app.js lignes 140-186 (formatCondensedMaterial + refreshMaterialBadge — à étendre pour exclure planchers si VRAC)
    - static/js/app.js lignes 373-395 (updateRecap — à étendre avec annotation "dont Vrac")
    - .planning/phases/02-type-uld-vrac/02-CONTEXT.md (D-07, D-08 format exact #liveRecap ; D-10 totaux globaux inchangés ; D-18 masquage planchers ; D-19 préservation data-attributes ; D-20 exclusion rendu PDF/email planchers VRAC — traité en Plan 02-02 ; D-21 declenchement refreshMaterialBadge au change)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-03-SUMMARY.md (pattern formatCondensedMaterial + refreshMaterialBadge pour rétro-compat de l'approche)
    - tests/tests.html lignes 803-913 (suites 'Materiel ULD - recap *' — pattern à mirrorer pour les tests exclusion planchers VRAC)
    - CLAUDE.md sections Conventions + Sécurité (esc en défense en profondeur)
  </read_first>
  <behavior>
    - Test 1 (updateRecap annotation VRAC présente) : Créer 2 ULD, type ULD[0]=PMC/colis=10, type ULD[1]=VRAC/colis=12/weight=240, appeler updateRecap(). `#liveRecap.textContent` contient `dont Vrac : 1 (12 colis, 240 kg)`.
    - Test 2 (updateRecap format exact sans VRAC) : Créer 2 ULD type PMC (pas de VRAC), `#liveRecap.textContent` NE contient PAS `dont Vrac` (annotation conditionnelle absente).
    - Test 3 (updateRecap totaux globaux incluent VRAC, D-10) : ULD[0]=PMC/colis=10/weight=500, ULD[1]=VRAC/colis=12/weight=240. `#liveRecap` contient `Colis :` suivi de `22` ET `Poids :` suivi de `740 kg` (totaux globaux, pas seulement palettes).
    - Test 4 (updateRecap plusieurs VRAC agrègent) : 3 ULD VRAC avec colis=5,3,7 et weights=100,200,50. L'annotation doit contenir `dont Vrac : 3 (15 colis, 350 kg)`.
    - Test 5 (openMaterialModal masque planchers si VRAC, D-18) : Créer ULD, changer type à VRAC, ouvrir le modal. Les 2 <label> contenant "Planchers bois EU" et "Planchers bois Standard" doivent avoir la class `.mat-flooring-hidden` (ou attribut hidden, ou style display:none — approche concrète : class `.mat-flooring-hidden { display: none; }`).
    - Test 6 (openMaterialModal affiche planchers si non-VRAC) : Créer ULD type PMC (default), ouvrir le modal. Les 2 <label> planchers sont visibles (pas la class `.mat-flooring-hidden`).
    - Test 7 (Persistance data-flooring-* quand toggle VRAC, D-19) : Créer ULD type PMC, ouvrir modal, saisir flooringEu=3 + forfait Std coché, valider. Changer type à VRAC via changeUldType. Vérifier que `block.dataset.flooringEu === '3'` et `block.dataset.flooringStdForfait === 'true'` (pas d'écrasement). Changer type retour à AKE (non-VRAC). Ouvrir modal → les valeurs 3 et forfait sont RESTORES dans les inputs du modal.
    - Test 8 (formatCondensedMaterial exclut planchers VRAC, D-21) : Créer ULD avec type=VRAC et strapsCount=2 et flooringEuCount=5. Appeler refreshMaterialBadge. Le récap inline NE contient PAS "Planchers EU" mais contient "Sangles: 2".
    - Test 9 (formatCondensedMaterial inclut planchers si non-VRAC) : Créer ULD avec type=PMC et flooringEuCount=5. Le récap inline contient "Planchers EU: 5".
    - Test 10 (changeUldType declenche refreshMaterialBadge avec exclusion, D-21) : ULD avec type=PMC + flooringEu=5. Récap inline montre "Planchers EU: 5". Changer type à VRAC via changeUldType. Récap inline recalculé NE contient plus "Planchers EU".
  </behavior>
  <action>
    **1) Étendre `updateRecap()` (app.js:373-395)** — calculer le compteur VRAC + colis VRAC + poids VRAC pendant l'itération existante, puis injecter conditionnellement l'annotation "dont Vrac" dans le span ULD :

    Remplacer tout le corps de la fonction par :
    ```javascript
    function updateRecap() {
        var blocks = document.querySelectorAll('.uld-block');
        var nbUld = blocks.length;
        var totalColis = 0;
        var totalWeight = 0;
        var hasWeight = false;
        var ltaSet = {};
        var hasDgr = false;
        // VRAC-02, D-07/D-08 : agregation conditionnelle des ULD VRAC
        var vracCount = 0, vracColis = 0, vracWeight = 0;
        blocks.forEach(function(block) {
            var blockColis = 0;
            var blockWeight = 0;
            var hasBlockWeight = false;
            block.querySelectorAll('.colis-input').forEach(function(inp) { blockColis += parseInt(inp.value) || 0; });
            var w = parseFloat(block.querySelector('.uld-weight') ? block.querySelector('.uld-weight').value : '');
            if (!isNaN(w) && w > 0) { blockWeight = w; hasBlockWeight = true; }
            // Accumulation globale (D-10 : totaux INCLUENT VRAC)
            totalColis += blockColis;
            if (hasBlockWeight) { totalWeight += blockWeight; hasWeight = true; }
            block.querySelectorAll('.lta-input').forEach(function(inp) { if (inp.value.trim()) ltaSet[inp.value.trim()] = true; });
            block.querySelectorAll('.dgr-input').forEach(function(sel) { if (sel.value === 'O') hasDgr = true; });
            // Aggregation VRAC : uniquement si le <select> vaut VRAC (source de verite live D-06)
            var typeSel = block.querySelector('.uld-type');
            var blockType = typeSel ? typeSel.value : 'PMC';
            if (blockType === 'VRAC') {
                vracCount++;
                vracColis += blockColis;
                if (hasBlockWeight) vracWeight += blockWeight;
            }
        });
        var ltas = Object.keys(ltaSet);
        // D-07 / D-08 : si vracCount > 0, annoter le span ULD avec "dont Vrac : N (X colis, Y kg)"
        // Format EXACT : "4 dont Vrac : 1 (12 colis, 240 kg)"
        // Les valeurs sont des entiers/floats, pas de XSS possible — esc() applique defensive sur l'annotation
        var uldSpanContent = String(nbUld);
        if (vracCount > 0) {
            uldSpanContent = nbUld + ' dont Vrac : ' + vracCount + ' (' + vracColis + ' colis, ' + vracWeight + ' kg)';
        }
        var html = '<span class="recap-item">ULD : <span class="recap-value">' + esc(uldSpanContent) + '</span></span>';
        html += '<span class="recap-item">Colis : <span class="recap-value">' + totalColis + '</span></span>';
        if (hasWeight) html += '<span class="recap-item">Poids : <span class="recap-value">' + totalWeight + ' kg</span></span>';
        html += '<span class="recap-item">LTA : <span class="recap-value">' + (ltas.length > 0 ? esc(ltas.join(', ')) : '-') + '</span></span>';
        if (hasDgr) html += '<span class="recap-item recap-dgr">DGR : OUI</span>';
        document.getElementById('liveRecap').innerHTML = html;
    }
    ```

    **Note esc() défense en profondeur** : `uldSpanContent` contient uniquement des entiers (parseInt, length des NodeList) et la chaîne littérale ' dont Vrac : ', mais on applique `esc()` quand même par habitude — cohérent avec le pattern `esc(ltas.join(', '))` existant. Pas de double-escape.

    **2) Étendre `formatCondensedMaterial(block)` (app.js:140-173)** — exclure les entrées Planchers EU et Planchers Std quand `block.dataset.uldType === 'VRAC'` (D-21) :

    Remplacer le corps de la fonction par (modifier UNIQUEMENT les 2 branches planchers, le reste est identique) :
    ```javascript
    function formatCondensedMaterial(block) {
        if (!block) return '';
        var parts = [];
        // VRAC-03 / D-21 : exclusion planchers pour ULD VRAC (coherent avec D-20 PDF/email)
        var isVrac = block.dataset.uldType === 'VRAC';
        var straps = parseInt(block.dataset.straps) || 0;
        if (straps > 0) parts.push('Sangles: ' + straps);
        // Planchers EU : OMIS si isVrac (D-21)
        if (!isVrac) {
            var flEuF = block.dataset.flooringEuForfait === 'true';
            var flEu = parseInt(block.dataset.flooringEu) || 0;
            if (flEuF) parts.push('Planchers EU: forfait');
            else if (flEu > 0) parts.push('Planchers EU: ' + flEu);
            // Planchers Std : OMIS si isVrac (D-21)
            var flStdF = block.dataset.flooringStdForfait === 'true';
            var flStd = parseInt(block.dataset.flooringStd) || 0;
            if (flStdF) parts.push('Planchers Std: forfait');
            else if (flStd > 0) parts.push('Planchers Std: ' + flStd);
        }
        // Autres compteurs : omettre si 0 (inchanges, valides aussi pour VRAC)
        var blocks = parseInt(block.dataset.blocks) || 0;
        if (blocks > 0) parts.push('Bois calage: ' + blocks);
        var tarps = parseInt(block.dataset.tarps) || 0;
        if (tarps > 0) parts.push('Bâches: ' + tarps);
        var dividers = parseInt(block.dataset.dividers) || 0;
        if (dividers > 0) parts.push('Intercalaires: ' + dividers);
        var honeycomb = parseInt(block.dataset.honeycomb) || 0;
        if (honeycomb > 0) parts.push("Nids d'abeille: " + honeycomb);
        // Commentaire : esc() + troncature 30 + "…" (MAT-11 / D-19 de Phase 1)
        var rawComment = String(block.dataset.uldComment || '');
        if (rawComment.length > 0) {
            var truncated = rawComment.length > 30 ? (rawComment.slice(0, 30) + '…') : rawComment;
            parts.push('Com: ' + esc(truncated));
        }
        if (parts.length === 0) return '';
        return parts.join(' | ');
    }
    ```

    **3) Étendre `openMaterialModal(uldIndex)` (app.js:27-78)** — ajouter la logique de masquage conditionnel des 2 labels planchers via class `.mat-flooring-hidden`.

    Approche retenue : **injecter la class `.mat-flooring-hidden` sur les 2 <label> planchers (EU + Std) dans l'innerHTML quand isVrac**, avec la classe CSS `{ display: none; }`. Ceci préserve totalement le DOM et les values (D-19 : si l'agent change de VRAC vers AKE, rouvrir le modal affichera les valeurs planchers intactes, car les data-attributes sont préservés).

    Modifier le début d'openMaterialModal pour lire le type :
    ```javascript
    function openMaterialModal(uldIndex) {
        var block = document.getElementById('uld-' + uldIndex);
        if (!block) return;
        closeMaterialModal();

        var straps = block.dataset.straps || '0';
        var flEu = block.dataset.flooringEu || '0';
        var flEuF = block.dataset.flooringEuForfait === 'true';
        var flStd = block.dataset.flooringStd || '0';
        var flStdF = block.dataset.flooringStdForfait === 'true';
        var blocks = block.dataset.blocks || '0';
        var tarps = block.dataset.tarps || '0';
        var dividers = block.dataset.dividers || '0';
        var honeycomb = block.dataset.honeycomb || '0';
        var uldComment = block.dataset.uldComment || '';
        // VRAC-01 / D-18 : masquer les 2 champs planchers (EU + Std) si type=VRAC
        var isVrac = block.dataset.uldType === 'VRAC';
        var flooringHiddenClass = isVrac ? ' mat-flooring-hidden' : '';
        // ... (reste de la logique overlay / modal inchangee)
    ```

    Puis modifier les 2 <label> planchers dans le modal.innerHTML pour ajouter la classe conditionnelle :
    ```javascript
    modal.innerHTML =
        '<div class="material-modal-header">' + ... +
        '<div class="material-modal-grid">' +
            '<label>Sangles<input type="number" min="0" class="mat-straps" value="' + esc(straps) + '"></label>' +
            // D-18 : label planchers EU avec class conditionnelle mat-flooring-hidden si VRAC
            '<label class="mat-flooring-label' + flooringHiddenClass + '">Planchers bois EU<input type="number" min="0" class="mat-flooring-eu" value="' + esc(flEu) + '"' + (flEuF ? ' disabled' : '') + '>' +
                '<span class="mat-forfait-label"><input type="checkbox" class="mat-flooring-eu-forfait"' + (flEuF ? ' checked' : '') + ' onchange="toggleForfait(this, \'mat-flooring-eu\')"> Forfait négocié</span></label>' +
            // D-18 : label planchers Std avec class conditionnelle mat-flooring-hidden si VRAC
            '<label class="mat-flooring-label' + flooringHiddenClass + '">Planchers bois Standard<input type="number" min="0" class="mat-flooring-std" value="' + esc(flStd) + '"' + (flStdF ? ' disabled' : '') + '>' +
                '<span class="mat-forfait-label"><input type="checkbox" class="mat-flooring-std-forfait"' + (flStdF ? ' checked' : '') + ' onchange="toggleForfait(this, \'mat-flooring-std\')"> Forfait négocié</span></label>' +
            '<label>Bois de calage<input type="number" min="0" class="mat-blocks" value="' + esc(blocks) + '"></label>' +
            // ... (reste inchangé)
    ```

    **Important D-19 :** `applyMaterialToUld()` (app.js:99-129) **ne doit PAS être modifié**. Si les inputs planchers sont masqués via CSS `display: none`, `modal.querySelector('.mat-flooring-eu').value` renvoie toujours la valeur sous-jacente (qui est celle lue depuis block.dataset.flooringEu au moment de openMaterialModal — c'est-à-dire la valeur préservée). Donc applyMaterialToUld écrit les mêmes valeurs, sans corruption.

    **Vérification de cette invariance** dans les tests (Test 7 ci-dessous) : bascule VRAC → PMC → rouvrir modal → les valeurs planchers sont restorées.

    **4) Ajouter CSS `.mat-flooring-hidden` dans static/css/style.css** — après la règle `.mat-comment-label { grid-column: 1 / -1; }` (ligne 154) :

    ```css
    /* Masquage conditionnel planchers (VRAC-01, D-18) — preserve le DOM, cache visuellement */
    .material-modal-grid .mat-flooring-hidden { display: none; }
    ```

    Pas de règle mobile supplémentaire — `display: none` a le même effet sur desktop et mobile.

    **5) Ajouter les tests dans tests/tests.html** — suite à placer APRÈS les tests Task 1 de ce plan (les suites "Type ULD - ..." déjà créées en Task 1) et AVANT le commentaire `// PLAN 01-02 : Materiel PDF` :

    ```javascript
    // ============================================
    // PLAN 02-01 Task 2 : Type ULD - updateRecap + modal planchers + formatCondensedMaterial
    // ============================================

    // --- updateRecap annotation "dont Vrac" presente si VRAC ---
    suite('Type ULD - updateRecap annotation dont Vrac (VRAC-02, D-07 D-08)');
    (function() {
        resetDOM();
        addUld(); // ULD 1 : PMC par defaut
        addUld(); // ULD 2 : VRAC
        var blocks = document.querySelectorAll('.uld-block');
        // Remplir ULD 1 : colis=10, weight=500, PMC
        blocks[0].querySelector('.colis-input').value = '10';
        blocks[0].querySelector('.uld-weight').value = '500';
        // Remplir ULD 2 : colis=12, weight=240, VRAC
        blocks[1].querySelector('.colis-input').value = '12';
        blocks[1].querySelector('.uld-weight').value = '240';
        blocks[1].querySelector('.uld-type').value = 'VRAC';
        changeUldType(blocks[1].querySelector('.uld-type'), 2);
        updateRecap();
        var recap = document.getElementById('liveRecap').textContent;
        assert('Recap contient "dont Vrac : 1 (12 colis, 240 kg)"', recap.indexOf('dont Vrac : 1 (12 colis, 240 kg)') >= 0);
    })();

    // --- updateRecap pas d'annotation si aucun VRAC ---
    suite('Type ULD - updateRecap pas annotation si aucun VRAC');
    (function() {
        resetDOM();
        addUld(); addUld();
        var blocks = document.querySelectorAll('.uld-block');
        blocks[0].querySelector('.colis-input').value = '5';
        blocks[1].querySelector('.colis-input').value = '7';
        updateRecap();
        var recap = document.getElementById('liveRecap').textContent;
        assert('Recap NE contient PAS "dont Vrac"', recap.indexOf('dont Vrac') < 0);
    })();

    // --- updateRecap totaux globaux incluent VRAC (D-10) ---
    suite('Type ULD - updateRecap totaux globaux incluent VRAC (D-10)');
    (function() {
        resetDOM();
        addUld(); addUld();
        var blocks = document.querySelectorAll('.uld-block');
        blocks[0].querySelector('.colis-input').value = '10';
        blocks[0].querySelector('.uld-weight').value = '500';
        blocks[1].querySelector('.colis-input').value = '12';
        blocks[1].querySelector('.uld-weight').value = '240';
        blocks[1].querySelector('.uld-type').value = 'VRAC';
        changeUldType(blocks[1].querySelector('.uld-type'), 2);
        updateRecap();
        var recap = document.getElementById('liveRecap').textContent;
        // Total colis = 10 + 12 = 22 (incluant VRAC)
        assert('Total Colis = 22 (global, incluant VRAC)', /Colis\s*:\s*22/.test(recap));
        // Total poids = 500 + 240 = 740 kg (incluant VRAC)
        assert('Total Poids = 740 kg (global, incluant VRAC)', /Poids\s*:\s*740\s*kg/.test(recap));
    })();

    // --- updateRecap plusieurs VRAC agregent ---
    suite('Type ULD - updateRecap plusieurs VRAC agregent');
    (function() {
        resetDOM();
        addUld(); addUld(); addUld();
        var blocks = document.querySelectorAll('.uld-block');
        var cols = [5, 3, 7]; var weights = [100, 200, 50];
        for (var j = 0; j < 3; j++) {
            blocks[j].querySelector('.colis-input').value = String(cols[j]);
            blocks[j].querySelector('.uld-weight').value = String(weights[j]);
            blocks[j].querySelector('.uld-type').value = 'VRAC';
            changeUldType(blocks[j].querySelector('.uld-type'), j + 1);
        }
        updateRecap();
        var recap = document.getElementById('liveRecap').textContent;
        // 3 VRAC, colis total VRAC = 5+3+7 = 15, weight total VRAC = 100+200+50 = 350
        assert('Annotation "dont Vrac : 3 (15 colis, 350 kg)" presente', recap.indexOf('dont Vrac : 3 (15 colis, 350 kg)') >= 0);
    })();

    // --- Modal masque planchers si VRAC (D-18) ---
    suite('Type ULD - Modal masque planchers si VRAC (D-18)');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        block.querySelector('.uld-type').value = 'VRAC';
        changeUldType(block.querySelector('.uld-type'), 1);
        openMaterialModal(1);
        var modal = document.querySelector('.material-modal');
        var flEuLabel = modal.querySelector('.mat-flooring-eu').closest('label');
        var flStdLabel = modal.querySelector('.mat-flooring-std').closest('label');
        assert('Label Planchers EU a class mat-flooring-hidden', flEuLabel.classList.contains('mat-flooring-hidden'));
        assert('Label Planchers Std a class mat-flooring-hidden', flStdLabel.classList.contains('mat-flooring-hidden'));
    })();

    // --- Modal affiche planchers si non-VRAC (PMC par defaut) ---
    suite('Type ULD - Modal affiche planchers si PMC');
    (function() {
        resetDOM();
        addUld(); // type par defaut PMC
        openMaterialModal(1);
        var modal = document.querySelector('.material-modal');
        var flEuLabel = modal.querySelector('.mat-flooring-eu').closest('label');
        var flStdLabel = modal.querySelector('.mat-flooring-std').closest('label');
        assert('Label Planchers EU sans mat-flooring-hidden', !flEuLabel.classList.contains('mat-flooring-hidden'));
        assert('Label Planchers Std sans mat-flooring-hidden', !flStdLabel.classList.contains('mat-flooring-hidden'));
    })();

    // --- Modal affiche planchers pour les 4 types non-VRAC ---
    suite('Type ULD - Modal affiche planchers pour AKE/AKN/PAG');
    (function() {
        var nonVracTypes = ['AKE', 'AKN', 'PAG'];
        nonVracTypes.forEach(function(t) {
            resetDOM();
            addUld();
            var block = document.querySelector('.uld-block');
            block.querySelector('.uld-type').value = t;
            changeUldType(block.querySelector('.uld-type'), 1);
            openMaterialModal(1);
            var modal = document.querySelector('.material-modal');
            var flEuLabel = modal.querySelector('.mat-flooring-eu').closest('label');
            assert('Type ' + t + ' : label Planchers EU visible (pas mat-flooring-hidden)', !flEuLabel.classList.contains('mat-flooring-hidden'));
            closeMaterialModal();
        });
    })();

    // --- D-19 : Persistance data-flooring-* quand toggle VRAC ---
    suite('Type ULD - Persistance data-flooring-* quand toggle VRAC (D-19)');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        // Saisir planchers en type PMC
        openMaterialModal(1);
        var modal = document.querySelector('.material-modal');
        modal.querySelector('.mat-flooring-eu').value = '3';
        modal.querySelector('.mat-flooring-std-forfait').checked = true;
        modal.querySelector('.mat-flooring-std').disabled = true;
        modal.querySelector('.mat-flooring-std').value = '0';
        applyMaterialToUld(1);
        assertEqual('data-flooring-eu = 3 apres save', block.dataset.flooringEu, '3');
        assertEqual('data-flooring-std-forfait = true apres save', block.dataset.flooringStdForfait, 'true');
        // Basculer vers VRAC
        block.querySelector('.uld-type').value = 'VRAC';
        changeUldType(block.querySelector('.uld-type'), 1);
        // Verifier que les data-attributes planchers sont preserves (D-19 pas d'effacement destructif)
        assertEqual('data-flooring-eu preserve = 3 apres VRAC', block.dataset.flooringEu, '3');
        assertEqual('data-flooring-std-forfait preserve = true apres VRAC', block.dataset.flooringStdForfait, 'true');
        // Rebasculer vers AKE (non-VRAC)
        block.querySelector('.uld-type').value = 'AKE';
        changeUldType(block.querySelector('.uld-type'), 1);
        // Rouvrir le modal : les valeurs sont restorees dans les inputs
        openMaterialModal(1);
        modal = document.querySelector('.material-modal');
        assertEqual('Modal reouvert : flEu input = 3', modal.querySelector('.mat-flooring-eu').value, '3');
        assert('Modal reouvert : flStd forfait checked', modal.querySelector('.mat-flooring-std-forfait').checked);
    })();

    // --- formatCondensedMaterial exclut planchers pour VRAC (D-21) ---
    suite('Type ULD - formatCondensedMaterial exclut planchers VRAC (D-21)');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        // Simuler flooringEu=5 + straps=2 + type=VRAC
        block.setAttribute('data-flooring-eu', '5');
        block.setAttribute('data-straps', '2');
        block.setAttribute('data-uld-type', 'VRAC');
        refreshMaterialBadge(1);
        var recap = document.getElementById('material-badge-1').textContent;
        assert('Recap contient "Sangles: 2"', recap.indexOf('Sangles: 2') >= 0);
        assert('Recap NE contient PAS "Planchers EU"', recap.indexOf('Planchers EU') < 0);
        assert('Recap NE contient PAS "Planchers Std"', recap.indexOf('Planchers Std') < 0);
    })();

    // --- formatCondensedMaterial inclut planchers pour non-VRAC ---
    suite('Type ULD - formatCondensedMaterial inclut planchers si non-VRAC');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        block.setAttribute('data-flooring-eu', '5');
        block.setAttribute('data-uld-type', 'PMC');
        refreshMaterialBadge(1);
        var recap = document.getElementById('material-badge-1').textContent;
        assert('Recap contient "Planchers EU: 5" pour PMC', recap.indexOf('Planchers EU: 5') >= 0);
    })();

    // --- changeUldType declenche refreshMaterialBadge avec exclusion planchers ---
    suite('Type ULD - changeUldType declenche refreshMaterialBadge (D-21)');
    (function() {
        resetDOM();
        addUld();
        var block = document.querySelector('.uld-block');
        block.setAttribute('data-flooring-eu', '5');
        block.setAttribute('data-uld-type', 'PMC');
        refreshMaterialBadge(1);
        var recapBefore = document.getElementById('material-badge-1').textContent;
        assert('Avant: recap contient "Planchers EU: 5"', recapBefore.indexOf('Planchers EU: 5') >= 0);
        // Changer a VRAC via changeUldType
        block.querySelector('.uld-type').value = 'VRAC';
        changeUldType(block.querySelector('.uld-type'), 1);
        var recapAfter = document.getElementById('material-badge-1').textContent;
        assert('Apres VRAC : recap NE contient PLUS "Planchers EU"', recapAfter.indexOf('Planchers EU') < 0);
    })();
    ```

    **Ne PAS toucher dans cette Task 2** : `collectData`, `addUld`, `loadManifest`, `buildPdf`, `sendEmail`, `buildUldMaterialRows`, `buildMaterialSummary`, `buildMaterialSummaryHtml`, `buildUldMaterialHtml`. Ces fonctions soit sont déjà modifiées (Task 1), soit seront modifiées en Plan 02-02.
  </action>
  <verify>
    <automated>grep -c "dont Vrac" static/js/app.js returns >= 1 AND grep -c "vracCount" static/js/app.js returns >= 1 AND grep -c "mat-flooring-hidden" static/js/app.js returns >= 1 AND grep -c "mat-flooring-hidden" static/css/style.css returns 1 AND grep -c "isVrac" static/js/app.js returns >= 1 AND grep -cE "suite\\(.Type ULD - updateRecap|Type ULD - Modal|Type ULD - Persistance|Type ULD - formatCondensed|Type ULD - changeUldType " tests/tests.html returns >= 8</automated>
  </verify>
  <done>
    - `grep -n "dont Vrac" static/js/app.js` retourne >= 1 ligne (dans updateRecap)
    - `grep -n "vracCount\|vracColis\|vracWeight" static/js/app.js` retourne >= 3 lignes (déclaration + usage)
    - `grep -n "mat-flooring-hidden" static/js/app.js` retourne >= 1 ligne (dans openMaterialModal)
    - `grep -n "mat-flooring-hidden" static/css/style.css` retourne exactement 1 ligne (règle display:none)
    - `grep -n "isVrac" static/js/app.js` retourne >= 2 lignes (formatCondensedMaterial + openMaterialModal)
    - `grep -nE "suite\\(.Type ULD - (updateRecap|Modal|Persistance|formatCondensed|changeUldType)" tests/tests.html` retourne >= 8 nouvelles suites
    - En local (`npx serve .`) : créer 2 ULD, changer la 2ème en VRAC avec colis=12 poids=240 → #liveRecap affiche `ULD : 2 dont Vrac : 1 (12 colis, 240 kg) | Colis : ... | Poids : ... | LTA : ...`
    - Ouvrir le modal d'une ULD VRAC : les 2 lignes Planchers bois EU et Planchers bois Standard sont invisibles (display:none)
    - Ouvrir le modal d'une ULD PMC : les 2 lignes Planchers sont visibles (comportement Phase 1 préservé)
    - Saisir planchers sur ULD PMC, basculer VRAC, rebasculer PMC, rouvrir modal → valeurs planchers restorées (test D-19)
    - Récap inline d'une ULD VRAC : pas de mention "Planchers EU" même si data-flooring-eu > 0
    - `tests/tests.html` → `all-pass` (incluant toutes les nouvelles suites Type ULD - *)
  </done>
</task>

</tasks>

<verification>
Phase 2 Plan 02-01 vérifications globales (à tester en local via `npx serve .` avant push) :

1. **VRAC-01 strict (critère #1 ROADMAP)** : chaque ULD a un sélecteur Type avec 5 options PMC/AKE/AKN/PAG/VRAC. Défaut PMC pour toute nouvelle ULD. VRAC apparaît dans la liste.
2. **VRAC-02 via annotation "dont Vrac"** : créer un manifeste avec 4 ULD (3 PMC + 1 VRAC) → #liveRecap affiche `ULD : 4 dont Vrac : 1 (... colis, ... kg) | Colis : ... | Poids : ... | LTA : ...`. L'agent peut déduire 3 palettes = ULD - Vrac.
3. **Totaux globaux incluent VRAC (D-10)** : les compteurs globaux Colis et Poids du #liveRecap incluent toutes les ULD, VRAC et non-VRAC.
4. **Rétro-compat D-15** : ouvrir un manifeste sauvegardé AVANT cette évolution (sans clé `type`) → chaque ULD se charge avec type='PMC' (select + data-attribute synchronisés).
5. **Save/load round-trip D-05** : créer une ULD VRAC, sauvegarder, recharger → type VRAC préservé.
6. **Override D-09 Phase 1 via D-18** : ouvrir modal d'une ULD VRAC → les 2 champs planchers bois (EU + Std avec forfait) sont masqués. Ouvrir modal d'une ULD PMC/AKE/AKN/PAG → les 2 champs planchers sont visibles.
7. **D-19 préservation data-attributes** : ULD PMC avec flooringEu=3 → bascule VRAC → rebascule PMC → rouvrir modal : flooringEu=3 restauré (pas d'écrasement destructif).
8. **D-21 récap inline exclut planchers VRAC** : ULD VRAC avec straps=2 et flooringEu=5 → récap inline affiche `Sangles: 2` mais PAS `Planchers EU: 5`.
9. **changeUldType cascade D-21** : changer le type d'une ULD avec planchers saisis de PMC à VRAC → récap inline recalculé sans planchers ; changer retour à PMC → planchers réapparaissent dans le récap inline.
10. **Défense en profondeur XSS** : data-uld-type corrompu (payload XSS via manifeste ancien altéré) → loadManifest valide contre ULD_TYPES, fallback PMC, aucun `<img>` injecté.
11. **Mobile ≤ 768px** : en DevTools mode mobile, le <select> occupe toute la largeur du `.uld-block`, lisible et touchable.
12. **Barre #liveRecap sans VRAC** : manifeste avec 0 ULD VRAC → #liveRecap identique au format Phase 1 (pas de "dont Vrac" parasite).
13. **Tests** : `tests/tests.html` → toutes les nouvelles suites `Type ULD - *` en vert (≥ 18 suites au total : 10 Task 1 + 8 Task 2 minimum), aucune régression sur suites existantes `Materiel ULD - *`, `Materiel PDF - *`, `Materiel email HTML - *`, SECU, etc.
</verification>

<success_criteria>
- `ULD_TYPES = ['PMC', 'AKE', 'AKN', 'PAG', 'VRAC']` défini en tête de app.js
- `<select class="uld-type">` présent dans addUld() ET loadManifest() avec 5 options dans l'ordre PMC/AKE/AKN/PAG/VRAC
- Select positionné AVANT `<input class="uld-number">` dans `.uld-header` (ordre visuel Type > N° ULD > Poids > Matériel > Supprimer)
- Défaut PMC pour toute nouvelle ULD (D-03)
- loadManifest() applique lecture défensive + validation contre ULD_TYPES (rétro-compat D-15 + défense en profondeur)
- collectData() retourne `type` dans chaque objet ULD (D-05, D-06 source = select)
- changeUldType(selectEl, uldIndex) existe et fait : dataset sync + updateRecap + refreshMaterialBadge + close modal si ouvert (D-21)
- updateRecap() annotation `dont Vrac : N (X colis, Y kg)` CONDITIONNELLE (D-07, D-08) — format exact vérifié
- Totaux globaux Colis et Poids INCLUENT les VRAC (D-10) — pas de soustraction
- openMaterialModal() masque les 2 labels planchers (EU + Std) via class `.mat-flooring-hidden` quand type=VRAC (D-18)
- formatCondensedMaterial() omet les 2 entrées Planchers pour les ULD VRAC (D-21)
- Data-attributes `data-flooring-*` PRÉSERVÉS quand le type bascule (D-19 — pas de reset destructif)
- CSS `.uld-type` + `.mat-flooring-hidden` ajoutés + règle mobile pour le select
- `esc()` appliqué en défense en profondeur sur le type et sur l'annotation "dont Vrac"
- ≥ 18 nouvelles suites de tests `Type ULD - *` couvrant D-01 à D-10 + D-15 à D-21
- Aucune régression sur les suites existantes Phase 1
- Override D-09 Phase 1 explicitement documenté dans le frontmatter (décision de contrat)
- Nuance D-09 Phase 2 explicitement documentée (annotation "dont Vrac" au lieu de "Palettes") pour éviter gap futur
</success_criteria>

<output>
After completion, create `.planning/phases/02-type-uld-vrac/02-01-SUMMARY.md` documenting :
- Décisions appliquées : D-01 à D-10, D-15 à D-21 (liste exacte)
- Constante ajoutée : `ULD_TYPES`, `ULD_TYPE_DEFAULT`
- Fonction publique ajoutée : `changeUldType(selectEl, uldIndex)`
- Fonctions étendues : `addUld`, `loadManifest`, `collectData`, `updateRecap`, `openMaterialModal`, `formatCondensedMaterial`
- Format exact du #liveRecap avec VRAC : `ULD : N dont Vrac : M (X colis, Y kg) | Colis : ... | Poids : ... | LTA : ... | DGR`
- Mechanisme de masquage planchers : class CSS `.mat-flooring-hidden { display: none; }` (préserve le DOM et les values — respect D-19)
- Liste exacte des tests ajoutés (noms des suites + couverture décisions)
- Override D-09 Phase 1 via D-18 : documenté comme changement de contrat (matériel PLUS uniforme pour tous types)
- Note pour Plan 02-02 : les helpers PDF/email (`buildUldMaterialRows`, `buildMaterialSummary`, `buildUldMaterialHtml`, `buildMaterialSummaryHtml`) NE sont PAS modifiés par ce plan — ils seront étendus en 02-02 pour D-11/D-12/D-14/D-20
- Statut VRAC-01 et VRAC-02 : SATISFIED au niveau critère #1 et #2 du ROADMAP (avec nuance D-09 Phase 2 documentée)
- Statut VRAC-03 : PARTIAL (critère #3 ROADMAP requiert affichage dans le récap — l'annotation "dont Vrac" satisfait le récap écran ; Plan 02-02 adressera le rendu PDF + email miroir)
</output>
</content>
</invoke>