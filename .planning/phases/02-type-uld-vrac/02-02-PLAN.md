---
phase: 02-type-uld-vrac
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - static/js/app.js
  - tests/tests.html
autonomous: true
requirements:
  - VRAC-03

# CONTEXT :
# Ce plan consomme les artefacts de 02-01 :
# - data.ulds[i].type (string ∈ {PMC, AKE, AKN, PAG, VRAC}) via collectData
# - helpers existants buildMaterialSummary, buildUldMaterialRows, buildMaterialSummaryHtml, buildUldMaterialHtml
# Il étend ces helpers + buildPdf + sendEmail pour satisfaire D-11 (colonne Type dans entête ULD PDF),
# D-12 (scission Palettes/Vrac en page 1 PDF), D-14 (email HTML miroir PDF), D-20 (exclusion planchers VRAC
# dans rendu ULD détail PDF + email + totaux agrégés page 1).

must_haves:
  truths:
    - "Chaque page ULD du PDF affiche le type (PMC/AKE/AKN/PAG/VRAC) dans l'entête, au même niveau que LTA et Poids (D-11)"
    - "La page 1 du PDF (récap) affiche les scissions 'dont Palettes : N (X colis, Y kg)' + 'dont Vrac : M (X colis, Y kg)' quand au moins une ULD VRAC est présente (D-12)"
    - "Sans aucune ULD VRAC, la page 1 du PDF conserve son format actuel (backward compatible visuellement)"
    - "L'email HTML miroir exact du PDF : colonne Type dans la table récap page 1 + annotation Palettes/Vrac scindée + type affiché dans chaque bloc ULD détail (D-14)"
    - "Les ULD VRAC n'affichent PAS les entrées Planchers bois EU / Planchers bois Standard dans leur section Matériel (ni page PDF ULD, ni email ULD) — D-20"
    - "Les totaux page 1 agrégés (buildMaterialSummary) EXCLUENT les planchers des ULD VRAC : un flooringEuCount=5 sur une ULD VRAC n'est PAS compté dans le total global Planchers EU (D-20)"
    - "esc() est appliqué au type dans toutes les insertions HTML email (défense en profondeur, même si contraint par <select>)"
    - "Un manifeste ancien format sans clé 'type' (rétro-compat D-15) continue à générer PDF et email sans erreur ; type affiché = PMC (déjà appliqué par loadManifest en 02-01, mais data.ulds[i].type via collectData est toujours défini)"
    - "Un manifeste avec uniquement des ULD non-VRAC (aucun VRAC) : le PDF et l'email ont UNIQUEMENT 'dont Palettes' OU un format unique (pas de 'dont Vrac : 0' parasite)"
    - "tests/tests.html contient ≥ 6 nouvelles suites 'Type ULD - PDF/email' couvrant : buildMaterialSummary exclut planchers VRAC, buildUldMaterialRows exclut planchers VRAC, PDF page 1 scission conditionnelle, PDF page ULD entête contient type, email HTML miroir, défense en profondeur XSS sur type"

  artifacts:
    - path: "static/js/app.js"
      provides: |
        - buildMaterialSummary étendu : exclusion planchers EU+Std pour ULD VRAC (D-20)
        - buildUldMaterialRows étendu : exclusion planchers EU+Std si u.type === 'VRAC' (D-20)
        - Nouveau helper : buildPalettesVracSplit(ulds) retourne { hasVrac, palettes: {count, colis, weight}, vrac: {count, colis, weight} } pour PDF page 1 + email
        - buildPdf étendu : page ULD entête affiche type (D-11), page 1 scission conditionnelle Palettes/Vrac (D-12)
        - sendEmail étendu : table récap page 1 scindée (D-12 mirror), titre de bloc ULD inclut type (D-11 mirror)
      contains: "function buildPalettesVracSplit"
    - path: "tests/tests.html"
      provides: "Suite '≥ 6 tests Type ULD - PDF/email' couvrant D-11, D-12, D-14, D-20"
      contains: "Type ULD - PDF"

  key_links:
    - from: "buildMaterialSummary(ulds) (app.js)"
      to: "Skip des champs flooringEu/Std pour chaque ULD avec u.type === 'VRAC' (D-20)"
      via: "check u.type dans le forEach + continue/skip des 4 branches planchers"
      pattern: "u\\.type === 'VRAC'|type !== 'VRAC'"
    - from: "buildUldMaterialRows(u) (app.js)"
      to: "Skip des rows Planchers bois EU/Std si u.type === 'VRAC' (D-20)"
      via: "check u.type en début + skip des 2 branches planchers"
      pattern: "u\\.type === 'VRAC'|isVrac"
    - from: "buildPdf page 1 (app.js)"
      to: "doc.text ou autoTable supplémentaire 'dont Palettes : N (X colis, Y kg)' + 'dont Vrac : M (X colis, Y kg)' quand hasVrac"
      via: "appel à buildPalettesVracSplit + rendu conditionnel sous la table récap existante"
      pattern: "buildPalettesVracSplit|dont Palettes|dont Vrac"
    - from: "buildPdf page ULD (app.js)"
      to: "doc.text 'Type : VRAC' (ou autoTable row) dans l'infoBox de chaque page ULD (D-11)"
      via: "ajout dans la construction de l'infoBox entre LTA et Poids"
      pattern: "Type :|u\\.type"
    - from: "sendEmail html table récap (app.js)"
      to: "Lignes scindées 'dont Palettes' + 'dont Vrac' sous la table récap quand hasVrac (D-14 miroir D-12)"
      via: "concat html après la table récap + avant la section détail ULD"
      pattern: "dont Palettes.*dont Vrac|dont Vrac.*html"
    - from: "sendEmail html détail ULD (app.js)"
      to: "Titre du bloc ULD inclut le type (D-14 miroir D-11)"
      via: "html += <h3>ULD : X [type] — ...</h3>"
      pattern: "esc\\(u\\.type\\)|Type.*esc\\(u\\.type\\)"
---

<objective>
Livrer le rendu VRAC-03 dans les deux canaux de sortie : PDF (buildPdf) et email HTML (sendEmail). Consomme la clé `type` ajoutée au modèle de données par Plan 02-01, étend les helpers matériel pour exclure les planchers des ULD VRAC (D-20), et rend cohérent avec l'annotation "dont Vrac" du #liveRecap.

Purpose: Le critère #3 du ROADMAP Phase 2 (une ligne "Vrac" séparée avec poids + colis) est satisfait en écran par Plan 02-01 (annotation "dont Vrac"). Ce plan étend cette cohérence au PDF (scission page 1 + colonne Type par page ULD) et à l'email HTML (miroir du PDF, D-14). Aucun destinataire ne doit recevoir un manifeste incohérent entre écran/PDF/email.

Output: `static/js/app.js` étendu (buildMaterialSummary + buildUldMaterialRows + nouveau buildPalettesVracSplit + buildPdf + sendEmail), `tests/tests.html` étendu (≥ 6 nouvelles suites Type ULD - PDF/email).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-type-uld-vrac/02-CONTEXT.md
@.planning/phases/02-type-uld-vrac/02-01-PLAN.md
@.planning/phases/02-type-uld-vrac/02-01-SUMMARY.md
@.planning/phases/01-mat-riel-uld-r-tro-compat/01-02-SUMMARY.md
@CLAUDE.md
@static/js/app.js
@tests/tests.html

<interfaces>
<!-- État APRÈS Plan 02-01 : chaque ULD a une clé `type` dans data.ulds[i], collectData la produit,
     helpers existants (buildMaterialSummary, buildUldMaterialRows, buildMaterialSummaryHtml, buildUldMaterialHtml)
     n'ont PAS encore la logique d'exclusion planchers pour VRAC (c'est le travail de ce plan). -->

Structure d'un objet ULD actuel (data.ulds[i]) :
```javascript
{
  uldNumber: string,
  rows: [...], totalColis: number, weight?: number,
  strapsCount, flooringEuCount, flooringEuForfait, flooringStdCount, flooringStdForfait,
  blocksCount, tarpsCount, dividersCount, honeycombCount, uldComment,
  type: 'PMC' | 'AKE' | 'AKN' | 'PAG' | 'VRAC'  // AJOUTÉ par Plan 02-01
}
```

Signatures à ÉTENDRE dans static/js/app.js :
```javascript
function buildMaterialSummary(ulds)       // app.js:624-644 — doit exclure planchers EU+Std pour VRAC (D-20)
function buildUldMaterialRows(u)          // app.js:659-677 — doit exclure planchers EU+Std si u.type === 'VRAC' (D-20)
function buildPdf(data)                   // app.js:741-937 — D-11 colonne Type par page ULD + D-12 scission page 1
async function sendEmail()                // app.js:957-1055 — D-14 miroir PDF (scission récap + type dans titre ULD)
```

buildMaterialSummary ACTUEL (code, app.js:624-644) :
```javascript
function buildMaterialSummary(ulds) {
    var s = { straps: 0, flooringEu: { count: 0, forfaits: 0 }, flooringStd: { count: 0, forfaits: 0 },
              blocks: 0, tarps: 0, dividers: 0, honeycomb: 0 };
    (ulds || []).forEach(function(u) {
        if (!u) return;
        s.straps += parseInt(u.strapsCount) || 0;
        if (u.flooringEuForfait === true) s.flooringEu.forfaits += 1;
        else s.flooringEu.count += parseInt(u.flooringEuCount) || 0;
        if (u.flooringStdForfait === true) s.flooringStd.forfaits += 1;
        else s.flooringStd.count += parseInt(u.flooringStdCount) || 0;
        s.blocks += parseInt(u.blocksCount) || 0;
        // ...
    });
    return s;
}
```

Changement CIBLE pour D-20 (exclure planchers de l'agrégation pour ULD VRAC) :
```javascript
(ulds || []).forEach(function(u) {
    if (!u) return;
    s.straps += parseInt(u.strapsCount) || 0;
    // D-20 : skip planchers pour ULD VRAC (coherent avec l'exclusion dans buildUldMaterialRows)
    if (u.type !== 'VRAC') {
        if (u.flooringEuForfait === true) s.flooringEu.forfaits += 1;
        else s.flooringEu.count += parseInt(u.flooringEuCount) || 0;
        if (u.flooringStdForfait === true) s.flooringStd.forfaits += 1;
        else s.flooringStd.count += parseInt(u.flooringStdCount) || 0;
    }
    s.blocks += parseInt(u.blocksCount) || 0;
    // ... (reste inchangé)
});
```

buildUldMaterialRows ACTUEL (code, app.js:659-677) :
```javascript
function buildUldMaterialRows(u) {
    if (!u) return [];
    var rows = [];
    var straps = parseInt(u.strapsCount) || 0;
    if (straps > 0) rows.push(['Sangles', String(straps)]);
    if (u.flooringEuForfait === true) rows.push(['Planchers bois EU', 'forfait']);
    else { var fe = parseInt(u.flooringEuCount) || 0; if (fe > 0) rows.push(['Planchers bois EU', String(fe)]); }
    // ... (pattern identique Std)
```

Changement CIBLE pour D-20 :
```javascript
function buildUldMaterialRows(u) {
    if (!u) return [];
    var rows = [];
    var isVrac = u.type === 'VRAC';
    var straps = parseInt(u.strapsCount) || 0;
    if (straps > 0) rows.push(['Sangles', String(straps)]);
    // D-20 : skip planchers EU+Std pour VRAC (ni page PDF ULD ni email)
    if (!isVrac) {
        if (u.flooringEuForfait === true) rows.push(['Planchers bois EU', 'forfait']);
        else { var fe = parseInt(u.flooringEuCount) || 0; if (fe > 0) rows.push(['Planchers bois EU', String(fe)]); }
        if (u.flooringStdForfait === true) rows.push(['Planchers bois Standard', 'forfait']);
        else { var fs = parseInt(u.flooringStdCount) || 0; if (fs > 0) rows.push(['Planchers bois Standard', String(fs)]); }
    }
    // ... (reste inchangé)
```

buildPdf page 1 ACTUEL (app.js:801-864) : table récap summary + totaux matériel conditionnels + drawFooter(1).
Point d'insertion pour D-12 : APRÈS `doc.autoTable` summary (ligne 822-833) et AVANT `buildMaterialSummary + hasAnyMat` (ligne 836).

buildPdf page ULD ACTUEL (app.js:867-933) : drawHeader('ULD : ' + u.uldNumber) + infoBox (LTA + Poids) + table détail + section Matériel.
Point d'insertion pour D-11 : dans l'infoBox (ligne 873-890), ajouter une ligne "Type : VRAC" entre LTA et Poids.

sendEmail ACTUEL (app.js:974-1025) :
- Ligne 983-1004 : table récap en haut du HTML
- Ligne 1007 : buildMaterialSummaryHtml(data.ulds) — totaux matériel
- Ligne 1010 : `data.ulds.forEach(function(u) { ... html += '<h3>ULD : ' + esc(u.uldNumber) + ...` — titre de chaque bloc
- Ligne 1024 : buildUldMaterialHtml(u) — section matériel par ULD

Changements CIBLES pour D-14 miroir PDF :
- Après la table récap (ligne 1004 `html += '</table>';`) et AVANT buildMaterialSummaryHtml (ligne 1007) : injecter scission Palettes/Vrac si hasVrac
- Ligne 1011 : inclure esc(u.type) dans le titre du bloc ULD → `<h3>ULD : X [VRAC] — ...</h3>` (format à définir)

esc() (app.js:16-19) : MANDATORY sur esc(u.type) en défense en profondeur (D-14 explicite).

Pattern de tests existant (tests.html:956-1192) pour PDF/email à mirrorer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend buildMaterialSummary + buildUldMaterialRows (D-20) + new helper buildPalettesVracSplit + buildPdf D-11 D-12 + tests</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js lignes 617-724 (section MATERIAL SUMMARY complète — les 4 helpers à étendre : buildMaterialSummary, formatFlooringDisplay inchangé, buildUldMaterialRows, buildMaterialSummaryHtml, buildUldMaterialHtml)
    - static/js/app.js lignes 741-937 (buildPdf entière — comprendre le flow jsPDF, points d'insertion exacts pour D-11 et D-12)
    - static/js/app.js lignes 801-864 (page 1 récap buildPdf, point d'insertion D-12 scission)
    - static/js/app.js lignes 867-933 (page ULD détail buildPdf, point d'insertion D-11 type dans infoBox)
    - .planning/phases/02-type-uld-vrac/02-CONTEXT.md (D-11 format Type dans entête page ULD, D-12 format exact scission page 1, D-13 type littéral PMC/AKE/..., D-20 exclusion planchers VRAC)
    - .planning/phases/02-type-uld-vrac/02-01-SUMMARY.md (nom exact de la clé `type` dans data.ulds[i], valeurs possibles)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-02-SUMMARY.md (structure retournée par buildMaterialSummary, signature buildUldMaterialRows)
    - tests/tests.html lignes 956-1092 (suites 'Materiel PDF - *' — pattern à mirrorer pour nouveaux tests PDF)
    - CLAUDE.md (rappel : jsPDF rendu par défaut n'a pas tous les glyphes UTF-8 — `Bâches` est mappé à `Baches` dans buildPdf app.js:918 ; l'email garde UTF-8 complet)
  </read_first>
  <behavior>
    - Test 1 (buildMaterialSummary exclut planchers VRAC, D-20) : Appeler sur `[{ type:'PMC', flooringEuCount:3 }, { type:'VRAC', flooringEuCount:10, flooringEuForfait:false }, { type:'PMC', flooringEuForfait:true }]`. Résultat : `flooringEu.count === 3` (pas 13 — le 10 du VRAC est exclu), `flooringEu.forfaits === 1`.
    - Test 2 (buildMaterialSummary VRAC n'incrémente pas flooringEu.forfaits) : Appeler sur `[{ type:'VRAC', flooringEuForfait:true }]`. Résultat : `flooringEu.forfaits === 0` (exclu, D-20).
    - Test 3 (buildMaterialSummary VRAC conserve straps, blocks, tarps, dividers, honeycomb) : Appeler sur `[{ type:'VRAC', strapsCount:2, blocksCount:3, tarpsCount:1 }]`. Résultat : `straps === 2, blocks === 3, tarps === 1` (seuls les 2 planchers sont exclus pour VRAC — D-20 stricte sur planchers).
    - Test 4 (buildUldMaterialRows exclut planchers VRAC, D-20) : Appeler sur `{ type:'VRAC', strapsCount:2, flooringEuCount:5, flooringStdForfait:true, blocksCount:1 }`. Les rows retournées : `[['Sangles','2'], ['Bois de calage','1']]` — NI 'Planchers bois EU' NI 'Planchers bois Standard'.
    - Test 5 (buildUldMaterialRows non-VRAC conserve planchers, régression) : Appeler sur `{ type:'PMC', flooringEuCount:5 }` → rows contient `['Planchers bois EU', '5']` (comportement Phase 1 préservé).
    - Test 6 (buildPalettesVracSplit helper) : Appeler sur `[{type:'PMC', totalColis:10, weight:500}, {type:'VRAC', totalColis:12, weight:240}, {type:'PMC', totalColis:20}]`. Résultat : `{ hasVrac: true, palettes: { count:2, colis:30, weight:500 }, vrac: { count:1, colis:12, weight:240 } }`.
    - Test 7 (buildPalettesVracSplit sans VRAC) : Appeler sur `[{type:'PMC', totalColis:10}]`. Résultat : `hasVrac === false`.
    - Test 8 (buildPdf page ULD entête contient Type, D-11) : Spy sur `doc.text`. buildPdf(data) avec une ULD VRAC → parmi les appels à doc.text, un appel contient la chaîne `'VRAC'` (test via tracking des arguments).
    - Test 9 (buildPdf page 1 scission Palettes/Vrac, D-12) : buildPdf sur manifeste avec 1 PMC + 1 VRAC → doc.text ou autoTable appelé avec body contenant `dont Palettes` ET `dont Vrac`.
    - Test 10 (buildPdf page 1 sans VRAC pas de scission) : buildPdf sur manifeste 100% PMC → aucune occurrence `dont Palettes` ni `dont Vrac` dans les appels doc.text/autoTable.
    - Test 11 (buildPdf avec type corrompu/absent ancien format) : buildPdf sur manifeste `{ulds:[{uldNumber:'U', rows:[...], totalColis:1}]}` (sans `type`) ne crash pas. Le rendu utilise 'PMC' par défaut (traité comme non-VRAC).
  </behavior>
  <action>
    **1) Étendre `buildMaterialSummary(ulds)` dans static/js/app.js (app.js:624-644)** — ajouter le skip des planchers pour les ULD VRAC :

    Remplacer la fonction entière par :
    ```javascript
    function buildMaterialSummary(ulds) {
        var s = {
            straps: 0,
            flooringEu: { count: 0, forfaits: 0 },
            flooringStd: { count: 0, forfaits: 0 },
            blocks: 0, tarps: 0, dividers: 0, honeycomb: 0
        };
        (ulds || []).forEach(function(u) {
            if (!u) return;
            s.straps += parseInt(u.strapsCount) || 0;
            // VRAC-03 / D-20 : exclusion planchers EU+Std pour ULD VRAC.
            // Les autres materiels (sangles, bois, baches, intercalaires, nids) restent comptes.
            if (u.type !== 'VRAC') {
                if (u.flooringEuForfait === true) s.flooringEu.forfaits += 1;
                else s.flooringEu.count += parseInt(u.flooringEuCount) || 0;
                if (u.flooringStdForfait === true) s.flooringStd.forfaits += 1;
                else s.flooringStd.count += parseInt(u.flooringStdCount) || 0;
            }
            s.blocks += parseInt(u.blocksCount) || 0;
            s.tarps += parseInt(u.tarpsCount) || 0;
            s.dividers += parseInt(u.dividersCount) || 0;
            s.honeycomb += parseInt(u.honeycombCount) || 0;
        });
        return s;
    }
    ```

    **Note rétro-compat** : un ULD ancien sans clé `type` a `u.type === undefined`, donc `undefined !== 'VRAC'` → la condition entre dans la branche "non-VRAC" → les planchers sont comptés. Comportement Phase 1 préservé.

    **2) Étendre `buildUldMaterialRows(u)` dans static/js/app.js (app.js:659-677)** — ajouter le skip des planchers si u.type === 'VRAC' :

    Remplacer la fonction entière par :
    ```javascript
    function buildUldMaterialRows(u) {
        if (!u) return [];
        var rows = [];
        // VRAC-03 / D-20 : planchers omis pour ULD VRAC
        var isVrac = u.type === 'VRAC';
        var straps = parseInt(u.strapsCount) || 0;
        if (straps > 0) rows.push(['Sangles', String(straps)]);
        if (!isVrac) {
            if (u.flooringEuForfait === true) rows.push(['Planchers bois EU', 'forfait']);
            else { var fe = parseInt(u.flooringEuCount) || 0; if (fe > 0) rows.push(['Planchers bois EU', String(fe)]); }
            if (u.flooringStdForfait === true) rows.push(['Planchers bois Standard', 'forfait']);
            else { var fs = parseInt(u.flooringStdCount) || 0; if (fs > 0) rows.push(['Planchers bois Standard', String(fs)]); }
        }
        var blocks = parseInt(u.blocksCount) || 0; if (blocks > 0) rows.push(['Bois de calage', String(blocks)]);
        var tarps = parseInt(u.tarpsCount) || 0; if (tarps > 0) rows.push(['Bâches', String(tarps)]);
        var dividers = parseInt(u.dividersCount) || 0; if (dividers > 0) rows.push(['Intercalaires', String(dividers)]);
        var honeycomb = parseInt(u.honeycombCount) || 0; if (honeycomb > 0) rows.push(['Nids d\'abeille', String(honeycomb)]);
        var comment = String(u.uldComment || '');
        if (comment.length > 0) rows.push(['Commentaire ULD', comment]);
        return rows;
    }
    ```

    **3) Ajouter le helper `buildPalettesVracSplit(ulds)`** dans static/js/app.js — à placer juste APRÈS `buildUldMaterialHtml` (app.js:~724) et AVANT la section `PDF GENERATION` (app.js:727) :

    ```javascript
    // ============================================
    // PALETTES / VRAC SPLIT (VRAC-03, D-12, D-14)
    // ============================================
    // Partitionne la liste des ULD en Palettes (non-VRAC) vs VRAC, aggrege compteurs + colis + poids.
    // Utilise en page 1 du PDF et dans l'email HTML pour afficher les scissions conditionnelles.
    // hasVrac: false => afficher le format actuel (backward compatible).
    function buildPalettesVracSplit(ulds) {
        var split = {
            hasVrac: false,
            palettes: { count: 0, colis: 0, weight: 0 },
            vrac: { count: 0, colis: 0, weight: 0 }
        };
        (ulds || []).forEach(function(u) {
            if (!u) return;
            var colis = parseInt(u.totalColis) || 0;
            var weight = parseFloat(u.weight) || 0;
            if (u.type === 'VRAC') {
                split.hasVrac = true;
                split.vrac.count++;
                split.vrac.colis += colis;
                if (weight > 0) split.vrac.weight += weight;
            } else {
                // Palettes = tout sauf VRAC (PMC, AKE, AKN, PAG, undefined — retro-compat)
                split.palettes.count++;
                split.palettes.colis += colis;
                if (weight > 0) split.palettes.weight += weight;
            }
        });
        return split;
    }
    ```

    **4) Étendre `buildPdf(data)` — page ULD détail (app.js:867-933)** pour ajouter la colonne Type dans l'infoBox (D-11) :

    Localiser la section qui construit l'infoBox (app.js:872-890). Agrandir infoBoxH pour contenir une ligne supplémentaire "Type : XXX" et insérer cette ligne dans le bloc.

    Remplacer le bloc infoBox actuel (app.js:872-891) par :
    ```javascript
    var ltaSet = {};
    u.rows.forEach(function(r) { if (r.lta) ltaSet[r.lta] = true; });
    // D-11 : afficher Type (VRAC/PMC/AKE/AKN/PAG) dans l'infoBox de chaque page ULD
    // Type affiche litteralement (D-13), fallback PMC si absent (retro-compat loadManifest D-15).
    var uldType = (u.type && ['PMC','AKE','AKN','PAG','VRAC'].indexOf(u.type) >= 0) ? u.type : 'PMC';
    // Hauteur infoBox : 3 lignes si weight, 2 sinon (LTA + Type obligatoire, Poids optionnel)
    var infoBoxH = u.weight > 0 ? 22 : 16;
    doc.setFillColor(240, 244, 248);
    doc.roundedRect(margin, y, pageW - margin * 2, infoBoxH, 2, 2, 'F');
    // Ligne 1 : LTA concernes
    doc.setFont('helvetica', 'bold');
    doc.setFontSize(10);
    doc.setTextColor(26, 58, 92);
    doc.text('LTA concernes :', margin + 4, y + 7);
    doc.setFont('helvetica', 'normal');
    doc.setTextColor(51, 51, 51);
    doc.text(Object.keys(ltaSet).join(', ') || 'N/A', margin + 42, y + 7);
    // Ligne 2 : Type (D-11)
    doc.setFont('helvetica', 'bold');
    doc.setTextColor(26, 58, 92);
    doc.text('Type :', margin + 4, y + 13);
    doc.setFont('helvetica', 'normal');
    doc.setTextColor(51, 51, 51);
    doc.text(uldType, margin + 22, y + 13);
    // Ligne 3 : Poids (optionnel)
    if (u.weight > 0) {
        doc.setFont('helvetica', 'bold');
        doc.setTextColor(26, 58, 92);
        doc.text('Poids :', margin + 4, y + 19);
        doc.setFont('helvetica', 'normal');
        doc.setTextColor(51, 51, 51);
        doc.text(String(u.weight) + ' kg', margin + 22, y + 19);
    }
    y += infoBoxH + 4;
    ```

    **Notes rendu PDF** :
    - Le type est une chaîne de 3-4 caractères ASCII (PMC/AKE/AKN/PAG/VRAC) : pas de problème de glyphe jsPDF (contrairement à `Bâches`).
    - Le label `Type :` s'aligne visuellement avec `Poids :` (marges identiques à margin+22).
    - `infoBoxH` passe de `u.weight > 0 ? 16 : 10` à `u.weight > 0 ? 22 : 16` pour accommoder la ligne Type.

    **5) Étendre `buildPdf(data)` — page 1 récap (app.js:801-864)** pour D-12 scission Palettes/Vrac conditionnelle :

    INSERTER entre `doc.autoTable({...})` du récap (app.js:822-833) et `var matSummary = buildMaterialSummary(data.ulds);` (app.js:836) le bloc suivant :

    ```javascript
    // D-12 : scission Palettes/Vrac sous la table recap, conditionnelle (affichee seulement si >= 1 ULD VRAC)
    var pvSplit = buildPalettesVracSplit(data.ulds);
    if (pvSplit.hasVrac) {
        var pvY = doc.lastAutoTable.finalY + 6;
        doc.setFont('helvetica', 'bold'); doc.setFontSize(10); doc.setTextColor(26, 58, 92);
        doc.text('Detail par categorie', margin, pvY);
        var pvRows = [
            ['dont Palettes', pvSplit.palettes.count + ' ULD (' + pvSplit.palettes.colis + ' colis, ' + pvSplit.palettes.weight + ' kg)'],
            ['dont Vrac', pvSplit.vrac.count + ' ULD (' + pvSplit.vrac.colis + ' colis, ' + pvSplit.vrac.weight + ' kg)']
        ];
        doc.autoTable({
            startY: pvY + 2,
            body: pvRows,
            margin: { left: margin, right: margin },
            styles: { fontSize: 9, cellPadding: 3, lineColor: [200, 210, 220], lineWidth: 0.3 },
            columnStyles: { 0: { fontStyle: 'bold', fillColor: [240, 244, 248], cellWidth: 45 } }
        });
    }
    ```

    Le bloc `var matSummary = buildMaterialSummary(...)` (ligne 836) et sa rendu totaux matériel (lignes 837-862) restent EN APRÈS la scission Palettes/Vrac. Pas de modification de cette logique ici.

    **6) Ajouter les tests dans tests/tests.html** — nouvelle suite à placer APRÈS la dernière suite `Materiel PDF - *` (tests.html:1092) et AVANT `// PLAN 01-02 : Materiel email HTML` :

    ```javascript
    // ============================================
    // PLAN 02-02 Task 1 : Type ULD - PDF (D-11, D-12, D-20)
    // ============================================

    // --- buildMaterialSummary exclut planchers VRAC (D-20) ---
    suite('Type ULD - buildMaterialSummary exclut planchers VRAC (D-20)');
    (function() {
        var s = buildMaterialSummary([
            { type: 'PMC', flooringEuCount: 3 },
            { type: 'VRAC', flooringEuCount: 10, flooringEuForfait: false }, // doit etre exclu
            { type: 'PMC', flooringEuForfait: true }
        ]);
        assertEqual('flooringEu.count = 3 (exclut les 10 du VRAC)', s.flooringEu.count, 3);
        assertEqual('flooringEu.forfaits = 1 (seul PMC avec forfait est compte)', s.flooringEu.forfaits, 1);
    })();

    // --- buildMaterialSummary VRAC n'incremente pas forfaits planchers ---
    suite('Type ULD - buildMaterialSummary VRAC ignore forfait planchers');
    (function() {
        var s = buildMaterialSummary([{ type: 'VRAC', flooringEuForfait: true, flooringStdForfait: true }]);
        assertEqual('flooringEu.forfaits = 0 (VRAC exclu)', s.flooringEu.forfaits, 0);
        assertEqual('flooringStd.forfaits = 0 (VRAC exclu)', s.flooringStd.forfaits, 0);
    })();

    // --- buildMaterialSummary VRAC conserve autres materiels ---
    suite('Type ULD - buildMaterialSummary VRAC conserve non-planchers');
    (function() {
        var s = buildMaterialSummary([
            { type: 'VRAC', strapsCount: 2, blocksCount: 3, tarpsCount: 1, dividersCount: 5, honeycombCount: 7 }
        ]);
        assertEqual('straps = 2 (conserve)', s.straps, 2);
        assertEqual('blocks = 3 (conserve)', s.blocks, 3);
        assertEqual('tarps = 1 (conserve)', s.tarps, 1);
        assertEqual('dividers = 5 (conserve)', s.dividers, 5);
        assertEqual('honeycomb = 7 (conserve)', s.honeycomb, 7);
    })();

    // --- buildUldMaterialRows exclut planchers VRAC (D-20) ---
    suite('Type ULD - buildUldMaterialRows exclut planchers VRAC (D-20)');
    (function() {
        var rows = buildUldMaterialRows({
            type: 'VRAC', strapsCount: 2, flooringEuCount: 5,
            flooringStdForfait: true, blocksCount: 1
        });
        var labels = rows.map(function(r) { return r[0]; });
        assert('Contient Sangles', labels.indexOf('Sangles') >= 0);
        assert('Contient Bois de calage', labels.indexOf('Bois de calage') >= 0);
        assert('OMET Planchers bois EU (VRAC, D-20)', labels.indexOf('Planchers bois EU') < 0);
        assert('OMET Planchers bois Standard (VRAC, D-20)', labels.indexOf('Planchers bois Standard') < 0);
    })();

    // --- buildUldMaterialRows non-VRAC conserve planchers (regression) ---
    suite('Type ULD - buildUldMaterialRows non-VRAC conserve planchers');
    (function() {
        var rows = buildUldMaterialRows({ type: 'PMC', flooringEuCount: 5 });
        var labels = rows.map(function(r) { return r[0]; });
        assert('PMC : contient Planchers bois EU', labels.indexOf('Planchers bois EU') >= 0);
    })();

    // --- buildUldMaterialRows retro-compat sans type (undefined) ---
    suite('Type ULD - buildUldMaterialRows retro-compat type absent => non-VRAC');
    (function() {
        var rows = buildUldMaterialRows({ flooringEuCount: 5 }); // pas de type
        var labels = rows.map(function(r) { return r[0]; });
        assert('Sans type : contient Planchers bois EU (retro-compat)', labels.indexOf('Planchers bois EU') >= 0);
    })();

    // --- buildPalettesVracSplit avec VRAC ---
    suite('Type ULD - buildPalettesVracSplit aggregation');
    (function() {
        var split = buildPalettesVracSplit([
            { type: 'PMC', totalColis: 10, weight: 500 },
            { type: 'VRAC', totalColis: 12, weight: 240 },
            { type: 'PMC', totalColis: 20 }
        ]);
        assertEqual('hasVrac = true', split.hasVrac, true);
        assertEqual('palettes.count = 2', split.palettes.count, 2);
        assertEqual('palettes.colis = 30', split.palettes.colis, 30);
        assertEqual('palettes.weight = 500', split.palettes.weight, 500);
        assertEqual('vrac.count = 1', split.vrac.count, 1);
        assertEqual('vrac.colis = 12', split.vrac.colis, 12);
        assertEqual('vrac.weight = 240', split.vrac.weight, 240);
    })();

    // --- buildPalettesVracSplit sans VRAC ---
    suite('Type ULD - buildPalettesVracSplit sans VRAC => hasVrac=false');
    (function() {
        var split = buildPalettesVracSplit([
            { type: 'PMC', totalColis: 10 },
            { type: 'AKE', totalColis: 5 }
        ]);
        assertEqual('hasVrac = false', split.hasVrac, false);
        assertEqual('palettes.count = 2', split.palettes.count, 2);
        assertEqual('vrac.count = 0', split.vrac.count, 0);
    })();

    // --- buildPalettesVracSplit retro-compat sans type ---
    suite('Type ULD - buildPalettesVracSplit retro-compat sans type => palette');
    (function() {
        var split = buildPalettesVracSplit([
            { totalColis: 10 }, // pas de type
            { totalColis: 5 }
        ]);
        assertEqual('hasVrac = false (aucun VRAC)', split.hasVrac, false);
        assertEqual('palettes.count = 2 (undefined != VRAC)', split.palettes.count, 2);
    })();

    // --- buildPdf ne crash pas avec VRAC + inclut Type dans appels doc.text (D-11) ---
    suite('Type ULD - buildPdf entete page ULD contient Type (D-11)');
    (function() {
        // On monkey-patch doc.text pour tracker les appels
        var jsPDF = window.jspdf.jsPDF;
        var origCtor = jsPDF;
        var allTexts = [];
        // Approche simple : rendre buildPdf, puis extraire les textes via spy en override du prototype
        // Note : jsPDF.prototype.text est la methode de rendu. On sauve l'original et on la remplace temporairement.
        var origText = jsPDF.API.text;
        jsPDF.API.text = function() {
            allTexts.push(arguments[0]);
            return origText.apply(this, arguments);
        };
        try {
            var data = {
                manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
                ulds: [{
                    uldNumber: 'V1', type: 'VRAC',
                    rows: [{ lta: 'L', dossier: 'D', colis: 5, dgr: 'N', comment: '' }],
                    totalColis: 5
                }]
            };
            buildPdf(data);
        } finally {
            jsPDF.API.text = origText;
        }
        assert('doc.text appele avec "VRAC" (dans infoBox page ULD)', allTexts.indexOf('VRAC') >= 0);
        assert('doc.text appele avec "Type :" label', allTexts.indexOf('Type :') >= 0);
    })();

    // --- buildPdf page 1 scission Palettes/Vrac quand VRAC present (D-12) ---
    suite('Type ULD - buildPdf page 1 scission Palettes/Vrac (D-12)');
    (function() {
        var jsPDF = window.jspdf.jsPDF;
        var allTexts = [];
        var origText = jsPDF.API.text;
        jsPDF.API.text = function() {
            allTexts.push(arguments[0]);
            return origText.apply(this, arguments);
        };
        // Spy aussi sur autoTable pour capturer les body rows
        var allAutoTableBodies = [];
        var origAutoTable = jsPDF.API.autoTable;
        jsPDF.API.autoTable = function(opts) {
            if (opts && opts.body) allAutoTableBodies.push(JSON.stringify(opts.body));
            return origAutoTable.apply(this, arguments);
        };
        try {
            var data = {
                manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
                ulds: [
                    { uldNumber: 'P1', type: 'PMC', rows: [{ lta: 'L', colis: 10, dgr: 'N', comment: '', dossier: '' }], totalColis: 10, weight: 500 },
                    { uldNumber: 'V1', type: 'VRAC', rows: [{ lta: 'L', colis: 12, dgr: 'N', comment: '', dossier: '' }], totalColis: 12, weight: 240 }
                ]
            };
            buildPdf(data);
        } finally {
            jsPDF.API.text = origText;
            jsPDF.API.autoTable = origAutoTable;
        }
        var allBodiesJoined = allAutoTableBodies.join('||') + '||' + allTexts.join('||');
        assert('Rendu contient "dont Palettes"', allBodiesJoined.indexOf('dont Palettes') >= 0);
        assert('Rendu contient "dont Vrac"', allBodiesJoined.indexOf('dont Vrac') >= 0);
    })();

    // --- buildPdf sans VRAC PAS de scission (backward compat) ---
    suite('Type ULD - buildPdf sans VRAC pas de scission (backward compat)');
    (function() {
        var jsPDF = window.jspdf.jsPDF;
        var allTexts = [];
        var allAutoTableBodies = [];
        var origText = jsPDF.API.text;
        var origAutoTable = jsPDF.API.autoTable;
        jsPDF.API.text = function() { allTexts.push(arguments[0]); return origText.apply(this, arguments); };
        jsPDF.API.autoTable = function(opts) { if (opts && opts.body) allAutoTableBodies.push(JSON.stringify(opts.body)); return origAutoTable.apply(this, arguments); };
        try {
            var data = {
                manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
                ulds: [{ uldNumber: 'P1', type: 'PMC', rows: [{ lta: 'L', colis: 10, dgr: 'N', comment: '', dossier: '' }], totalColis: 10 }]
            };
            buildPdf(data);
        } finally {
            jsPDF.API.text = origText;
            jsPDF.API.autoTable = origAutoTable;
        }
        var allJoined = allAutoTableBodies.join('||') + '||' + allTexts.join('||');
        assert('Aucune mention "dont Palettes" (pas de scission)', allJoined.indexOf('dont Palettes') < 0);
        assert('Aucune mention "dont Vrac" (pas de scission)', allJoined.indexOf('dont Vrac') < 0);
    })();

    // --- buildPdf retro-compat sans type ne crash pas ---
    suite('Type ULD - buildPdf retro-compat sans type ne crash pas');
    (function() {
        var data = {
            manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [{ uldNumber: 'U1', rows: [{ lta: 'L', colis: 1, dgr: 'N', comment: '', dossier: '' }], totalColis: 1 }]
        };
        var doc = null, err = null;
        try { doc = buildPdf(data); } catch(e) { err = e; }
        assert('Pas d\'erreur (retro-compat sans type)', err === null);
        assert('Doc genere', !!doc);
    })();
    ```

    **Ne PAS modifier** : `buildMaterialSummaryHtml`, `buildUldMaterialHtml`, `sendEmail` — Task 2 du même plan.
  </action>
  <verify>
    <automated>grep -c "u\\.type !== 'VRAC'" static/js/app.js returns >= 1 AND grep -c "isVrac = u\\.type === 'VRAC'" static/js/app.js returns 1 AND grep -c "function buildPalettesVracSplit" static/js/app.js returns 1 AND grep -c "'Type :'" static/js/app.js returns >= 1 AND grep -c "dont Palettes" static/js/app.js returns >= 1 AND grep -c "dont Vrac" static/js/app.js returns >= 2 AND grep -cE "suite\\(.Type ULD - (buildMaterialSummary|buildUldMaterialRows|buildPalettesVracSplit|buildPdf)" tests/tests.html returns >= 8</automated>
  </verify>
  <done>
    - `grep -n "u\\.type !== 'VRAC'" static/js/app.js` retourne >= 1 ligne (dans buildMaterialSummary)
    - `grep -n "isVrac = u\\.type === 'VRAC'" static/js/app.js` retourne exactement 1 ligne (dans buildUldMaterialRows)
    - `grep -n "function buildPalettesVracSplit" static/js/app.js` retourne 1 ligne
    - `grep -n "'Type :'" static/js/app.js` retourne >= 1 ligne (dans buildPdf infoBox)
    - `grep -n "dont Palettes" static/js/app.js` retourne >= 1 ligne (dans buildPdf page 1)
    - `grep -n "dont Vrac" static/js/app.js` retourne >= 2 lignes (updateRecap + buildPdf page 1)
    - En local (`npx serve .`) : créer un manifeste 1 PMC + 1 VRAC, générer PDF → page 1 contient une section "Detail par categorie" avec "dont Palettes : 1 ULD (10 colis, 500 kg)" et "dont Vrac : 1 ULD (12 colis, 240 kg)"
    - Page détail ULD VRAC du PDF : l'infoBox contient "Type : VRAC" entre "LTA concernes" et "Poids"
    - Page détail ULD VRAC du PDF : section Matériel ne contient PAS "Planchers bois EU" ni "Planchers bois Standard" même si ces champs ont été saisis
    - Totaux matériel page 1 excluent les planchers des ULD VRAC
    - `tests/tests.html` → suites `Type ULD - buildMaterialSummary/buildUldMaterialRows/buildPalettesVracSplit/buildPdf` toutes en vert
    - Suites existantes `Materiel PDF - buildUldMaterialRows omet zeros`, `Materiel PDF - buildUldMaterialRows forfait litteral` restent vertes (régression non introduite)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Email HTML miroir PDF — D-14 type dans titre ULD + scission Palettes/Vrac + esc() defense en profondeur + tests</name>
  <files>static/js/app.js, tests/tests.html</files>
  <read_first>
    - static/js/app.js (état après Task 1 : buildMaterialSummary étendu, buildUldMaterialRows étendu, buildPalettesVracSplit créé, buildPdf étendu — Task 2 email miroir doit rester cohérent)
    - static/js/app.js lignes 957-1055 (sendEmail entière — identifier les 2 points d'insertion : après table récap ligne 1004, et dans le forEach ULD ligne 1010-1025)
    - static/js/app.js lignes 679-724 (buildMaterialSummaryHtml et buildUldMaterialHtml — déjà corrects pour D-20 puisqu'ils consomment buildMaterialSummary et buildUldMaterialRows qui sont étendus en Task 1 — pas de changement de signature ici)
    - .planning/phases/02-type-uld-vrac/02-CONTEXT.md (D-14 miroir exact, esc() sur type défense en profondeur)
    - .planning/phases/01-mat-riel-uld-r-tro-compat/01-02-SUMMARY.md (structure email HTML existante, patterns esc() + inline styles)
    - tests/tests.html lignes 1094-1206 (suites 'Materiel email HTML - *' — pattern pour nouveaux tests email)
    - CLAUDE.md (rappel : esc() MANDATORY sur toute donnée user dans innerHTML ; type est contraint mais défense en profondeur exigée)
  </read_first>
  <behavior>
    - Test 1 (sendEmail inclut scission Palettes/Vrac dans HTML) : Spy le fetch ou intercepter la construction du html via monkey-patch. Créer une ULD PMC + 1 VRAC, appeler sendEmail (fetch va échouer en test mais html est construit avant). Le html émis contient `dont Palettes` ET `dont Vrac`.
    - Test 2 (sendEmail pas de scission si aucun VRAC) : Créer 2 ULD PMC. Le html émis NE contient PAS `dont Palettes` ni `dont Vrac`.
    - Test 3 (sendEmail titre bloc ULD contient le type) : ULD VRAC V1. Le html émis contient un bloc avec `ULD : V1` ET la mention `VRAC` visible dans le titre (format : `ULD : V1 [VRAC] — ...` ou `ULD : V1 (VRAC) — ...` à la discrétion du planner, tant qu'on voit VRAC).
    - Test 4 (sendEmail type absent rétro-compat) : ULD sans clé type. Le html émis affiche `PMC` (fallback) dans le titre ou l'entête.
    - Test 5 (sendEmail defense en profondeur XSS sur type) : Injecter `u.type = '<img src=x onerror="window._xss++">'` (payload direct, pas via select contraint — test du code défensif). Le html émis contient `&lt;img` (échappé), aucune balise img injectée dans le DOM après test injection, window._xss reste à 0.
    - Test 6 (sendEmail totaux matériel miroir exclut planchers VRAC) : ULD VRAC avec flooringEu=5 + 1 ULD PMC avec flooringEu=3. Le html des totaux matériel montre `Planchers bois EU: 3` (pas 8 — exclusion du VRAC, cohérence avec Task 1).
  </behavior>
  <action>
    **1) Étendre `sendEmail()` dans static/js/app.js (app.js:957-1055)** — 2 modifications :

    **A. Ajouter la scission Palettes/Vrac après la table récap** (point d'insertion : après la ligne 1004 `html += '</table>';` et avant la ligne 1007 `html += buildMaterialSummaryHtml(data.ulds);`) :

    ```javascript
    // D-14 miroir PDF D-12 : scission Palettes/Vrac si >= 1 ULD VRAC
    var pvSplit = buildPalettesVracSplit(data.ulds);
    if (pvSplit.hasVrac) {
        html += '<h3 style="color:#1a3a5c;margin-top:14px;">Detail par categorie</h3>';
        html += '<table style="border-collapse:collapse;font-size:13px;">';
        html += '<tr><td style="padding:4px 10px;background:#f0f4f8;font-weight:600;">dont Palettes</td>';
        html += '<td style="padding:4px 10px;">' + pvSplit.palettes.count + ' ULD (' + pvSplit.palettes.colis + ' colis, ' + pvSplit.palettes.weight + ' kg)</td></tr>';
        html += '<tr><td style="padding:4px 10px;background:#f0f4f8;font-weight:600;">dont Vrac</td>';
        html += '<td style="padding:4px 10px;">' + pvSplit.vrac.count + ' ULD (' + pvSplit.vrac.colis + ' colis, ' + pvSplit.vrac.weight + ' kg)</td></tr>';
        html += '</table>';
    }
    ```

    Les valeurs sont des entiers (int/float via parseInt/parseFloat dans buildPalettesVracSplit), pas besoin d'esc() — mais cohérent avec le pattern buildMaterialSummaryHtml, aucun XSS possible.

    **B. Modifier le titre de chaque bloc ULD** (point d'insertion : app.js:1011-1012). Remplacer :
    ```javascript
    var uldTitle = 'ULD : ' + esc(u.uldNumber);
    if (u.weight > 0) uldTitle += ' — ' + u.weight + ' kg';
    ```

    Par :
    ```javascript
    // D-14 miroir PDF D-11 : inclure le type dans le titre du bloc ULD
    // Defense en profondeur (D-14) : esc(u.type) meme si contraint par <select>
    var validUldType = (u.type && ['PMC','AKE','AKN','PAG','VRAC'].indexOf(u.type) >= 0) ? u.type : 'PMC';
    var uldTitle = 'ULD : ' + esc(u.uldNumber) + ' [' + esc(validUldType) + ']';
    if (u.weight > 0) uldTitle += ' — ' + u.weight + ' kg';
    ```

    **Note sur la validation `validUldType`** : si u.type provient directement de data.ulds[i] (via collectData en Plan 02-01) il est déjà validé contre ULD_TYPES et sera toujours dans la liste. Mais ce code est défensif (data peut venir d'un appel direct ou manifeste altéré). Le fallback PMC couvre ce cas.

    **Note sur `esc(validUldType)` : même si validUldType est déjà dans la liste figée (donc sûr), on applique esc() par principe de défense en profondeur (D-14 explicite dans CONTEXT.md). Coût nul, bénéfice de robustesse.

    **2) Vérifier que les helpers HTML email ne doivent PAS être modifiés** :
    - `buildMaterialSummaryHtml(ulds)` (app.js:682-707) — utilise `buildMaterialSummary` qui est étendu en Task 1 → l'exclusion planchers VRAC est automatique, ZERO modif nécessaire.
    - `buildUldMaterialHtml(u)` (app.js:712-724) — utilise `buildUldMaterialRows(u)` qui est étendu en Task 1 → l'exclusion planchers VRAC est automatique, ZERO modif nécessaire.

    **3) Ajouter les tests dans tests/tests.html** — nouvelle suite à placer APRÈS la dernière suite `Materiel email HTML - *` (tests.html:~1207) et AVANT `// ---- Storage FIFO max 50 ----` :

    ```javascript
    // ============================================
    // PLAN 02-02 Task 2 : Type ULD - email HTML (D-14, D-20)
    // ============================================

    // Helper local pour tests email : on reconstruit le HTML comme sendEmail le fait,
    // sans envoyer la requete reseau. On simule le flow suivant app.js sendEmail.
    function buildEmailHtmlForTest(data) {
        var allLtas = getAllLtas(data).join(', ') || '-';
        var grandTotal = data.ulds.reduce(function(s, u) { return s + u.totalColis; }, 0);
        var html = '<div style="font-family:Arial,sans-serif;max-width:700px;">';
        html += '<h2 style="color:#1a3a5c;">Loadsheet - ' + esc(data.manifestId) + '</h2>';
        html += '<p><strong>Client :</strong> ' + esc(data.client || '-') + ' | <strong>Dest :</strong> ' + esc(data.destAirport) + ' | <strong>Agent :</strong> ' + esc(data.agent) + '</p>';
        html += '<p><strong>Date :</strong> ' + esc(data.date) + ' | <strong>LTA :</strong> ' + esc(allLtas) + '</p>';
        html += '<hr style="border-color:#1a3a5c;">';
        html += '<h3 style="color:#1a3a5c;">Recapitulatif (' + data.ulds.length + ' ULD - ' + grandTotal + ' colis)</h3>';
        var emailHasWeight = data.ulds.some(function(u) { return u.weight > 0; });
        html += '<table style="width:100%;border-collapse:collapse;font-size:14px;">';
        html += emailHasWeight
            ? '<tr style="background:#1a3a5c;color:#fff;"><th style="padding:8px;text-align:left;">ULD</th><th>LTA(s)</th><th>Nb Colis</th><th>Poids (kg)</th><th>DGR</th></tr>'
            : '<tr style="background:#1a3a5c;color:#fff;"><th style="padding:8px;text-align:left;">ULD</th><th>LTA(s)</th><th>Nb Colis</th><th>DGR</th></tr>';
        data.ulds.forEach(function(u, idx) {
            var ltaSet = {};
            u.rows.forEach(function(r) { if (r.lta) ltaSet[r.lta] = true; });
            var bg = idx % 2 === 0 ? '#f5f7fa' : '#fff';
            var hasDgr = u.rows.some(function(r) { return r.dgr === 'O'; });
            html += '<tr style="background:' + bg + ';"><td style="padding:6px 8px;">' + esc(u.uldNumber) + '</td><td>' + esc(Object.keys(ltaSet).join(', ')) + '</td><td style="text-align:center;">' + u.totalColis + '</td>';
            if (emailHasWeight) html += '<td style="text-align:center;">' + (u.weight ? u.weight : '-') + '</td>';
            html += '<td style="text-align:center;' + (hasDgr ? 'color:red;font-weight:bold;' : '') + '">' + (hasDgr ? 'OUI' : 'NON') + '</td></tr>';
        });
        var emailGrandWeight = data.ulds.reduce(function(s, u) { return s + (u.weight || 0); }, 0);
        if (emailHasWeight) {
            html += '<tr style="background:#e2e8f0;font-weight:bold;"><td style="padding:6px 8px;">TOTAL</td><td></td><td style="text-align:center;">' + grandTotal + '</td><td style="text-align:center;">' + emailGrandWeight + '</td><td></td></tr>';
        } else {
            html += '<tr style="background:#e2e8f0;font-weight:bold;"><td style="padding:6px 8px;">TOTAL</td><td></td><td style="text-align:center;">' + grandTotal + '</td><td></td></tr>';
        }
        html += '</table>';
        // D-14 miroir D-12 : scission Palettes/Vrac
        var pvSplit = buildPalettesVracSplit(data.ulds);
        if (pvSplit.hasVrac) {
            html += '<h3 style="color:#1a3a5c;margin-top:14px;">Detail par categorie</h3>';
            html += '<table style="border-collapse:collapse;font-size:13px;">';
            html += '<tr><td style="padding:4px 10px;background:#f0f4f8;font-weight:600;">dont Palettes</td>';
            html += '<td style="padding:4px 10px;">' + pvSplit.palettes.count + ' ULD (' + pvSplit.palettes.colis + ' colis, ' + pvSplit.palettes.weight + ' kg)</td></tr>';
            html += '<tr><td style="padding:4px 10px;background:#f0f4f8;font-weight:600;">dont Vrac</td>';
            html += '<td style="padding:4px 10px;">' + pvSplit.vrac.count + ' ULD (' + pvSplit.vrac.colis + ' colis, ' + pvSplit.vrac.weight + ' kg)</td></tr>';
            html += '</table>';
        }
        html += buildMaterialSummaryHtml(data.ulds);
        data.ulds.forEach(function(u) {
            var validUldType = (u.type && ['PMC','AKE','AKN','PAG','VRAC'].indexOf(u.type) >= 0) ? u.type : 'PMC';
            var uldTitle = 'ULD : ' + esc(u.uldNumber) + ' [' + esc(validUldType) + ']';
            if (u.weight > 0) uldTitle += ' — ' + u.weight + ' kg';
            html += '<h3 style="color:#1a3a5c;margin-top:20px;">' + uldTitle + '</h3>';
            html += '<table style="width:100%;border-collapse:collapse;font-size:13px;">';
            html += '<tr style="background:#1a3a5c;color:#fff;"><th style="padding:6px;">LTA</th><th>Dossier</th><th>Colis</th><th>DGR</th><th>Commentaire</th></tr>';
            u.rows.forEach(function(r, idx) {
                var bg = idx % 2 === 0 ? '#f5f7fa' : '#fff';
                html += '<tr style="background:' + bg + ';"><td style="padding:4px 6px;">' + esc(r.lta) + '</td><td>' + esc(r.dossier) + '</td><td style="text-align:center;">' + r.colis + '</td><td style="text-align:center;' + (r.dgr === 'O' ? 'color:red;font-weight:bold;' : '') + '">' + esc(r.dgr) + '</td><td>' + esc(r.comment) + '</td></tr>';
            });
            html += '<tr style="background:#e2e8f0;font-weight:bold;"><td></td><td>TOTAL</td><td style="text-align:center;">' + u.totalColis + '</td><td></td><td></td></tr>';
            html += '</table>';
            html += buildUldMaterialHtml(u);
        });
        html += '</div>';
        return html;
    }

    // --- sendEmail HTML scission Palettes/Vrac (D-14 mirror D-12) ---
    suite('Type ULD - email HTML scission Palettes/Vrac (D-14)');
    (function() {
        var data = {
            manifestId: 'M', client: 'C', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [
                { uldNumber: 'P1', type: 'PMC', rows: [{ lta: 'L', colis: 10, dgr: 'N', comment: '', dossier: '' }], totalColis: 10, weight: 500 },
                { uldNumber: 'V1', type: 'VRAC', rows: [{ lta: 'L', colis: 12, dgr: 'N', comment: '', dossier: '' }], totalColis: 12, weight: 240 }
            ]
        };
        var html = buildEmailHtmlForTest(data);
        assert('HTML contient "dont Palettes"', html.indexOf('dont Palettes') >= 0);
        assert('HTML contient "dont Vrac"', html.indexOf('dont Vrac') >= 0);
        assert('HTML contient "1 ULD (10 colis, 500 kg)" pour palettes', html.indexOf('1 ULD (10 colis, 500 kg)') >= 0);
        assert('HTML contient "1 ULD (12 colis, 240 kg)" pour vrac', html.indexOf('1 ULD (12 colis, 240 kg)') >= 0);
    })();

    // --- sendEmail HTML pas de scission si aucun VRAC ---
    suite('Type ULD - email HTML pas de scission si aucun VRAC');
    (function() {
        var data = {
            manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [
                { uldNumber: 'P1', type: 'PMC', rows: [{ lta: 'L', colis: 10, dgr: 'N', comment: '', dossier: '' }], totalColis: 10 },
                { uldNumber: 'P2', type: 'AKE', rows: [{ lta: 'L', colis: 5, dgr: 'N', comment: '', dossier: '' }], totalColis: 5 }
            ]
        };
        var html = buildEmailHtmlForTest(data);
        assert('HTML NE contient PAS "dont Palettes"', html.indexOf('dont Palettes') < 0);
        assert('HTML NE contient PAS "dont Vrac"', html.indexOf('dont Vrac') < 0);
    })();

    // --- sendEmail HTML titre bloc ULD contient type (D-14 mirror D-11) ---
    suite('Type ULD - email HTML titre bloc contient type (D-14)');
    (function() {
        var data = {
            manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [{ uldNumber: 'V1', type: 'VRAC', rows: [{ lta: 'L', colis: 5, dgr: 'N', comment: '', dossier: '' }], totalColis: 5 }]
        };
        var html = buildEmailHtmlForTest(data);
        // Le titre doit contenir le format "ULD : V1 [VRAC]"
        assert('HTML contient "[VRAC]" dans le titre du bloc', html.indexOf('[VRAC]') >= 0);
    })();

    // --- sendEmail HTML type absent fallback PMC (retro-compat) ---
    suite('Type ULD - email HTML retro-compat type absent => PMC');
    (function() {
        var data = {
            manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [{ uldNumber: 'U1', rows: [{ lta: 'L', colis: 1, dgr: 'N', comment: '', dossier: '' }], totalColis: 1 }] // pas de type
        };
        var html = buildEmailHtmlForTest(data);
        assert('HTML contient "[PMC]" (fallback)', html.indexOf('[PMC]') >= 0);
    })();

    // --- sendEmail HTML defense en profondeur XSS sur type ---
    suite('Type ULD - email HTML defense XSS type (D-14)');
    (function() {
        window._xss = 0;
        var data = {
            manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [{
                uldNumber: 'U1',
                type: '<img src=x onerror="window._xss++">', // payload direct (ne vient PAS d'un select contraint)
                rows: [{ lta: 'L', colis: 1, dgr: 'N', comment: '', dossier: '' }],
                totalColis: 1
            }]
        };
        var html = buildEmailHtmlForTest(data);
        // Le type invalide doit fallback a PMC (code defensif sur la validation validUldType)
        assert('HTML contient [PMC] (type invalide fallback)', html.indexOf('[PMC]') >= 0);
        assert('HTML NE contient PAS <img src=x onerror=... litteral', html.indexOf('<img src=x onerror=') < 0);
        // Injecter dans le DOM pour prouver pas d'execution
        var testDiv = document.createElement('div');
        testDiv.innerHTML = html;
        document.body.appendChild(testDiv);
        assertEqual('Aucune balise img injectee', testDiv.querySelectorAll('img').length, 0);
        assertEqual('window._xss inchange (pas d\'execution XSS)', window._xss, 0);
        testDiv.remove();
    })();

    // --- sendEmail HTML totaux materiel excluent planchers VRAC (D-20) ---
    suite('Type ULD - email HTML totaux materiel excluent planchers VRAC (D-20)');
    (function() {
        var data = {
            manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [
                { uldNumber: 'P1', type: 'PMC', rows: [{ lta: 'L', colis: 1, dgr: 'N', comment: '', dossier: '' }], totalColis: 1, flooringEuCount: 3 },
                { uldNumber: 'V1', type: 'VRAC', rows: [{ lta: 'L', colis: 1, dgr: 'N', comment: '', dossier: '' }], totalColis: 1, flooringEuCount: 5 } // doit etre exclu
            ]
        };
        var html = buildEmailHtmlForTest(data);
        // Les totaux materiel affichent seulement 3 (PMC), pas 8
        assert('Totaux materiel contiennent "Planchers bois EU" label', html.indexOf('Planchers bois EU') >= 0);
        // Extraire le contenu des totaux materiel (sous le <h4>Totaux matériel</h4>)
        var idx = html.indexOf('Totaux matériel');
        assert('Section Totaux materiel presente', idx >= 0);
        var totalsSection = html.substring(idx, idx + 500);
        assert('Totaux contiennent "3" (PMC only, pas 8=3+5)', />3</.test(totalsSection));
        assert('Totaux NE contiennent PAS "8"', />8</.test(totalsSection) === false);
    })();

    // --- sendEmail HTML section par ULD VRAC exclut planchers (D-20) ---
    suite('Type ULD - email HTML bloc ULD VRAC exclut planchers (D-20)');
    (function() {
        var data = {
            manifestId: 'M', client: '', agent: 'A', destAirport: 'CDG', date: '01/01/2025',
            ulds: [{
                uldNumber: 'V1', type: 'VRAC',
                rows: [{ lta: 'L', colis: 1, dgr: 'N', comment: '', dossier: '' }],
                totalColis: 1,
                strapsCount: 2, flooringEuCount: 5, flooringStdForfait: true
            }]
        };
        var html = buildEmailHtmlForTest(data);
        // Section Materiel du bloc VRAC : Sangles oui, Planchers non
        var idxUld = html.indexOf('[VRAC]');
        assert('Bloc VRAC present', idxUld >= 0);
        var uldBlock = html.substring(idxUld);
        assert('Bloc VRAC contient Sangles', uldBlock.indexOf('Sangles') >= 0);
        assert('Bloc VRAC NE contient PAS "Planchers bois EU"', uldBlock.indexOf('Planchers bois EU') < 0);
        assert('Bloc VRAC NE contient PAS "Planchers bois Standard"', uldBlock.indexOf('Planchers bois Standard') < 0);
    })();
    ```

    **Note importante sur buildEmailHtmlForTest** : cette fonction est un helper LOCAL aux tests (défini dans le script de tests.html), MIROIR du code de sendEmail. Elle permet de tester le rendu HTML sans invoquer fetch/network. Le maintien du miroir est de la responsabilité du code reviewer humain (règle : si sendEmail change, buildEmailHtmlForTest doit suivre). Alternative plus robuste serait d'extraire buildEmailHtml(data) dans app.js comme helper réutilisable — mais c'est un refactor hors scope, préférable dans une phase future.

    **Ne PAS modifier** dans cette Task 2 : `buildPdf`, `buildMaterialSummary`, `buildUldMaterialRows`, `buildPalettesVracSplit` — ces fonctions sont finalisées en Task 1.
  </action>
  <verify>
    <automated>grep -cE "buildPalettesVracSplit\\(data\\.ulds\\)" static/js/app.js returns >= 2 AND grep -c "dont Palettes" static/js/app.js returns >= 2 AND grep -c "dont Vrac" static/js/app.js returns >= 3 AND grep -c "esc\\(validUldType\\)" static/js/app.js returns 1 AND grep -c "esc\\(u\\.type\\)" static/js/app.js returns 0 AND grep -cE "suite\\(.Type ULD - email HTML" tests/tests.html returns >= 6</automated>
  </verify>
  <done>
    - `grep -n "buildPalettesVracSplit(data.ulds)" static/js/app.js` retourne >= 2 lignes (buildPdf de Task 1 + sendEmail de cette Task 2)
    - `grep -n "dont Palettes" static/js/app.js` retourne >= 2 lignes (buildPdf + sendEmail)
    - `grep -n "dont Vrac" static/js/app.js` retourne >= 3 lignes (updateRecap + buildPdf + sendEmail)
    - `grep -n "esc(validUldType)" static/js/app.js` retourne exactement 1 ligne (dans sendEmail)
    - `grep -nE "suite\\(.Type ULD - email HTML" tests/tests.html` retourne >= 6 nouvelles suites
    - En local (`npx serve .`) : manifeste 1 PMC + 1 VRAC, cliquer "Envoyer par email" (l'appel fetch va échouer sans config SMTP mais le body JSON est envoyé) → inspecter dans DevTools Network le payload `htmlBody`: il contient "dont Palettes", "dont Vrac", et le titre "[VRAC]" du bloc VRAC
    - Manifeste 100% PMC : le htmlBody NE contient PAS "dont Palettes" ni "dont Vrac" (backward compat)
    - Manifeste avec ULD VRAC + flooringEu=5 : la section "Totaux matériel" et la section "Matériel" du bloc VRAC N'affichent PAS "Planchers bois EU" (D-20)
    - `tests/tests.html` → toutes les suites `Type ULD - email HTML - *` en vert
    - Aucune régression sur les suites existantes `Materiel email HTML - *` (elles sont construites avec les helpers Phase 1 qui ont été étendus — le comportement pour les ULD sans type/non-VRAC est inchangé)
  </done>
</task>

</tasks>

<verification>
Phase 2 Plan 02-02 vérifications globales (à tester en local via `npx serve .` avant push) :

1. **D-11 colonne Type par page ULD PDF** : créer un manifeste 1 ULD VRAC, générer PDF → page 2 (détail ULD VRAC) a "Type : VRAC" dans l'infoBox, visible entre "LTA concernes" et "Poids".
2. **D-12 scission Palettes/Vrac page 1 PDF** : manifeste 2 PMC + 1 VRAC → page 1 contient la section "Detail par categorie" avec "dont Palettes : 2 ULD (X colis, Y kg)" et "dont Vrac : 1 ULD (12 colis, 240 kg)".
3. **D-12 pas de scission sans VRAC** : manifeste 100% PMC → page 1 du PDF identique au format Phase 1 (pas de section "Detail par categorie" parasite).
4. **D-14 miroir PDF dans email HTML** : envoyer par email (DevTools Network) → htmlBody contient les mêmes sections "dont Palettes" + "dont Vrac" ET chaque bloc ULD a le type dans le titre (`ULD : X [VRAC] — ...`).
5. **D-20 exclusion planchers VRAC dans totaux page 1 PDF** : manifeste 1 PMC avec flooringEu=3 + 1 VRAC avec flooringEu=5. Page 1 PDF → section "Totaux materiel" affiche "Planchers bois EU: 3" (pas 8).
6. **D-20 exclusion planchers dans page détail ULD VRAC PDF** : ULD VRAC avec flooringEu=5 + straps=2. Page détail VRAC du PDF → section "Materiel" affiche "Sangles: 2" mais PAS de ligne "Planchers bois EU".
7. **D-20 exclusion miroir dans email HTML** : même manifeste → email htmlBody a les mêmes exclusions (totaux + détail par bloc VRAC).
8. **Rétro-compat sans type (manifeste Phase 0 ou Phase 1 pre-Plan 02-01)** : manifeste ancien sans clé type → buildPdf et sendEmail ne crashent pas. Le type est traité comme non-VRAC (pas de scission, pas d'exclusion planchers). Comportement Phase 1 préservé.
9. **Défense en profondeur XSS sur type** : simuler un manifeste corrompu avec `u.type = '<script>alert(1)</script>'` → le rendu fallback à 'PMC', aucune alert, aucune balise script injectée dans le HTML email.
10. **Tests** : `tests/tests.html` → toutes les nouvelles suites `Type ULD - buildMaterialSummary/buildUldMaterialRows/buildPalettesVracSplit/buildPdf/email HTML` en vert, aucune régression sur les suites existantes Phase 1 (Materiel PDF, Materiel email HTML, SECU, etc.). Compteur global >= 380+ tests passés.
</verification>

<success_criteria>
- `buildMaterialSummary` exclut les planchers EU + Std des ULD VRAC (D-20)
- `buildUldMaterialRows` exclut les planchers EU + Std pour u.type === 'VRAC' (D-20)
- `buildPalettesVracSplit(ulds)` existe et retourne `{ hasVrac, palettes:{count,colis,weight}, vrac:{count,colis,weight} }`
- `buildPdf` page ULD : infoBox contient "Type : XXX" (D-11)
- `buildPdf` page 1 : scission Palettes/Vrac via autoTable conditionnel (D-12)
- `sendEmail` : scission Palettes/Vrac dans html après table récap + avant totaux matériel (D-14 miroir D-12)
- `sendEmail` : titre de chaque bloc ULD inclut `[TYPE]` avec esc() défense en profondeur (D-14 miroir D-11)
- Rétro-compat : sans clé `type`, le code fallback à 'PMC' partout (validUldType) et PDF/email ne crashent pas
- ≥ 12 nouvelles suites de tests `Type ULD - *` couvrant D-11, D-12, D-14, D-20 (8 Task 1 + 6 Task 2 minimum)
- Aucune régression sur les suites Phase 1 existantes
- VRAC-03 strict : ligne "Vrac" séparée présente dans récap écran (via Plan 02-01), PDF page 1 (via D-12), et email HTML (via D-14 miroir)
</success_criteria>

<output>
After completion, create `.planning/phases/02-type-uld-vrac/02-02-SUMMARY.md` documenting :
- Décisions appliquées : D-11, D-12, D-13 (type littéral), D-14, D-20
- Fonction publique ajoutée : `buildPalettesVracSplit(ulds)` (signature + structure retournée)
- Helpers étendus : `buildMaterialSummary` (D-20 skip planchers VRAC), `buildUldMaterialRows` (D-20 skip planchers VRAC)
- Point d'insertion PDF page 1 : doc.autoTable conditionnel après doc.lastAutoTable.finalY de la table récap, avant les totaux matériel
- Point d'insertion PDF page ULD : ligne "Type : XXX" dans l'infoBox entre LTA et Poids (infoBoxH étendu)
- Point d'insertion email HTML : table scission après table récap + esc(validUldType) dans titre <h3> de chaque bloc ULD
- Note sur buildEmailHtmlForTest : helper de test miroir, à maintenir sync avec sendEmail
- Liste exacte des tests ajoutés (noms des suites + couverture décisions)
- Statut VRAC-03 : SATISFIED (ligne Vrac dans récap écran + PDF + email)
- Tout écart par rapport à CONTEXT.md D-11/D-12/D-14/D-20 et justification (ex: format exact du titre ULD HTML `[VRAC]` vs `(VRAC)` — discrétion planner)
- Pointer vers Phase 3 pour TEST-01 (couverture complète) et TEST-03 (validation end-to-end locale)
</output>
</content>
</invoke>
